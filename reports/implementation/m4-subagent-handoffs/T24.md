# T24 — Independent Acceptance Auditor — handoff

**Task:** T24 (Milestone 4, workstream 18). **Date:** 2026-09-06/07 UTC.
**Verdict:** **ACCEPT-WITH-CAVEATS** (full report: `reports/implementation/m4-audit-report.md`).
**HEAD audited:** `cec6d85fe3b6fdadcd15438cb93b9fe17a864a4c` (moved during the audit: `13c6b3a` → `63d5527` → `cec6d85`, coordinator committing concurrently).

## Method

Treated all prior reports as untrusted. Recomputed every hash myself; executed both empty-volume boots myself; ran the acceptance evaluator myself. Owned/wrote only: `m4-audit-report.md`, this file, `reports/implementation/m4-finalize-boots.json`, and boot evidence under `RED_LOOP/runs/m4-audit-t24-20260906T214001Z/`. Did not edit source/tests/substitutes/config/evaluator.

## Commands run (key ones) + outputs

| Step | Command | Result |
|---|---|---|
| Git | `git cat-file -t red-loop-m3.1`; `rev-parse 'red-loop-m3.1^{commit}'`; `merge-base --is-ancestor` | annotated tag / `3a044f8` / ancestor ✔ |
| M4 hashes | `shasum -a 256` over 13 evidence files + 19 manifest entries | 13/13 present non-zero; **manifest 6 mismatch** (stale @ `49966f2d`) = G68 |
| M3.1 G02 | recompute `m3-1-acceptance.json` evidence map + archive self-hash | 8/9 match; `m31-calibration-crossprovider.json` HEAD copy regenerated by M4 `139c3a3` (**tag intact**: `git show red-loop-m3.1:…`=`fdfc05bc`=record); archive self-hash `266756e4` = companion ✔ |
| Snapshot | python capture of networks/volumes/container-ids/enforce | `RED_LOOP/runs/m4-audit-t24-20260906T214001Z/live-arena-snapshot-*.json` |
| Stop live | `cd ENV2_COMPOSE && docker compose --env-file .env.arena --profile datastores --profile substitutes --profile core stop` | 0 running, 16 volumes kept |
| Boot A | `KONG_ENFORCE_ROUTE_POLICY=1 ARENA_HOST_BRIDGE=1 bash ENV2_COMPOSE/scripts/clean-boot.sh m4-final-a` | 27p/1xf/**9f**/0e, egress 0/10122, boot_id `11a7792f` |
| Provision A | isolated runner (redirects `config.ENV2`→instance workcopy) → `provision_direct_merchant`+`self_check`+`prove` | **ARENAD87268730**, 38/0-fail, self-check **29/29**, proof **ok=true** |
| Teardown A | `python3 ENV2_COMPOSE/scripts/instance-cleanup.py m4-final-a` | CLEAN=True |
| Boot B | same, `m4-final-b` | 27p/1xf/**9f**/0e, egress 0/8889, boot_id `6bd1b49f` |
| Provision B | isolated runner | **ARENAD80787200**, 38/0-fail, self-check **29/29**, proof **ok=true** |
| Teardown B | `instance-cleanup.py m4-final-b` | CLEAN=True |
| Restart live | `docker compose … --profile … start` | 67 running / 66 healthy, nets unchanged, container-id set 68=68 identical, enforce=1 ✔ |
| Evaluator | `python3 RED_LOOP/surface/m4_acceptance.py --dry-run` | **71 pass / 2 pending (G71,G73) / 1 fail (G68)** |

Also ran a **verbatim frozen boot first** (`m4-final-a-FROZEN`, boot_id `9b8ac7d1`, enforce=0): the 9 boundary tests **ERROR** (hardened-edge guard) rather than fail; egress 0/8320. Preserved under `…/m4-final-a-FROZEN/`.

## Disputes / findings

1. **G68 (fail) — evidence manifest stale.** Generated @ `49966f2d`; 6 files changed by later commits (`m4-hypothesis-lifecycle.json`, `m4-route-coverage.json`, `m4-invariant-results.json`, `m4-decisions.md`, `m4-contradictions.md`, `m4-fidelity-matrix.csv`). **Hard blocker — regenerate before tag.**
2. **Clean-boot verifier not all-green.** 9 M4 boundary tests (`test_m4_g46…g52`) require live-seeded `ARENA_M1/M2_KEY_SECRET`, absent on empty volumes → fail (hardened) / error (frozen). **Not regressions** (27 non-boundary pass, 0 failures; same tests pass on live seeded arena — evaluator scores G46–G50/G52 pass). Reported honestly as `both_2600=false`. Coordinator: skip/deselect these on empty-volume boots.
3. **M3.1 evidence hygiene.** `m31-calibration-crossprovider.json` overwritten at HEAD by M4 `139c3a3`; tag intact, but an accepted-M3.1-named file no longer matches its M3.1 record on this branch.
4. **G72 parser fragile** (`m4_acceptance.py:757-759`, naive substring). Harden to read the `## VERDICT:` line.
5. **Provisioner vs clean-boot secrets.** `clean-boot.sh` forces `REGEN_SECRETS=1`; the main-tree provisioner can't auth against the regenerated instance. I redirected `config.ENV2`/`SEEDS` at the instance working copy (read-only; **no main-tree secret modified**, so the stopped live arena restarts cleanly). Tooling gap, not a twin defect.

## Self-disclosure (corrected)

My first `m4_direct_provision.py all` (before the isolated runner) wrote a **failed** result over committed `reports/implementation/m4-direct-provision.json`. Detected via the evaluator, **restored from HEAD** (`git checkout HEAD --`; hash back to `2591bad7…`, matches manifest). All later provisioning used the isolated runner writing only to `RED_LOOP/runs/`. No committed evidence left modified.

## Live-arena state at finish

**RESTORED and healthy.** Project `env2_compose`: 67 running / 66 healthy / 0 unhealthy, `rzp-arena bedd720758fa` (internal=true) + `rzp-ingress 72e15908c82e` unchanged, 16 named volumes, container-id set identical to snapshot (68=68), `KONG_ENFORCE_ROUTE_POLICY=1`, kong up. No disposable-instance resources remain (both `instance-cleanup` = CLEAN=True). `down` never used on the live project.

## Confidence

High: git/tag integrity, hash recompute, both boots' verifier+egress+provisioning (machine-derived, artifacts retained in `RED_LOOP/runs/m4-audit-t24-20260906T214001Z/`), evaluator re-run, live restoration (container-id set + network ids verified). Medium only on whether the coordinator intends the boundary tests to run on empty-volume boots — flagged for decision.

## For the coordinator (before tag `red-loop-m4`)

- Regenerate `m4-direct-e2e-evidence-manifest.json`(+`.sha256`) at the final evidence commit → clears **G68**.
- Commit `m4-finalize-boots.json` + `m4-audit-report.md` + this file + the 3 untracked finalize artifacts; clean tree → clears **G71**.
- Skip/deselect fixture-dependent boundary tests on empty-volume boots (optional but recommended so the two-boot verifier is all-green; provisioning/egress already prove G17/G70).
- Annotate `red-loop-m4` on the evidence commit → clears **G73**. Then `accepted` flips true.

# Remaining information requests — Payouts Twin fidelity

Consolidated from all 12 fidelity lanes' "Cannot be derived from repositories" sections, deduplicated
where multiple lanes asked for the same underlying artifact. Each row is written so it can be sent to
the named team as-is. No item asks for broad "production access" — every request is a specific export,
schema, config value, or confirmation.

## P0 — blocks a payout family

| # | Artifact | Why it matters | Likely owner/team | Minimal precise request | Sufficient form | Priority | Lanes |
|---|---|---|---|---|---|---|---|
| 1 | Real Splitz experiment variant/rollout values in production | The twin's `splitz-stub` currently cannot distinguish "off" from "unmeasured," and defaults every unseeded experiment to `"on"` — silently enabling ~30+ unramped behaviors across routing, approval, pricing-source selection, Kafka-vs-webhook status delivery, and reservation gating. Without real values, no payout family gated by Splitz (which is nearly all of them) can be seeded correctly, even after the arena config fix (`client_side_eval=false`) is applied. Covers: payouts' 38-field `SplitzExperimentList`, FTS's `FireStatusUpdateKafka`/`CreateTransferMetaRollout`/`NewMaintainNeftFlow`/NEFT-24x7 experiments (~5 ids), and 3 monolith-side named experiments (`PAYOUTS_SERVICE_CRON_TRIGGER_FOR_QUEUED_PAYOUTS`, `NON_TERMINAL_MIGRATION_HANDLING`, the direct-proxy cutover experiment) | Splitz/experimentation platform team (MySQL is the only source of truth), with Payouts and FTS platform teams as co-requesters | A sanitized export of `{experiment_id/name: current_variant, rollout_percentage}` for the ~45 named ids above, or explicit confirmation that "off" is the safe universal default for every unlisted one | Values-only export (schema-only is NOT sufficient — the actual rollout values are the point) | P0 | 01, 04, 06, 09, 10 |
| 2 | Caller + field mapping for ledger `AccountAPI/CreateOnEvent` on merchant-side onboarding events, and the signed-vs-unsigned `banking_account_id` convention | The twin's ledger seed for the Direct/M2 FTS pair is confirmed wrong (`fund_account_type=nodal` should be `current`, missing Current parent accounts), blocking `payout_processed`/`payout_reversed` journal entries for any Direct-routed payout. Fixing the monolith-stub caller correctly, and resolving a signed/unsigned-id inconsistency observed in a passing golden run, needs this confirmation | API-monolith/BAS team (caller identification); ledger + payouts teams (id-format question) | (a) Which monolith module calls ledger's `AccountAPI/CreateOnEvent` for merchant-side onboarding, and the exact request-body field mapping — a sanitized code excerpt or one-paragraph confirmation is enough, no data export needed; (b) one real production `account_details.entities` row export (schema-only field values, no merchant PII) for a MerchantBalance/VendorPayable account, to see the literal stored `banking_account_id` string format | (a) code excerpt/confirmation; (b) schema-only, values redacted | P0 | 07 |
| 3 | `workflow_configs` DB rows (recon/ART) | These are the actual production endpoint templates, payload schemas, and file-type/auto-fire gates ART uses to call FTS's `/v1/attempts/:action`/`reconcile`. Without them, no protocol-faithful ART substitute can be built for the recon/repair payout family (currently entirely MISSING from the twin) | `recon-dev` team (per `recon/servicetree.yaml`) | Schema-only export of the `workflow_configs` table: column list plus a handful of anonymized example rows with any merchant/account data stripped | Schema-only, anonymized example rows | P0 | 11 |

## P1 — fidelity

| # | Artifact | Why it matters | Likely owner/team | Minimal precise request | Sufficient form | Priority | Lanes |
|---|---|---|---|---|---|---|---|
| 4 | FastCron per-endpoint cadence for payouts' `/v1/cron/*` routes (~24) plus the monolith's `payouts/auto_expire` | The twin's `cron-driver` cadence (300s/900s fixed intervals) is entirely assumed; wrong cadence changes queue-dwell-time-sensitive test outcomes (SLA breach, reservation-reconcile staleness, stuck-payout dispatch, FMP check, batch-submitted dispatch) | Platform/FastCron SaaS owner, with Payouts team (`#xp-payouts-service`) | Export of the FastCron job definitions (endpoint, cron expression, retry policy) for the payouts and monolith cron job groups, or a schedule dump/screenshot from the FastCron dashboard | Cadence values only | P1 | 04, 11, 12 |
| 5 | Real DCS values for the ~28 payout-relevant fields, per representative merchant segment | The twin's 3 seeded merchant profiles (M1/M2/M3) are illustrative, not measured against real merchant-config distributions | DCS admin / Payouts team | Sanitized DCS `Get` dump for 3-5 representative real merchant ids across the 28 fields | Sanitized export | P1 | 04 |
| 6 | Real `ledger_config` rows for tenant X in production (`SELECT * FROM ledger_config WHERE tenant='X'`, not the Go seed) | The twin's hand-transcribed `ledger_config` rows for `payout_initiated`/`payout_processed`/etc were abandoned in-seed (produced zero ledger entries) in favor of an unverified bulk-load path; a real dump would let a verifier diff intended vs. live config drift | Ledger team | Schema-only/values-only (no merchant ids) export of `ledger_config` rows for the 4-6 payout-relevant transactor events | Schema-only, values-only | P1 | 04, 07 |
| 7 | Writer of the beneficiary-bank-down/up Redis map (IFSC-prefix keyed) | Only reader-only code was found in the payouts repo; the on-hold trigger cannot be reproduced faithfully without knowing the writer's key format/TTL/update cadence | Likely a bank-uptime-detector service; owner unknown | Identify the writer service, then get its key format and write frequency | Sanitized contract description | P1 | 04 |
| 8 | Monolith `fetch_pricing_info`/`internal/merchants/{id}` full response JSON shape (beyond field names already confirmed) | Needed to build a fully contract-faithful monolith substitute, since the twin's current stub is a hand-maintained partial reconstruction | API monolith team | Sanitized sample response for 2-3 representative merchants/purposes | Sanitized sample | P1 | 04, 10 |
| 9 | `authz` service config for the `x_platform` resource group, and the deployed value of `AUTHZ_XPLATFORM_ENFORCER_MOCK` | Resolves whether the dashboard-merchant surface's real (non-mock) AuthZ enforcement path is actually live in any prod deployment | AuthZ platform team | Sanitized `authz` service config export (env-var values only, not source) for the `x_platform` resource group, plus the monolith's authz-enforcer feature-flag deployment value | Sanitized config export | P1 | 02, 03 |
| 10 | `PAYOUTS_FEATURES_FORWARD_DUAL_WRITE`/`REVERSE_DUAL_WRITE` runtime values | Confirmed as `env|`-templated placeholders in `payouts/config/*.toml`; actual deployed value is injected by a Vault/Confd/Helm layer not present in any cloned repo | Payouts platform/SRE | The env-var boolean pair as deployed in prod | Sanitized config, single boolean pair | P1 | 02 |
| 11 | `merchant_users`/user↔merchant↔role table schema | Role is carried to the monolith via an `X-Dashboard-User-Role` header from the dashboard's own session; the authoritative table backing "which user has which role for which merchant" lives in the dashboard repo, not read this pass | Dashboard/growth-banking team | Schema-only DDL for the user↔merchant↔role table(s) | Schema-only | P1 | 02 |
| 12 | Stack trace / incident sample for `ApprovedPayoutProcessor`'s write path (`Table 'api.payouts' doesn't exist` incident) | Confirms the exact failing write call implied by a reported prod incident; this pass only confirmed the read side is safely PS-first | API-monolith/Payouts platform team | A stack trace or `ExternalServiceCallResponse`/exception-log sample from the actual incident, or a sanitized read of `Payout\Core::handlePayoutProcessed`/`handlePayoutFailed`'s save path | Sanitized log sample or code read | P1 | 02 |
| 13 | Real (non-mock) production toggle values for `applications.authzXPlatformEnforcer.mock` and `applications.payouts_shadow_router.kill_switch`/experiment id | Both are confirmed live, env-toggled mechanisms in code; their actual prod settings aren't retrievable from static config | API-monolith platform team | Sanitized deployment env-var values for these two flags | Sanitized config values | P1 | 02 |
| 14 | Callers of `payouts/2fa/create[_internal]`, `payouts/2fa/send_otp`, `payouts_internal_direct` | No caller was found in any of the 13 repos read across this investigation; needed to know whether/how this OTP flow is exercised in production at all | Unclear — possibly an ICICI-specific frontend, or `scrooge`/`settlements-service`/`xpayroll` (none in the current clone set) | A repo-name confirmation from the owning team, or an APM trace of inbound callers by route name | Confirmation or trace | P1 | 02 |
| 15 | Batch's outbound auth identity/host config for `payouts/bulk_approve`, and whether the monolith's Basic-Auth resolution treats Batch's `rzp_<mode>_<entity_id>` credential as "Proxy" auth | If Batch's calls do **not** resolve to Proxy auth, the entire WFS-backed branch of Batch's bulk-approve would 400 for every workflow-backed payout in prod today — a significant finding if confirmed true, not just a fidelity nuance | Batch service team; API-monolith/edge team | The `application-*.yml` (or equivalent) resolving `authType`/base-host for the `bulk_approve` `apiProcessor` call type; and either a read of `BasicAuth::isProxyAuth()`'s resolution logic for the `rzp_<mode>_<id>` username pattern, or a sanitized prod trace of one real Batch-driven `payouts/bulk_approve` call against a workflow-backed payout | Config file read or sanitized trace | P1 | 03, 08 |
| 16 | Whether Batch's `payouts/bulk_approve` is currently functional in prod at all (the monolith's own `payouts` table is reportedly dropped, and the handler fetches via Eloquent) | Determines whether this entire flow needs modeling in the twin at all, or is itself dead in production | Payments-Payouts/monolith on-call | Confirmation (or the actual runtime error/outcome) of a real `payouts/bulk_approve` call in the current prod environment | Confirmation | P1 | 08 |
| 17 | Production WFS `Config` template values per real merchant (range/steps/roles JSON) | The twin's DCS `Workflows` fixture for M3 is internally consistent but not measured against a real merchant's configured approval policy | Payouts/WFS platform team | Sanitized export of one or two real `configs.template` rows (`config_type=payout-approval`), merchant-identifying fields redacted | Sanitized export | P1 | 08 |
| 18 | Cadence server version/image actually run in prod | WFS's `go.mod` only pins the client SDK (`go.uber.org/cadence v0.13.4`); the server image/tag isn't in any repo | WFS/infra team | The exact `uber/cadence` server image tag/version used in prod's Cadence cluster, or confirmation that any recent compatible tag is fine | Version string | P1 | 08 |
| 19 | `enable_approval_via_oauth`'s real adoption (which partner merchants have it enabled; whether `rx_partner_read_write` OAuth scope is used in practice) | Determines whether the OTP-bypass-via-OAuth path needs any twin modeling at all | Partnerships/OAuth platform team | Schema-only confirmation of feature-flag adoption counts (no PII) | Schema-only counts | P1 | 08 |
| 20 | Dashboard-side UI/UX around self-approval (is "Approve" hidden for the payout's own creator?) | This repo trio has no frontend code; self-approval prevention, if it exists at all, is client-side or in a repo outside this investigation's scope | RazorpayX Dashboard/Frontend-X team | Confirmation of whether client-side self-approval prevention exists, and where | Confirmation | P1 | 08 |
| 21 | `api` monolith's Kafka consumer-group name for `api-kafka-fts-status-update-consumer` | Needed to confirm it does NOT share a consumer group with `rx-payouts-fts-status-update-consumer-group` — if it did, the two services would split partition assignment and silently miss messages (a correctness bug, not by design) | API/FTS platform team | The `api` repo's kafka-consumer config file (same shape as `payouts/config/prod.toml`'s `[kafka_consumers]` block) | Sanitized config export | P1 | 09 |
| 22 | Trino table schemas for `hive.stork.<webhook_disable_table>` and `hive.stork.webhook_event_low_volume` | Without the exact columns, a substitute cannot faithfully seed "this webhook should get auto-disabled" test fixtures | Stork/data platform team (Airflow DAG owner) | Schema-only DDL or a `SELECT * LIMIT 0` column list for both tables, plus the Airflow DAG's population query (what "failing" means quantitatively) | Schema-only + query text | P1 | 09 |
| 23 | `WebhookCh.GetHTTPTimeout()`/`GetTickerHTTPTimeout()`/`GetRemindersHTTPTimeout()` numeric defaults | Relevant to a stork substitute's delivery-timeout fidelity; not located in the TOML files read | Stork team | The relevant `[webhookCh]`/`[ticker]`/`[reminders]` TOML block from `stork/configs/prod.toml` or `default.toml` | Sanitized config export, no secrets | P1 | 09 |
| 24 | Real per-partition assignment behaviour for `rx-fts-status-update-events` under the 2-partition/3-replica prod topology, with both `api` and `payouts` consumer groups active | Live cluster fact (rebalance behaviour, lag per partition), not derivable from static config | Kafka/platform SRE (`#gandalf`) | Grafana/Prometheus lag dashboard link or a snapshot of consumer-group lag by partition for both groups | Dashboard link/screenshot | P1 | 09 |
| 25 | Shield's real rule catalogue (rule IDs/conditions for the payout entity type) | Needed for a more representative `shield-stub`; the `shield` repo itself wasn't in scope for cloning | Shield/risk platform team | Sanitized rule-definition export (rule name/condition/action, no merchant lists) | Sanitized export | P1 | 10 |
| 26 | Charge-Collections-SDK's exact wire shape to Governor's `/execute` | The vendored `charge-collections-sdk` module is unreadable in this sandbox; needed to fully spec the (currently unreachable) Governor pricing path | Payments-Platform/Governor team | A copy of the vendored `charge-collections-sdk` module source (or its own repo, if separate from `governor`) | Source read | P1 | 10 |
| 27 | Real XAS ingestion contract (payload shape for the `XasEventDetails` queue, queue names, SQS vs Kafka) | Would let `xas-sink` actually drain and validate events, closing the "nothing reads it back" gap | x-account-statements team | Schema-only description of the inbound event contract (queue names + payload shape) | Schema-only description | P1 | 10 |
| 28 | KYC-status field / full `accountv1.Account` proto message | The task named a KYC-status field PS consumes from ASV, but no such field exists in the 25-path `AccountServiceFetchParams` field mask read this pass; either it doesn't exist on this path or is fetched elsewhere | Payouts team / ASV proto owners | The full `accountv1.Account` proto message definition (not present in this clone set) | Proto file | P1 | 10 |
| 29 | banking-accounts' real onboarding/activation route table and DB schema (channel/account_type full enum, `balance_id`/`fts_fund_account_id` write path) | Would let the `bankingaccounts-stub` grow safely beyond its current 1-route scope if onboarding flows are ever tested | banking-accounts team (per CODEOWNERS) | Schema-only DDL dump of `banking_account`/`banking_account_application` plus the route list | Schema-only + route list | P1 | 07 |
| 30 | Live `SHOW CREATE TABLE api.banking_accounts` (the API monolith's own copy) | The migration read this pass only went to line 60; the real table likely has more columns (gateway states, TPV flags) given separate `banking_account_state`/`banking_account_details`/`banking_account_tpvs` tables exist | API-DB/monolith owners | Schema-only export | Schema-only | P1 | 05 |
| 31 | `SHOW CREATE TABLE payouts.payout_details` (payouts-service's own production DB) | Confirms `beneficiary_bank_code`'s actual type/length/nullability/default, and whether any other out-of-band column exists beyond the one found | Payouts platform team (DB owner for payouts-service MySQL) | Schema-only `SHOW CREATE TABLE` export, no rows | Schema-only | P1 | 05 |
| 32 | `x-balances/queries.sql`'s actual applied state in production (as-written vs. already patched with `name`/`deleted_at`) | Determines the true `sub_balances`/`sub_balance_limits` shape — the file-as-written and current code disagree | x-balances service owners | `SHOW CREATE TABLE sub_balances, sub_balance_limits` (schema-only) from any environment where PR #147's feature has shipped | Schema-only | P1 | 05 |
| 33 | `db.hash_lookup.getIndexes()`, `db.tokenized_iin.getIndexes()`, `db.iins.getIndexes()` (cfa, live DocumentDB) | No index-creation code exists in the repo for these 3 collections despite hash-based lookup usage; unknown whether indexes were added out-of-band | cfa/DocumentDB ops owner | Schema-only index list export (`getIndexes()` output, no documents) | Schema-only | P1 | 05 |
| 34 | cfa DocumentDB engine version | Determines which MongoDB-compatible behaviors (aggregation operators, `$jsonSchema` support, TTL indexes) the twin can faithfully emulate with vanilla MongoDB | cfa/infra/Terraform owner | Terraform var or AWS console value (`engine_version`) | Single value | P1 | 05 |
| 35 | `payouts`' production migration-application log / gh-ost changelog for `beneficiary_bank_code` | Would confirm how the out-of-band column was applied and rule out other out-of-band columns on other payouts tables | Payouts platform/DBRE | DBRE runbook or gh-ost changelog table export (schema-only) — may not exist, in which case this stays permanently unresolved | Schema-only, if it exists at all | P1 | 05 |
| 36 | Which app-name XAS's `APISERVICE_AUTH_USERNAME`/`PASSWORD` secret resolves to inside the monolith's `internalBasicAuthAppConfigs` | Determines whether the monolith can distinguish XAS-initiated BAS-recon calls from genuine PS calls on the shared `banking_account_statement/payout_update` route | API-monolith infra/config team | Sanitized (secret-redacted) dump of `internalBasicAuthAppConfigs`/`applications_v2` config entries whose `name` resolves to `payouts_service`, confirming whether XAS's client-id is among them | Sanitized config dump | P1 | 11 |
| 37 | Confirmation that no other emitter produces `payouts_service_stuck_payouts_count{status="non_terminal",...}` | Determines whether the two "Stuck Payouts" IDFC/IMPS+xpayroll alert rules are truly dead in production, or just not sourced from the cloned `payouts` repo | payouts-service team | A live Prometheus query or Grafana panel screenshot from Vajra confirming whether the `non_terminal` label value has any data points in the last 30 days | Screenshot/query result | P1 | 11 |
| 38 | Whether PR 2577's policy-seeding pipeline used `non_lms` as `POLICY_SUBPRODUCT` | Carried over unresolved from an earlier report; a runbook fact, not a code artifact | Spine-Edge/authz team | The actual `POLICY_ORG_ID`/`POLICY_SUBPRODUCT` values used for that pipeline run | Config/runbook values | P1 | 03 |
| 39 | How production API-key (non-dashboard) auth's `roles` claim gets populated, if at all | Not wired to any confirmed role source in `edge`; matters for anyone testing role-gated behavior on the pure-API-key path | Edge team | Point to the actual Lua/config that sets `passport_roles`/`authorization_roles` for the API-key auth flow, or confirmation it doesn't exist (API-key traffic has no roles) | Confirmation | P1 | 03 |
| 40 | Which struct (`ConsumerTask` vs `Task`) is authoritative for `[consumer_task] name="fts"` vs `[kafka_consumers]`/`[topics]` at runtime | Ambiguous dual config axis found in `config/devstack.toml`; could affect topic/group resolution in a reseeded environment | Payouts team (config owner) | A one-line confirmation of which struct is read in the deployed prod config | Confirmation | P1 | 04 |
| 41 | Prod's effective payouts `[passport.edge]` identifier (`kid`) value | Only `default.toml` (edgev1) and `devstack.toml`/`sample.toml` (edgev2) exist in the clone; no `payouts/config/prod.toml` was found. A wrong `kid` mapping would make PS reject every real Edge-minted passport in prod, so this is worth confirming even though the twin's own internal consistency doesn't depend on it | Payouts team (`@razorpay/razorpayx_payouts_be`) | Sanitized `prod.toml` `[passport]` section only (no other secrets) | Sanitized config section | P1 | 03 |

## P2 — nice-to-have

| # | Artifact | Why it matters | Likely owner/team | Minimal precise request | Sufficient form | Priority | Lanes |
|---|---|---|---|---|---|---|---|
| 42 | Real Kafka topic/consumer-group names for ledger's PG-CDC and x-balances' legacy `makeshift` queues, and the scheduler's exact cron cadences | Env-injected, not in-repo; not payout-relevant (PG/legacy paths only) | Ledger team | Config values (topic/group names, cron expressions) | Sanitized config | P2 | 07 |
| 43 | Spinnaker Front50 pipeline-template body (`spinnaker://664e59d6-943b-44e7-a3ab-fa5db69705f6:latest` and siblings) | Where the actual bake/deploy/parameter-substitution logic lives; mostly affects image-tag/parameter resolution rather than application-level config semantics | Platform/Release-Engineering (owns Spinnaker) | A `spin pipeline-template get 664e59d6-…` export (or equivalent Front50 API read), sanitized of any embedded account credentials | Sanitized template export | P2 | 12 |
| 44 | AWS SQS queue resource definitions (visibility timeout, redrive/DLQ policy) for payouts' 24 queues, and cfa/x-balances/xas queues | Payouts' own config never sets `visibilityTimeout` (unlike ledger); the real value is likely set on the AWS resource via Terraform, not in the readable repo set | Infra/Datastores | A `terraform show`/`aws sqs get-queue-attributes` sanitized export (names + timeout/redrive values only) | Sanitized export | P2 | 12 |
| 45 | Terraform/infra repo values for RDS/Aurora, MSK, ElastiCache engine versions | No application config states a DB/Kafka/Redis engine version; the twin's `mysql:8.0`/`postgres:15-alpine`/`apache/kafka:3.8.0`/`redis:7-alpine` choices are unverified against real prod versions | Infra/Datastores (`#gandalf`) | Engine-version-only export (no credentials) for the payouts-adjacent clusters | Version strings only | P2 | 12 |
| 46 | `stork`'s `partitioner_cron_schedule` value | Templated in `templates/stork/templates/cronjobs.yaml:189` but not located in the section of `prod/stork/values.yaml` inspected this pass | Stork/Platform team | Confirm whether `partitioner_cron_schedule` is set in `prod/stork/values.yaml` (a full-file grep would likely resolve this without an external ask) or is intentionally absent | Confirmation | P2 | 12 |

## Resolvable from repositories with more time

The lanes themselves flagged these as budget/time-box gaps, not genuine access gaps — closeable with a
further repo read, no external request needed:

- `Initiator::sendFTSFundTransferRequest`'s async queue + consumer retry/backoff cadence (grep `sendFTSFundTransferRequest|class Initiator` in `api/app/Models/Payout/`). — Lane 01
- `CounterHelper::fetchCounterAndDecreaseFreePayoutsConsumed`'s exact response shape (`api/app/Models/Payout/**/CounterHelper.php`). — Lane 01
- Whether `payouts_service/*` outbound PS→monolith calls carry an actor/passport header (`PayoutService\Base::makeRequestAndGetContent` + ~30 client classes). — Lane 01
- `contacts_internal`'s full allowlisted-caller set (grep across all `$internalApps` blocks, not just `payouts_service`'s). — Lane 01
- PS's own internal validation/idempotency/Shield-integration parity with the monolith's private-auth allowlist (`payouts/internal/app/payouts/processor/base.go`'s create-time validation list). — Lane 02
- kube-manifests' 24-vs-25 worker Deployment discrepancy (`git blame`/history on `internal/job/` and the kube-manifests templates for the 5 unmatched names). — Lane 04
- Exact `batch` repo literal for its "bulk approve" HTTP call construction (dead `bulk_approve` route vs. looping `/approve`). — Lane 04
- GORM association-save mechanics for `p.PayoutLog` slice → `payout_logs` DB rows (grep `payoutLogsCore`/`PayoutLog` write sites around `repo.Update`/`repo.Create`). — Lane 04
- Full column DDL for the monolith's `payouts` table beyond the first ~14 columns transcribed (read the rest of `2016_06_16_081431_create_payouts_table.php`). — Lane 05
- Full `create_banking_account_statement_table` migration and related `ps_banking_account_statement*`/`banking_account_statement_pool_{rbl,icici}` DDL (out of this lane's core 9-table scope, not unavailable). — Lane 05
- Full contract of `PerformAttemptAction` and `PUT /v1/attempts/reconcile` (`attempt_bulk_action.go:142-260` and the `PerformAttemptAction` switch statement). — Lane 06
- Real NEFT-24×7 eligibility internals (`source_account_selector.go:1284-1829`, large file not opened this pass). — Lane 06
- `send_notification` task handler body (`internal/tasks/send_notification.go`, exists per task registry, not opened this pass). — Lane 06
- Production `env.<prod>.toml` overlay for non-RBL banks' amount caps/time windows (wider grep across all 9+ `config/env.*.toml` files). — Lane 06
- Whether `/v1/transfer`'s `X-Origin` header truly defaults to `"API"` when absent (`service.go` lines ~1906-1930). — Lane 06
- Literal `ledger-api` error log for the `test_v06` failure — resolvable from the arena itself (`docker compose logs ledger-api | grep -A5 ACCOUNT_DISCOVERY` during a rerun), not an external ask. — Lane 07
- Whether `ledger`/`cfa` call Splitz directly in this arena, and with what `ClientSideEval` setting (a fuller grep than the one done this pass). — Lane 10
- Exact queue name for `x_account_statement_source_event` (config key confirmed at `internal/config/config.go:282`; value likely under an unsearched queue-name-mapping block). — Lane 10
- `stork`'s `partitioner_cron_schedule` (a full-file grep of `prod/stork/values.yaml` was not completed this pass — see P2 item 46, which should be tried this way first). — Lane 12
- `payouts_sla_breach_monitor`'s actual trigger mechanism — a grep for an in-process ticker calling `SLAMonitorService.CheckSLABreaches` directly, alongside the FastCron dashboard check in P1 item 4. — Lane 12
- Later ALTER migrations to `fund_transfer_attempt`/`payouts_details` for out-of-band columns (a wider grep pattern, `grep -rn "fund_transfer_attempt" database/migrations/*.php`, before falling back to the schema-only export in P1 item 31/6). — Lane 01

# M4 decision log

| ID | Date | Decision | Rationale / evidence |
|---|---|---|---|
| D-001 | 2026-09-06 | Branch `milestone-4-direct-reconciliation` created from `red-loop-m3.1` (3a044f8). No push; no rewrite of M3.1 tag or evidence. | `git rev-parse red-loop-m3.1^{}` = 3a044f8; tree clean at branch time. |
| D-002 | 2026-09-06 | Execution contexts (G63, Phase 7) are per-merchant namespaces inside ONE arena (separate merchants, webhook-sink namespaces, artifact dirs, correlation IDs, Kafka key/SQS message namespaces), not concurrent arenas. | Docker Desktop memory 11.65 GiB; one 66-container arena ≈ host capacity (M3.1 evidence). Namespace isolation will be *proven* per store, not assumed; contamination checks will be explicit. |
| D-003 | 2026-09-06 | Clean-boot reverification is sequential (stop live arena → boot disposable instance → teardown → restart live), reusing `ENV2_COMPOSE/scripts/clean-boot.sh`. | Same RAM constraint. |
| D-004 | 2026-09-06 | The autonomous runtime keeps calling models through the LiteLLM gateway configured in git-ignored `RED_LOOP/llm.env` (host-side only; never from arena containers). This is the accepted M3.1 model path (kimi-k3 primary, gpt-5.5 reproducer). Gateway reachable at session start (HTTP 200 on /v1/models, 27 models). | Carried-forward assumption from M3.1 (D-004 is a boundary note, not new access). Containers remain on the `internal: true` network; egress audits stay zero-outside-packet. Claude Code subagents cannot be the campaign model for auditability reasons recorded in M3.1. |
| D-005 | 2026-09-06 | Host Python 3.14 has no pytest/boto3/hypothesis; all runtime tests continue to run inside the `verifier` container (its requirements.txt); host-side harness code stays stdlib-only (M3.1 convention). | `python3 -c "import pytest"` fails on host. |
| D-006 | 2026-09-07 | **Direct reconciliation architecture for M4** (from T02/T03/T04/T05): (1) Statement ingestion = the REAL payouts worker `rbl_banking_account_statement` (same accepted binary; add `[job]` key, SQS queue, worker container, cron-driver trigger) fed by a mozart-sim `razorpayx/rbl/v2/account_statement` substitute endpoint (base64 CSV per T03 Finding 8) and a bankingaccounts-stub credentials route; BASD rows are inserted by the provisioner (mirrors the monolith's insert into the PS DB, Details/Core.php:219-237). (2) Matching = two paths: (a) REAL PS ART route `POST /v1/banking_account_statement/process/batch` (recon substitute issues it) and (b) an XAS substitute `xas-sim` implementing the source-backed XAS enrichment (utr→grn→cms + balance/account + amount, external marking, ≤2/dual-type guards, `(event_id,entity_type)` dedup) consuming the REAL PS `x_account_statement_source_event` queue and POSTing the 9-field `payout_update` to monolith-stub which relays to the REAL PS `UpdatePayoutAfterBASRecon`; xas-sim also serves `fetch_multiple_by_reference_numbers` so the REAL `VerifyPayoutFailedTransaction` and FTS retry-preprocessor gates execute. (3) Direct Ledger accounting = REAL Ledger service (`direct_account_x` config, `AccountAPI/CreateInBulk` + `CreateOnEvent{direct_merchant_onboarding}` real Twirp calls, real `journal_create` worker) driven by a monolith-stub substitute emitter reproducing `processLedgerPayoutForDirect` (Core.php:5175-5228; `da_payout_processed` + `da_payout_processed_recon`) gated by feature `da_ledger_journal_writes` and skipped when `payout_service_txn_recon` is on. PS itself posts nothing for Direct (commented out, core.go:7284/7356) and this is preserved and asserted. | Fidelity labels: ingestion REAL-worker/substitute-bank; ART linking REAL; XAS matching SUBSTITUTE (contract-faithful); repair route REAL PS; DA journals REAL Ledger, SUBSTITUTE emitter; production reachability of DA journals for PS-recon merchants UNKNOWN (T03 Finding 7). Real XAS build is a stretch (Kafka CDC not reproducible; dual_write hook exists). |
| D-007 | 2026-09-07 | File ownership during parallel implementation on the live arena: T09 owns provisioner*.py + surface/m4_direct_provision.py; T10 owns payouts/fts config templates, init-queues.sh, docker-compose.yml (new PS worker only), cron-driver, mozart-sim, bankingaccounts-stub, bas_fixtures.py; T11 owns substitutes/xas-sim/*, monolith-stub/server.py, scripts/up.sh (ledger DA bulk create), ledger_da.py; compose wiring of xas-sim done by the coordinator after T10 lands. | Avoid uncontrolled concurrent edits of the same files; worktrees are unusable for arena work because generated secrets/config are git-ignored and live in the main tree. |
| D-008 | 2026-09-07 | Candidates H-D3 (`GET free_payout/{balance_id}` returns 200 cross-tenant, no merchant scope; scoped payout fetch 404s) and H-D4 (create quoting another merchant's fund_account_id succeeds but binds to caller + caller's balance) are recorded as autonomous-replay candidates, NOT findings. T14 asserted them as xfail/observation only. The autonomous run (T15/soak) must exercise H-D3 with a divergent Merchant-B config to determine whether real value leaks (unauthorized effect) vs a substitute-fidelity artefact. | M4 mandate: a suspicious response is not a verified issue without an observable unauthorized effect, negative control, and clean-state reproduction. |
| D-009 | 2026-09-07 | F-T10-1 (statement-first `VerifyPayoutFailedTransaction` guard is on the FTS-direct `/transfer_status_webhook` route but the monolith-relay route still moved a link-before-terminal payout to `failed`) is recorded as a source-behavior OBSERVATION, routed to the autonomous run as a candidate, not asserted as a defect. The guard's route-scoping matches T02/T05 (the guard runs only on the webhook path). | M4 mandate: preserve source behavior honestly; a route asymmetry is not a verified issue without an unauthorized effect. |
| D-010 | 2026-09-07 | The PS external-path `da_ext_debit` journal (Unreconciled ART) publishes to SNS which is blackholed for payouts-api (no `AWS_ENDPOINT_URL_SNS`). We do NOT add that env for the mandatory path: the external/unmatched case is an edge negative, and the matched Direct success accounting is proven via the monolith-stub emitter (T11). The gap is declared (DEV-171); adding the SNS localstack endpoint to payouts-api is a stretch item. | Keeps the mandatory acceptance to the real matched path; avoids a config change to the core service container beyond what parity requires. |
| D-011 | 2026-09-07 | H-D3 promoted from candidate to VERIFIED finding F-M4-001 (cross-tenant free-payout IDOR in accepted payouts source `GetFreePayoutAttributes`). Meets the verified-issue bar (unauthorized effect + deterministic evidence + negative control + independent fresh-ID reproduction). Twin source left UNPATCHED (mandate: do not patch source to go green); recorded in reports/implementation/m4-findings.md with production reachability UNKNOWN. H-D4 benign, F-T10-1 source-faithful (both recorded as honest non-findings). | This is the assurance harness working as intended: a central hypothesis was exercised, verified, and independently replayed rather than left proposed. NOT a production claim. | 

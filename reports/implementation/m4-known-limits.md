# M4 known limits

| ID | Limit | Class | Evidence |
|---|---|---|---|
| L-001 | Only one arena at a time on this host (11.65 GiB Docker memory). | environment | `docker info` MemTotal=12513980416 |
| L-002 | `xas-sink` at M3.1 is a health-only placeholder; no BAS/XAS ingestion exists in the twin yet. | substitute gap (to be closed in M4) | ENV2_COMPOSE/substitutes/xas-sink/CONTRACT.md |
| L-003 | Mozart RBL v2 `account_statement` is a mozart-sim substitute (base64 CSV, Success / "No Records Found", next_key paging); Mozart itself is unbuildable (private modules). Direct vs Pool remain indistinguishable at the bank boundary (credentials ignored). | substitute | T03 Finding 8/9; T05 Finding 8; `TWIN_SPEC/route-matrix.yaml` R3 |
| L-004 | The Direct-account (DA) ledger journal emitter is a monolith-stub substitute reproducing `processLedgerPayoutForDirect` (`api app/Models/BankingAccountStatement/Core.php:5175-5278`); PS posts no Direct journal at this HEAD (`payouts core.go:7284,7356` commented). Whether any live merchant still receives DA journals (`da_ledger_journal_writes`, `payout_service_txn_recon`) is unknown. | substitute + production_unknown | C-004/C-010; T03 Finding 6, Unresolved Q1; EF-004 |
| L-005 | XAS (x-account-statements) is substituted by `xas-sim`; the real service's Kafka CDC dual-write (`mysql_cdc_events_prod_rx_account_statements_*`, Maxwell) is not reproducible, so outbound `payout_update` is issued directly (equivalent of `POST /v1/statement/dual_write`). The monolith's own statement matcher and RBL fetch job are not modelled at all. | substitute | D-006 (2); T03 Finding 4/5/9; C-011/C-013 |
| L-006 | `banking_account_statement_details` (BASD) rows are inserted by the provisioner; in production the monolith writes them into the PS DB at CA onboarding (`api app/Models/BankingAccountStatement/Details/Core.php:219-237`). PS never creates them. | substitute | T03 Finding 2; D-006 (1) |
| L-007 | Which RBL statement fetcher is live in production (PS worker `rbl_banking_account_statement`, monolith `RblBankingAccountStatement`, or XAS multi-bank fetch) and the FastCron cadence of `/v1/cron/banking_account_statement/fetch/initiate` are unknown; kube shows all three fleets deployed. The twin drives the PS worker via cron-driver. | production_unknown | T03 Finding 1, Unresolved Q2; `KUBE:cde/payouts/values.yaml:112,175-176`, `cde/api/values.yaml:340-342,427` |
| L-008 | Production values of DCS `in_flight_reservation_enabled`, monolith feature `enable_payouts_buffer`, feature `da_ledger_journal_writes`, Splitz `payout_service_txn_recon`, `fetch_balance_entity_from_x_balance`, `account_statement_source_event`, `xas_source_event_utr_and_grn_match_experiment`, `fts_request_from_payouts_service`, `create_transfer_meta_rollout`, `fire_status_update_kafka` are not derivable from repositories; the twin fixes them per merchant (see `m4-source-map.md` §7, gate G11). | production_unknown | T02 "Unknown rollout config"; T03 §8; T05 Unresolved |
| L-009 | x-balances `BalanceRefreshEvent` (reservation release trigger A) cannot be produced by the twin: x-balances `[Queue] Driver="inmemory"`, env prefix `arena-` vs payouts `stage-`, no `/v1/cron/balance/{channel}/fetch` caller, seed `status='activated'`, Mozart/banking-accounts `Mock=true`. Tests inject the event into SQS or bump `balance.last_fetched_at` and run the reconciler. x-balances→monolith dual-write is likewise absent. | environment (substitute trigger) | T04 §5/§9; C-009; EF-007; DEV-024 |
| L-010 | FTS partner-bank health notification to PS (`[payouts_service.notify_channel_status]`) is never emitted (placeholder URL `http://localhost:8080/`); Direct on-hold is exercised only by a direct `POST /v1/notify/health/update` with `cred.FTS`. | environment | T05 Finding 7; EF-008 |
| L-011 | Only one arena fits on this host (L-001); Direct namespaces are per-merchant inside the live arena, and the provisioner's stub restarts (monolith-stub, bankingaccounts-stub, dcs-stub, splitz-stub, kong-lite) wipe in-memory state for all tenants. | environment | D-002; T07 §2 tail |
| L-012 | SLICE is excluded from the twin's Direct scope: PS lists `slice` in `FtsToPayoutStatusMap[direct]` and `DirectChannelsForConnectedBanking`, the monolith does not (`Status.php:273-307`, `Settlement/Channel.php:304-313`), so a SLICE Direct payout would map FAILED differently per transport. RBL (IMPS) only; Direct NEFT/RTGS FTS workers (`rbl::direct::{initiate_transfer,check_transfer_status}`) are absent from compose. | simplification | T02 #12; T05 Twin deviation table |
| L-013 | The monolith ingress is missing, so `X-Razorpay-Account` partner/sub-merchant scoping (`api app/Http/BasicAuth/BasicAuth.php:751,2619-2666`) and the SHARED-sub-merchant-on-Direct-master `create_fta` branch (`FundTransfer.php:2053-2072`) are unverifiable; PS never reads the header. | simplification | T06 §4; T02 §A2; monolith-stub `server.py:406-409` |
| L-014 | FTS fund/source account ids `900001`/`900002` (and per-namespace `9000x1/9000x2`) are twin-only fixtures (zero hits in payouts/FTS/monolith); fresh Direct merchants use a disjoint integer range. The twin seeds one source account per merchant whereas prod RBL onboarding creates v1 and v2, so default-rule selection by `mozart_identifier` is never exercised. | simplification | T02 §7/§A9/§B-D; T05 §1.5, Twin deviation table |
| L-015 | Twin seed casing/enum drift left in place pending runtime observation: FTS `source_accounts.account_type='DIRECT'` (code `direct`, C-005), x-balances `account_type='pool'` for Shared (code `shared`, C-007) and `status='activated'` (code `active`, C-008). Fresh Direct merchants use the code values; the M2 fixture is untouched. | environment (open) | T05 §1.3; T04 C1/C7; C-005/C-007/C-008 |
| L-016 | The PS `transaction_id`/reversal `transaction_id` for Direct is set only by BAS recon; the `da_ext_*` and DA journals require ledger DA accounts created by two Twirp calls in `up.sh` — without them every DA journal is dropped as `ErrAccountDiscoveryFailure` with no retry (the M2 fixture has none). | substitute (seeding) | T03 Finding 6/7; T07 §1 ledger rows |
| L-017 | Pricing for Direct is the flat twin plan (no `account_type`/`channel` hard-filter, no slabs); the monolith's Direct/RBL default bands (500/900/1500 paise) and per-channel free-payout defaults are not modelled. | simplification | T02 §4/§A4; `TWIN_SPEC/synthetic-data-model.yaml` pricing.rule |

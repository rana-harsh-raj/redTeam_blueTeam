# M4 decision log

| ID | Date | Decision | Rationale / evidence |
|---|---|---|---|
| D-001 | 2026-09-06 | Branch `milestone-4-direct-reconciliation` created from `red-loop-m3.1` (3a044f8). No push; no rewrite of M3.1 tag or evidence. | `git rev-parse red-loop-m3.1^{}` = 3a044f8; tree clean at branch time. |
| D-002 | 2026-09-06 | Execution contexts (G63, Phase 7) are per-merchant namespaces inside ONE arena (separate merchants, webhook-sink namespaces, artifact dirs, correlation IDs, Kafka key/SQS message namespaces), not concurrent arenas. | Docker Desktop memory 11.65 GiB; one 66-container arena ≈ host capacity (M3.1 evidence). Namespace isolation will be *proven* per store, not assumed; contamination checks will be explicit. |
| D-003 | 2026-09-06 | Clean-boot reverification is sequential (stop live arena → boot disposable instance → teardown → restart live), reusing `ENV2_COMPOSE/scripts/clean-boot.sh`. | Same RAM constraint. |
| D-004 | 2026-09-06 | The autonomous runtime keeps calling models through the LiteLLM gateway configured in git-ignored `RED_LOOP/llm.env` (host-side only; never from arena containers). This is the accepted M3.1 model path (kimi-k3 primary, gpt-5.5 reproducer). Gateway reachable at session start (HTTP 200 on /v1/models, 27 models). | Carried-forward assumption from M3.1 (D-004 is a boundary note, not new access). Containers remain on the `internal: true` network; egress audits stay zero-outside-packet. Claude Code subagents cannot be the campaign model for auditability reasons recorded in M3.1. |
| D-005 | 2026-09-06 | Host Python 3.14 has no pytest/boto3/hypothesis; all runtime tests continue to run inside the `verifier` container (its requirements.txt); host-side harness code stays stdlib-only (M3.1 convention). | `python3 -c "import pytest"` fails on host. |

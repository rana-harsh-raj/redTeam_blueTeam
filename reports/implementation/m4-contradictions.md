# M4 contradictions log

Format: C-nnn | claim A (source) | claim B (source) | status | resolution evidence

(none yet)
| C-001 | M3.1 completion report says live-arena enforcement was "restored to 0" (RED_LOOP_MILESTONE3_1_COMPLETION.md) | Accepted `hardened_gateway` gate requires enforcement=1 and the live arena runs with KONG_ENFORCE_ROUTE_POLICY=1 (T01 observation) | resolved | Live state + m3-1-acceptance.json win (source-of-truth order: artifacts > review docs). The report line describes an intermediate state before the second campaign. |
| C-002 | M3.1 acceptance evaluator `historical_integrity` hardcodes M3.1 branch names | On branch milestone-4-direct-reconciliation the same commit checks pass but the gate flips false | resolved | Evaluator-scope limitation (T01 scratch re-evaluation); the M4 gate G03 will re-evaluate commit checks branch-independently. |
| C-003 | TWIN_SPEC/synthetic-data-model.yaml:437 + components.yaml:56-57: a default `direct_account_routing_rules` row is a seed gap to fix | FTS account/service.go:5333-5380: a rule is only a tie-breaker among multiple source_accounts for the same fund_account_id; one row ⇒ never read (T02) | resolved | Source wins. G15 resolved as "not required with one source account"; seeding a merchant rule is harmless parity. |
| C-004 | PAYOUTS_FLOW_CATALOG.md:62 "no Direct ledger journal until BAS linking" | payouts core.go:7284,7356 BAS→Ledger call sites commented out, deprecation note core.go:7208-7215 (T02) | resolved | PS never posts a Direct journal at this HEAD. Direct Ledger accounting must come from another actor (T03 pending). |
| C-005 | ENV2_COMPOSE/seeds/s4/fts.sql M2 `source_accounts.account_type='DIRECT'` | FTS constants lowercase `direct`, case-sensitive compares (attempt_processor.go:4054,4341; T05) | open | Runtime worker route for M2 vs fresh lowercase merchant to be observed by T09 (`fts_worker_route_observed`). M2 seed untouched. |
| C-006 | TWIN_SPEC/route-matrix.yaml: FTS service.go:549-552 is the Direct selector; twin lacks `[payouts_service.update_fts_fund_transfer]` | service.go:549-552 is a metrics label only; real selector transfer_processor.go:964/1025 → account/service.go:5314; routes.py:60-63 injects the config for every profile (T05) | resolved | Spec to be corrected in Phase 1 spec update. |
| C-007 | seeds/s4/xbalances.sql comment: enum `direct|pool|sub_balance|master` | x-balances constants `direct|shared|sub_balance` (T02 #9); Shared provisioner writes `pool` | open | Verify live: does the runtime Shared merchant's x-balances row say `pool`? If PS copies it into banking_accounts.account_type the Shared provisioner would be selecting an unregistered processor — but M3.1 fresh Shared merchants reached `processed`, so PS must not read x-balances account_type on that path. Decisive check assigned to T09/T10. |
| C-008 | Twin x-balances seed rows use `status='activated'` (seeds/s4/xbalances.sql) | x-balances code matches `active`/`under_maintenance` only (T04 C1) | open | T09 to observe how PS's Direct create precondition resolved for M2 vs a fresh `active` row. |
| C-009 | config/declared-deviations DEV-024 describes the x-balances refresh gap as minor | x-balances arena config `[Queue] Driver="inmemory"`, Mozart/banking-accounts `Mock=true`, no fetch cron caller, queue prefix `arena-` vs `stage-` ⇒ BalanceRefreshEvent trigger (A) can never fire in the twin (T04 §6) | open | G38 will be classified: trigger B (reconciler via last_fetched_at) executable; trigger A requires a substitute event injection, to be declared. |
| C-010 | TWIN_SPEC architecture.yaml / route-matrix.yaml: PS accounts Direct via ledger direct_account_x keyed by banking_account_stmt_detail_id | PS BAS→Ledger calls commented out; only the monolith's own statement-fetch linking emits `da_payout_processed(+_recon)` gated by `da_ledger_journal_writes`, and it skips PS-recon merchants (T03 Findings 6/7) | resolved | D-006: DA journals modelled as monolith-substitute emitter into real Ledger; production reachability UNKNOWN. |
| C-011 | xas-sink CONTRACT.md: "payouts/ledger/FTS enqueue statement-relevant events that XAS would consume" | Only PS produces `x_account_statement_source_event`; FTS→XAS is read-only; ledger never talks to XAS (T03 Findings 3/5) | resolved | Contract to be rewritten for xas-sim. |
| C-012 | BOM "xas 12 queues" | 11 SQS + 2 Kafka (T03 C-T03-8) | resolved | documentation only |
| C-013 | Earlier lane-11 docs scope matching/ingestion to XAS alone | Three matchers exist: XAS (own fetches), monolith (own fetches), PS (ART instruction only; UTR auto-linker is a stub) (T03 Finding 4) | resolved | D-006 exercises PS ART route (real) and XAS-shaped substitute. |
| C-014 | PS source-event producer sends `event_created_timestamp` | XAS reads `event_create_timestamp` (T03 Finding 3) | open (production field mismatch, preserved) | xas-sim must reproduce the mismatch faithfully (read the PS field name, record the discrepancy). |

# T23 — Fidelity and Documentation Critic (Milestone 4)

Task ID: **T23** (workstream 17). Branch `milestone-4-direct-reconciliation`. Written 2026-09-07 UTC.
**Nothing committed by me** (coordinator commits). Read-only except my two owned outputs:
`reports/implementation/m4-direct-e2e-final-review.md` (the review) and this handoff. No docker, no
network, no arena; no source/config/evidence file modified.

## Review method

1. Read all coordinator files (`m4-decisions.md` D-001..D-011, `m4-contradictions.md` C-001..C-015,
   `m4-known-limits.md` L-001..L-018), the fidelity matrix (`m4-fidelity-matrix.csv`, 42 rows), findings
   (`m4-findings.md`), and `declared-deviations.yaml` (spot-checked DEV-001/002/024/025/120/171/172).
2. Extracted pass/fail and the load-bearing fields from every evidence JSON via `jq` rather than trusting
   prose: boundary, invariant, route-coverage, xas-ledger, bas-ingest, direct-journeys, direct-provision,
   e2e-replay, assurance-run, hypothesis-lifecycle, lifecycle-validation, harness-selftest, recovery-matrix.
3. Read the reviewer-relevant handoffs in full (T16, T17, T19, T22) and grepped the rest for
   overclaim/repro/finding terms.
4. **Spot-checked the highest-stakes claims directly in the accepted source** (not via reports).
5. Wrote the review treating every report as a claim; downgraded two labels; confirmed F-M4-001.

## Claims verified against source (result)

| Claim | Source checked | Result |
|---|---|---|
| F-M4-001 IDOR: `free_payout` unscoped by merchant | `freePayout/core.go:678-739`, `payout_routes.go:78-82` (nil mw), `payoutController.go:1157-1183` (only BalanceID), `service.go:1115-1126`, `bankingAccount/core.go:391` (balance-id-only lookup) | **CONFIRMED** — no merchant reference anywhere in the chain |
| Scoped route denies cross-tenant (negative control) | `m4-direct-e2e-replay.json:candidates[H-D3].arms.expected_denial_control` (400) | **CONFIRMED** |
| PS posts NO Direct ledger journal at this HEAD | `payouts core.go:7284` + `7356` (`//err = c.SendLedgerEventPostBasLinking(...)` commented), `core.go:2575-2577` (CA skip `if !EqualFold(...,Shared){return nil}`) | **CONFIRMED** |
| Direct status map: Direct/RBL failed→failed (not reversed); Shared failed→reversed | `appConstants/states.go:65-128` FtsToPayoutStatusMap | **CONFIRMED** (SLICE also present in Direct map → L-012 accurate) |
| `VerifyPayoutFailedTransaction` is route-scoped to the FTS-direct webhook | `fts_transfer_status_webhook.go:612,654-710`; relay path `HandlePayoutStatusUpdateViaFTS core.go:1465` has no such call | **CONFIRMED** — F-T10-1 source-faithfulness holds |
| Kafka Direct FAILED/REVERSED drop (EF-002/003) | `KAFKA_ROUTE_EVIDENCE.md`; `fts_status_updates.go:57-63`, `_retry.go:60-66` | **source-inferred, NOT executed** — transport observed only |
| L-018 DCS panic = substitute bug | T16 + DEV-172; production DCS redaction confirmed in pinned clone | **CONFIRMED** (substitute, not source/security) |
| H-D4 benign (binds to caller, B untouched) | `m4-direct-e2e-replay.json:candidates[H-D4].oracle` | **CONFIRMED** benign; CFA-resolution is substitute-grade |

## Overclaims found

1. **Route F "independently_reproduced"** in `m4-fidelity-matrix.csv` — **downgrade** to "transport
   executed; Direct-drop source-inferred." Only FTS→PS transport was observed (and it failed to reach
   terminal state for an unrelated unregistered-jobs bootstrap fact); EF-002/003 was asserted from source,
   not run. `m4-route-coverage.json` R3 is more honest.
2. **`cfa-server` matrix row `real`** — its material behaviour (fund-account resolution, no merchant filter)
   is substitute-grade; would qualify as `real-binary/substitute-resolution`. The H-D4 text is honest about it.
3. **Autonomous-assurance gate passes vacuously** — `m4-hypothesis-lifecycle.json` `gate.passed=true` with
   `total_hypotheses=0`; the live campaign (`m4-assurance-run.json`) found **0** hypotheses. F-M4-001 came
   from a human hypothesis + deterministic replay, NOT the model loop. No handoff claims otherwise, but the
   "autonomous discovery" framing must not be read as the model finding the IDOR.

Honest non-overclaims worth noting: F-T10-1 relay outcome is **timing-dependent** (reproduced by T10/T19,
not T17) — a limitation, correctly classified as a source-faithful non-finding by both.

## F-M4-001 disposition

**CONFIRMED** as a verified twin finding (broken object-level authorization / cross-tenant disclosure) with
**production reachability UNKNOWN** (kong-lite mints the passport; production edge/monolith ingress scoping
is out of corpus). Sound divergent-config canary, sound negative controls, independent reproduction on a
second fresh pair via a different mechanism (Broker gateway, deterministic oracle, no model). No downgrade,
nothing overclaimed. Twin source correctly left unpatched.

## Confidence

**High** on F-M4-001 (verified against source line-by-line + deterministic replay + negative controls) and
on the Direct/recon/accounting execution (real binaries, real balanced Ledger rows, negative-controlled
invariants). **High** on the two label downgrades. **Medium** only on the parts that are inherently
UNKNOWN by construction (DA-journal production reachability; F-M4-001 production reachability) — correctly
stated as UNKNOWN, not resolved.

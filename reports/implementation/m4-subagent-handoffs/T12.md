# T12 — Source Map and Specification Updater (Milestone 4)

Task ID: **T12** (`m4-task-ledger.jsonl` line 22: `source_map_spec_updater`; depends on T02..T07). Date 2026-09-07.
Mode: documentation only — no docker, no network, no commits. Owned files only; `ENV2_COMPOSE/**` and `RED_LOOP/**`
untouched (their concurrent modifications in `git status` belong to T09/T10/T11/T13).

## Inspected files

- Handoffs (read in full): `reports/implementation/m4-subagent-handoffs/T02.md` (428 lines), `T03.md` (362), `T04.md` (291),
  `T05.md` (222), `T06.md` (230), `T07.md` (301); headers of `T08.md`, `T13.md` (out of scope, harness).
- Coordinator files: `reports/implementation/m4-decisions.md` (D-001..D-007), `m4-contradictions.md` (C-001..C-014),
  `m4-known-limits.md` (L-001/L-002), `m4-source-map.md` (placeholder), `m4-execution-plan.md` (phases, no numeric G11 text),
  `m4-task-ledger.jsonl`, `m4-direct-provision.json` (header only, T09 in progress).
- Specs (read in full before editing): `TWIN_SPEC/{README.md,route-matrix.yaml,state-machines.yaml,expected-failures.yaml,
  components.yaml,architecture.yaml,synthetic-data-model.yaml}`; grep-level: `runtime-topology.yaml:84-90,248-256`,
  `acceptance-invariants.yaml`, `configuration-manifest.yaml`, `substitute-contracts/xas-recon-repair.md` §1-4.
- Read-only context: `ENV2_COMPOSE/substitutes/xas-sink/CONTRACT.md` (C-011 claim, not edited — T11 owns it).

No source repositories were re-read; every `file:line` in the deliverables is copied from a handoff, with the handoff and
section named next to it.

## Changes

| File | Change |
|---|---|
| `reports/implementation/m4-source-map.md` | Rewritten (313 lines): conventions + 8 row labels; §1 end-to-end map (34 rows A1-A7, F1-F7, S1-S7, R1-R5, B1-B14, P1-P2, I1) each with citations and an initial D-006 label; §2 Direct status-transport route matrix (T05 rows A-I); §3 reservation state machine (T04); §4 DA ledger event table (T03 Finding 6); §5 table/column/ID cheat sheet; §6 workers present/missing; §7 deployment-unknown assumptions U-01..U-17 (G11 feed); §8 contradiction → spec cross-reference |
| `TWIN_SPEC/route-matrix.yaml` | version 2; `merchant_class_selectors.shared_vs_direct` rewritten (monolith/payouts/fts/ledger/x-balances); new `direct_source_selection`; `create_fta_direct_marker` channel condition + SLICE note; `queueing_gate_direct` twin note un-staled; `ps_fts_direct.twin`, `ps_direct_route` (guards + twin) un-staled; `kafka.drop_root_cause`; `reservation_reconcile.twin`; new `direct_status_transports` (A-I) and `direct_reconciliation_routes` (R1-R15) with `fidelity: real|substitute|expected-failure|missing` |
| `TWIN_SPEC/state-machines.yaml` | version 2; `fts_to_payout_status_map` lower-cased channels + 5 scope/guard notes; `monolith_fta_to_payout_status_map.slice_divergence` + `secondary_rule`; `ledger_journal.events_direct_account_x` filled (events, entries, who_posts, discovery_failure) + `ps_call_timing.direct_any_event`; `inflight_reservation` reserve/release/reconcile sharpened + full `transitions` table + `lag`, `balance_refresh_producer`, `invariants`; new machines `bas_statement_linking`, `da_journal_events` |
| `TWIN_SPEC/expected-failures.yaml` | EF-001 mechanism attribution note corrected (T02 #3); new EF-004, EF-005, EF-006, EF-007, EF-008 (existing ids unchanged) |
| `TWIN_SPEC/synthetic-data-model.yaml` | version 2; M2 identity map (features, x-balances status, SA casing, routing-rule note, BASD absence); `fts.source_accounts.account_type` lowercase + C-005 conflict; `direct_account_routing_rules` not required (C-003); `x-balances.balance` `account_type`/new `status`/`balance` rows (C-007/C-008); `monolith.banking_accounts.account_type` CURRENT (T02 #11); new sections `direct_merchant_record_set` and `direct_statement_and_reconciliation_records`; two new `cross_service_consistency_rules` (fund_account_id vs id; basd join key) |
| `TWIN_SPEC/components.yaml` | `fts` (casing, no default rule needed), `xas_x_account_statements` (three matchers, one PS-only queue, xas-sim target), `ledger` (DA accounts), `banking_accounts` (credentials route), `mozart` (account_statement), `api_monolith` (DA emitter), `x_balances` (inert worker, EF-007) |
| `TWIN_SPEC/architecture.yaml` | `system_of_record.money` (Direct authority = x-balances/BASD; PS posts no DA journal); `families.direct_current_account_rails.path` rewritten + 2 key_facts; `account_statements_and_reconciliation` summary |
| `TWIN_SPEC/README.md` | "Milestone 4 update" section: per-file correction table, what was deliberately not changed (xas-sink CONTRACT.md → T11), open contradictions |
| `reports/implementation/m4-known-limits.md` | Appended L-003..L-017 (class + evidence each) |

YAML validity: all nine `TWIN_SPEC/*.yaml` load with `python3 -c "import yaml; yaml.safe_load(...)"` (two unquoted
scalars with embedded `: ` in the new route-matrix sections were quoted during validation).

## Contradiction ID → spec file:line

| ID | Spec change (file:line) | Status carried |
|---|---|---|
| C-003 routing rule not required | `route-matrix.yaml:79` (`direct_source_selection`), `:87`; `synthetic-data-model.yaml:53,442`; `components.yaml:57` | resolved |
| C-004 / C-010 PS posts no Direct journal; DA emitter substitute | `architecture.yaml:27,60`; `route-matrix.yaml:76,262` (R12); `state-machines.yaml:254-266` (`events_direct_account_x.who_posts`), `:348` (`da_journal_events`); `expected-failures.yaml:185` (EF-004); `components.yaml:22,72` | resolved; production reachability unknown |
| C-005 SA casing | `synthetic-data-model.yaml:53,432`; `components.yaml:57`; `route-matrix.yaml:75` | OPEN (inline) |
| C-006 FTS selector; `update_fts_fund_transfer` injected | `route-matrix.yaml:75,79,110,137` | resolved |
| C-007 x-balances enum | `route-matrix.yaml:77`; `synthetic-data-model.yaml:554`; `components.yaml:80` | OPEN (inline) |
| C-008 x-balances status | `route-matrix.yaml:77`; `synthetic-data-model.yaml:52,555,695` (record set uses `active`); `components.yaml:80` | OPEN (inline) |
| C-009 BalanceRefreshEvent unreachable | `state-machines.yaml:321` (`balance_refresh_producer`); `expected-failures.yaml:314` (EF-007); `route-matrix.yaml:177`; `components.yaml:80` | OPEN → EF-007 declared |
| C-011 xas-sink contract | `components.yaml:178`; `route-matrix.yaml:220` (R5), `:227` (R6); `README.md:29` (ENV2 CONTRACT.md deferred to T11) | resolved in spec; ENV2 file not touched |
| C-013 three matchers | `components.yaml:178`; `route-matrix.yaml:227` (R6), R15; `architecture.yaml:109`; `state-machines.yaml:325` (`bas_statement_linking`) | resolved |
| C-014 field-name mismatch | `expected-failures.yaml:241` (EF-005); `route-matrix.yaml:220` (R5 `field_mismatch`) | open (preserved) |
| T02 #3 Kafka drop root cause | `expected-failures.yaml:72` (EF-001 note), `:277` (EF-006); `route-matrix.yaml:151`; `state-machines.yaml:162` | resolved |
| T02 #4 DCS keys | `route-matrix.yaml:74` | resolved |
| T02 #6 / #7 Direct channels list (unlisted-channel fallback; PS channel condition) | `state-machines.yaml:160`; `route-matrix.yaml:87` | resolved |
| T02 #11 monolith `banking_accounts.account_type='current'` | `route-matrix.yaml:73`; `synthetic-data-model.yaml:314` | resolved |
| T02 #12 SLICE divergence | `state-machines.yaml:186`; `route-matrix.yaml:87`; `m4-known-limits.md` L-012 | deliberately simplified |
| T04 C2 (queueing gate note stale) / C3 (release paths) | `route-matrix.yaml:92`; `state-machines.yaml:292-321` | resolved |
| T05 #2 (`ps_direct_route` stale), #4 (source-updater listed as polling) | `route-matrix.yaml:137`; #4: no "payout-source-updater" text exists in the current `route-matrix.yaml` — nothing to remove | resolved / n-a |
| T07-C1 M2 features | `synthetic-data-model.yaml:50` | resolved |

New expected-failure ids: EF-004 (`:185`), EF-005 (`:241`), EF-006 (`:277`), EF-007 (`:314`), EF-008 (`:368`).
New known limits: L-003..L-017.

## Unresolved

1. G11 numbering: `m4-execution-plan.md` does not spell out G11; the "Deployment-unknown assumptions" list (`m4-source-map.md` §7, U-01..U-17) is written to be consumed by whatever gate the coordinator assigns.
2. Initial row labels in `m4-source-map.md` §1 follow D-006 and M3.1 evidence (`executed` only where a retained run/verifier exists: V08/V12/V13/V16/V18/V19/V20, route/kafka scenarios). Rows marked `implemented`/`substitute` must be re-labelled by the coordinator after T09-T11 land and Phase 3-5 runs.
3. `ENV2_COMPOSE/substitutes/xas-sink/CONTRACT.md` still carries the C-011 claim ("payouts/ledger/FTS enqueue"); correction is recorded in `components.yaml` and `README.md`, the file itself is T11's (xas-sim).
4. `TWIN_SPEC/substitute-contracts/xas-recon-repair.md` §1 was not edited (not in the T12 ownership list); it is consistent with C-013 except that it does not mention the monolith/PS matchers or the timestamp mismatch — README notes this.
5. `runtime-topology.yaml:87,251` (C-T03-6: config key + queue also missing, not just the container) left untouched — not in the requested correction list; the fact is recorded in `m4-source-map.md` §6 and `components.yaml` xas entry.
6. Open contradictions C-005/C-007/C-008 are carried inline as OPEN; their resolution is T09's runtime observation (`fts_worker_route_observed`, x-balances row values), not a spec decision.
7. T04 handoff cites `PS:config/default.toml:388` for the payouts→x-balances payout-event queue name; T07 cites `arena.toml:419-420`. Both recorded as-is (A7 row); no conflict, different files.

## Confidence

- Source map citations: **high** that every citation is verbatim from a handoff (no citation was invented; where a handoff marked INFERENCE the row says so).
- Spec corrections for resolved contradictions (C-003/004/006/010/011/013, T02 #3/#4/#11/#12): **high**.
- New record set / CSV contract / DA account sections: **high** for column constraints (copied from T03/T04/T05/T07), **medium** for the provisioner-recipe values that T09 may still change (fts id range, secret names).
- EF-004..EF-008 `deployed_status` text: **medium** — restates handoff evidence; nothing verified live by T12.
- Row labels: **low/medium** by design (initial, pre-execution).

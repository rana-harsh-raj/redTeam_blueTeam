# ARCHITECTURE_CORRECTIONS — what the earlier reports got wrong, verified against source (2026-09-05)

Scope: corrections to `PAYOUTS_ARCHITECTURE.md`, `PAYOUTS_FLOW_CATALOG.md`, `ARCHITECTURE_DELTA.md`, `CONTROL_AND_INVARIANT_CATALOG.md`, `UNRESOLVED_QUESTIONS.md`, `ENV2_BUILD_STATUS.md`, `SYNTHETIC_FIXTURE_SPEC.md`, `EFFECTIVE_CONFIG_GAPS.md`, the raw findings 01–31 and the twin's own CONTRACT.md files. Every row cites the source that decides it. Full detail per lane: `reports/fidelity/raw/01..12_*.md` (§"Corrections to existing reports").

## A. Corrections that change the architecture picture

| # | Earlier claim (where) | What source says | Evidence | Lane |
|---|---|---|---|---|
| A1 | The monolith must identify a payout's merchant by header or probe; the twin brute-forces passports per merchant (`monolith-stub`, ENV2 notes) | The monolith reads the PS payout row directly over a second MySQL connection into PS's database and takes `merchant_id` from the row | `api/app/Models/Payout/Repository.php:5313-5323`; `Core.php:8461-8530`; `api/database/Connection.php:37`; `config/database.php:608-623` | 01, 05 |
| A2 | `ps_*` tables are the monolith's shadow tables for PS data | `ps_*` tables are CI-only test scaffolding ("This table doesn't exist on prod"); prod uses `payout_service_database` connection | `api/database/migrations/2022_07_01_095954_create_ps_payouts.php:12-14`; `app/Base/Repository.php:1454-1471` | 05 |
| A3 | `beneficiary_bank_code` is undocumented schema drift of unknown provenance (W10, UNRESOLVED #109) | Never in any of 51 migration commits; the monolith's dual-write explicitly `unset()`s it as "present only in Payouts Service's payout_details" — intentional PS-only out-of-band DDL | `gh api repos/razorpay/payouts/commits?path=internal/database/migrations`; `api/app/Models/Payout/DualWrite/PayoutDetails.php:10-13,63-64`; `payouts/slits/**/setup_test.go` | 05 |
| A4 | Batch's `payout_approval` calls `POST payouts/bulk_approve` on Payouts Service, bypassing WFS (ARCHITECTURE_DELTA Confirmed-1, 23 §B.8) | `payouts/bulk_approve` is a **monolith** route; the handler calls `Core::approvePayout` per row which invokes WFS `ActionAPI` when a `workflow_entity_map` row exists; no such route in `payouts` | `api/app/Http/Route.php:2319`; `Service.php:3539-3681`; `Core.php:3232-3335,3589-3604`; grep of `payouts` router | 02, 04, 08 |
| A5 | No loop guard exists anywhere (W6/C45) | Edge/Kong has none for the Edge→PS divert; PS's own shadow gateway has a working `X-PS-Proxied` guard (disabled by default); the monolith logs `X-Payouts-Service-Proxy` presence | `payouts/internal/routing/shadowgateway/middleware.go:14-18,91-96`; `monolith_client.go:183`; `api PayoutController.php:88-95` | 02, 03 |
| A6 | `X-Payouts-Service-Proxy` 1=classic / 0=direct, two mechanisms | Three coexisting proxy mechanisms; the V2 balance-based pass-through sets no header at all | `api/app/Services/PayoutService/Create.php:133-186,208-219` | 02 |
| A7 | Create/OTP/approve/reject routes are also in the `$admin` group (20 §1) | None of them is in `$admin`; `payout_create` is `$private` only | `api/app/Http/Route.php:5780,5595-5596,7350-7353,8002-8005` | 02 |
| A8 | Dashboard role enforcement reads `Route::$routePermission` (20 §3) | `UserAccess` reads `Route::$bankingRoutePermissions`; `$routePermission` serves `AdminAccess`/Workflow | `api/app/Http/Middleware/UserAccess.php:518-527`; `AdminAccess.php:117` | 02 |
| A9 | AuthZ integration in the monolith is mock-only (20 §7) | `AccessAuthorizationService` → `AuthzEnforcer\Service::enforcerAPIEnforce` is live and env-toggled (`AUTHZ_XPLATFORM_ENFORCER_MOCK`, default true) | `api/app/Http/AccessAuthorizationService.php:56-102`; `config/applications.php:2205-2212` | 02 |
| A10 | Dashboard → monolith identity is a passport JWT (07 #34 "no proxy mechanism found") | Legacy dashboard proxy mints Basic `rzp_<mode>_<merchantId>` + shared secret + `X-Dashboard-User-*` headers; passports only on the newer `apps/shell` and `xperience` surfaces | `dashboard/app/Admin/ApiRequestAny.php:141-503`; `xperience/config/default.toml:82-89` | 02 |
| A11 | `PAYOUTS_FEATURES_FORWARD/REVERSE_DUAL_WRITE` not found anywhere (UNRESOLVED #6) | Present as env-templated TOML keys; baseline reverse=true, forward=false; runtime value still outside repos | `payouts/config/prod.toml:665-666`; `default.toml:715-716`; `internal/config/config.go:472-473` | 02 |
| A12 | PS sends `config_id` to WFS from a prior ConfigAPI call (23 §C.6.1) | PS never sets `config_id`; WFS resolves by `(owner_id, owner_type, service, org_id, enabled)` | `payouts/pkg/workflow/workflow_create.go:225-262`; `workflows/internal/entities/config/repo.go:93-104` | 08 |
| A13 | `enable_approval_via_oauth` is an OTP/2FA gate of unknown location | It is a per-merchant OTP **bypass** for OAuth partner approvals; OTP is otherwise mandatory on single dashboard approve; Batch bulk_approve never calls OTP | `api/app/Models/Payout/Service.php:1165-1226,6914-6932`; `Validator.php:2389-2407` | 08 |
| A14 | PS is the sole caller of `banking_account_statement/payout_update` (UNRESOLVED #31 CLOSED) | XAS calls the same monolith route live (under an existing app credential); the handler can flip a failed payout to reversed on a matched return credit | `x-account-statements/internal/gateway/api/service/service.go:34-35,108-127`; `api Core.php:10286-10379` | 11 |
| A15 | Stuck-payout alerts exist but are narrow (C58) | The two alert rules query labels the metric never emits → effectively dead; no monitor covers `initiated` | `alert-rules/rules/prod-rules/payouts_rules.yaml:2222-2269`; `payouts/internal/metric/metric.go:1014-1021` | 11 |
| A16 | Governor/Charge-Collections is the pricing path (26 §C) | Default path is Redis-cached rule or monolith `fetch_pricing_info`; Governor only under Splitz `ChargeCollectionsCallExperiment`; one `pricing` table, not plans+rules | `payouts/internal/app/payouts/processor/payout_pricing.go:138-355`; `api/database/migrations/2014_07_22_114552_create_pricing.php` | 04, 10 |
| A17 | ASV is REST/Twirp | Native gRPC (`goutils/account-service`) | `payouts/pkg/account/client.go:15-28` | 10 |
| A18 | Kafka and HTTP status paths may both fire (UNRESOLVED #19) | Mutually exclusive per transfer (early return) | `fts/internal/transfer/service.go:1136-1141` | 09 |
| A19 | Stork wire format JSON (25 opening) | Payouts uses the protobuf Twirp client; the twin's JSON patch is necessary | `payouts/pkg/stork/client.go:105` | 09 |
| A20 | Merchant webhook `Create` caller unknown (25 §A.3) | Monolith `WebhookV2Controller@create` | `api/app/Http/Route.php:946`; `app/Models/Merchant/WebhookV2/Stork.php:82-99` | 09 |
| A21 | Axis/IDFC/Kotak fixtures usable via `mozart -mock` (25 §B.4) | Fixture dirs exist but have no `mappings.go` entry | `mozart/app/mock/mappings.go:1-156` | 06 |
| A22 | Channel health maintained by an FTS cron (03 §5) | Written by the Alertmanager webhook `/v1/alert_manager` and ops routes; cron is read-only alerting | `fts/internal/controllers/alert_manager.go:27`; `route_list.go:186-197`; `channel/cron.go:35-169` | 06 |
| A23 | Dual DCS/API-DB feature naming affects 3 flags (12 §A.6) | At least 10 mismatched pairs; only one is OR-reconciled | `payouts/pkg/dcs/features/features.go:75-133` | 04 |
| A24 | 25 payouts worker Deployments match 25 job configs (W9) | 24 Deployments; 2 deployed names have no job constant, 3 jobs have no Deployment | `kube-manifests/templates/payouts/templates/*-live.yaml`; `payouts/internal/job/*.go` | 04, 12 |
| A25 | `[kafka_consumers]/[task]/[topics]` only in devstack.toml (EFFECTIVE_CONFIG_GAPS §L, W9) | Present in `prod.toml` with production values | `payouts/config/prod.toml:312-343,362-363` | 12 |
| A26 | Payouts needs `sqs_local` to honour an endpoint | Payouts' vendored SQS broker always honours `Endpoint`; `sqs_local` is a ledger convention | `payouts/pkg/worker/queue/broker/sqs/queue.go:61-68` | 12 |
| A27 | `bulk_approve` symbol absent from all repos | `BATCH_SERVICE_PAYOUT_APPROVAL_BULK_RESPONSE` exists in the monolith trace codes | `api/app/Trace/TraceCode.php:7970,8002` | 08 |
| A28 | Payout sub-model table is `payouts_status_details` (PS) | PS table is `payout_status_details` (singular); the monolith's is `payouts_status_details` | `payouts/internal/database/migrations/20211026104754:13`; `api/app/Constants/Table.php:90` | 04, 05 |
| A29 | "MerchantBalance" is a PS concept (C15/C16/C19) | Ledger-side account type; PS only sends generic journal requests | grep of `payouts` | 04 |
| A30 | 3-month queued expiry has no ceiling in PS (12 §B.3) | `IsValidQueuedPayout` exists but has zero production call sites (dead) | `payouts/internal/app/payouts/validation.go:45-70` | 04 |

## B. Corrections to the twin's own documentation and seeds

| # | Twin artifact | Wrong | Right | Evidence |
|---|---|---|---|---|
| B1 | `seeds/s4/ledger.sql` §3, `ledger_accounts_via_api.sh:53-62` | M2 (fts_fund_account_id 900002, bank_account_type CURRENT) seeded with `fund_account_type: nodal`; Current parent accounts missing | `current`, under `Razorpay Current Receivable/Payable` parents via `current_pool_account_onboarding` | lane 07 §3 (fix SQL included) |
| B2 | `seeds/schema-patches/apidb.sql`, `seeds/mysql/apidb-ddl/00_init.sql` | `fund_transfer_attempt`, `banking_account`, `payout_status_details` singular; `payouts_details` extra `id` PK; `reversals.payout_id`, `fees`; `balance.primary`; `features` without UNIQUE(name, entity_id) | see `TWIN_SPEC/schema/apidb-subset.mysql.sql` | lanes 01, 05 |
| B3 | `seeds/mysql/apidb_seed.json`, `seeds/mysql/xbalances_seed.sql` | never loaded / wrong table name | delete or mark superseded | lanes 01, 05 |
| B4 | `config/templates/base/payouts/arena.toml:546` | `client_side_eval = true` (copied from default.toml) | production `prod.toml:442` has `false`; stub's empty `FetchDecisionContext` makes every payouts experiment resolve to default | lane 10 + verification |
| B5 | `substitutes/splitz-stub` | unseeded experiments default to `on` | production fails safe to `off` | lane 04 |
| B6 | `substitutes/monolith-stub/server.py` | `dual_write` returns `queued`; `status_details_source_update` echoes ids; `deduct_credits` stateless; `fetch_pricing_info` ignores channel/method/amount/balance_id; `decrement_free_payouts` keyed by merchant; `create_fta` hard-fails on FTS error; `internal/merchants` exposes `account_type/channel/balance_id` | see `TWIN_SPEC/substitute-contracts/monolith-stub.md` | lane 01 |
| B7 | `scripts/cron-driver/driver.py` | `/process_on_hold_payouts`, `/reservation_reconcile`, `/process_dual_write_failures` do not exist; `/process_batch_submitted_payouts` missing | real names in `payouts/internal/routing/router/cron_routes.go` | lane 04 |
| B8 | `config/templates/base/payouts/arena.toml:299-316` | `[workflow] host = http://127.0.0.1:1` (dead) — M3 workflow creates fail with 400 | run WFS or a substitute | lane 08 |
| B9 | `dcs-stub/CONTRACT.md` | describes the abandoned `Env` workaround | actual mechanism is the `ARENA_DCS_URL` module patch (covers login and per-request calls) | lane 10 + `build/arena-patches/goutils-dcs-v1.7.3/dcs.go:111,180-186,192` |
| B10 | `verifier/verifiers/test_v21_*.py` | calls `/v1/captured` | stork-capture exposes `/_captured/events` | lane 09 |
| B11 | `kong-lite/CONTRACT.md`, `seeds/merchants.json` | `[passport.arena]`; key ids embed the merchant id | section is `[passport.edge]`; real key ids are random, merchant via `keys.merchant_id` | lane 03 |
| B12 | `ENV2_BUILD_STATUS.md` V12 attribution ("DCS key shape") | DCS stub bytes are correct and reachable; likelier cause: reconciler heartbeat never set (cron path bug) so the gate fails closed, plus Splitz client-side eval | lanes 04, 10 + verification |
| B13 | `ENV2_BUILD_STATUS.md` V06/V18 attribution (timing/headroom only) | V06: payout_processed is async on `payout_update_failure_handling` and Shared-only; V18 stimulus bypasses the monolith remap; V10 expectation wrong (400 at create is production behaviour) | lanes 01, 04, 07 |
| B14 | `seeds/localstack/init-queues.sh`, arena payouts `[queue]` | `stage-x-balances-…-payouts-event` | prod names `prod-x-balances-balance-refresh-live`, `prod-x-balances-payout-event` (naming only) | lane 12 |

## C. Lane inferences overridden by verification during synthesis

| Lane claim | Verification | Outcome |
|---|---|---|
| Lane 10 §A.4: per-request DCS `Get` calls still resolve to the real hostname because only `NewService` is patched | `build/arena-patches/goutils-dcs-v1.7.3/dcs.go:180-186` makes `GetContextUrl` return `ARENA_DCS_URL` whenever the context has no URL; `getURLAndMode` checks it first (`:192`) | DCS reachability is fine; lane 10's INFERRED gap withdrawn |
| Lane 03 §Cannot-derive #1: prod `[passport.edge]` identifier unknown | `payouts/config/prod.toml:198` sets `identifier = "edgev2"` | resolved: prod kid is `edgev2` |
| Lane 10 §B.2 framed the Splitz gap as needing `FetchDecisionContext` | `payouts/config/prod.toml:442` `client_side_eval = false` | the correct fix is arena config parity (server-side Evaluate); FetchDecisionContext optional |

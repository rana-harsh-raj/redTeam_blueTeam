# T25 — Two-Boot Reverification (post-fix) — handoff

**Task:** T25 (Milestone 4). **Date:** 2026-09-06/07 UTC. **Branch:** milestone-4-direct-reconciliation.
**Fix under test:** C-018 — M4 boundary `hardened_edge` fixture now SKIPs (not fail/error) when `KONG_ENFORCE_ROUTE_POLICY!=1`.
**Verdict:** **BOTH BOOTS CLEAN GREEN** — 0 failed, M4 boundary suite skipped on the frozen baseline; provisioning + Direct payout proven from empty volumes on both, distinct fresh ids; egress 0 both; live arena restored.

## Sequence (strict single-arena, memory-safe)

Snapshot live → stop live (keep volumes) → boot A → verify → provision A → teardown A → boot B → verify → provision B → teardown B → restart live → verify. Only one 66+-container arena ran at any time.

## Commands (key) + outputs

| Step | Command | Result |
|---|---|---|
| Snapshot | python capture (nets/volumes/container-ids/enforce) → `RED_LOOP/runs/m4-t25-20260906T221546Z/live-arena-snapshot.json` | 67 running/66 healthy, 68 containers, nets `bedd720758fa`/`72e15908c82e`, enforce=1 |
| Stop live | `cd ENV2_COMPOSE && docker compose --env-file .env.arena --profile datastores --profile substitutes --profile core stop` | 0 running, 17 arena volumes kept |
| Boot A | `ARENA_HOST_BRIDGE=1 bash ENV2_COMPOSE/scripts/clean-boot.sh m4-final2-a` | **27p/0f/10skip**, egress passed 0 outside / 6793 control, coverage proven, boot_id `e0a145bf-13c8-45e3-a3bf-d75b744fa6fa` |
| Provision A | isolated runner (redirects `config.ENV2`→`.local/m31-clean/m4-final2-a/ENV2_COMPOSE`) → `provision_direct_merchant`+`self_check`+`prove` | **ARENAD85334359**, 38 steps 0-fail, self-check **29/29**, proof **15/15 ok** (processed=true, failed=true) |
| Teardown A | `python3 ENV2_COMPOSE/scripts/instance-cleanup.py m4-final2-a` | CLEAN=True (0/0/0) |
| Boot B | `ARENA_HOST_BRIDGE=1 bash ENV2_COMPOSE/scripts/clean-boot.sh m4-final2-b` | **27p/0f/10skip**, egress passed 0 outside / 9987 control, coverage proven, boot_id `232e25bf-f908-4476-877f-f9c69b5465fd` |
| Provision B | isolated runner (instance m4-final2-b, kong 18368) | **ARENAD83727794** (distinct), 38 steps 0-fail, self-check **29/29**, proof **15/15 ok** (processed=true, failed=true) |
| Teardown B | `python3 ENV2_COMPOSE/scripts/instance-cleanup.py m4-final2-b` | CLEAN=True (0/0/0) |
| Restart live | `docker compose … --profile … start` | 67 running / 66 healthy, container-id set identical (68=68), nets unchanged, enforce=1, kong 401 on :18080 |

### Verifier line (both boots, from junit.xml)
`37 total / 27 passed / 0 failed / 0 error / 10 skipped / 0 xfailed`.
The 10 skips are all M4 boundary items across 7 files: `test_m4_g46/g47/g48/g49/g50/g52` (9 items) + `test_m4_hd3_free_payout_ownership` (1 item), each skipped with "hardened edge not active (KONG_ENFORCE_ROUTE_POLICY!=1; probe got 401)". This is the C-018 fix behaviour (skip, not fail/error). hd3 skips under the same guard (not xfail).

## Findings / anomalies

1. **Passed count is 27, not 26.** Task expected "26 passed". The frozen non-boundary verifier suite is now **27 tests** (it grew by one M4-era non-boundary verifier since T01's 26-test M3.1 baseline). Both boots are still clean green (0 failed, 10 boundary skipped). `both_2600=true` is set on the **real** acceptance gate — `failed==0` with the M4 boundary suite skipped on the frozen baseline — and each `verifier.passed` records the true 27. Not fabricated to 26.
2. **Host bridge required for provisioning.** clean-boot.sh defaults `ARENA_HOST_BRIDGE=0`, which leaves the instance kong port unpublished, so the host-side provisioner/self-check kong probe (`P._kong_call` via urllib) returns ERR → self-check 28/29. A first no-bridge boot of m4-final2-a produced the identical verifier result (27p/0f/10skip, boot_id `26c05665`) but self-check 28/29 (`kong.auth_probe` ERR). Re-booting with `ARENA_HOST_BRIDGE=1` (matches T24) starts the loopback ingress bridge on the instance kong port; `FROZEN_BASELINE` stays on (enforce=0), so the verifier result is unchanged and provisioning reaches kong. Both final boots use `ARENA_HOST_BRIDGE=1`. Tooling gap, not a twin defect.
3. **Stale T24 ingress bridges.** Host `ingress.py` processes for T24's long-gone `m4-final-a` (:18579) and `m4-final-b` (:18457) were still alive at task start; killed at teardown. Live bridge on :18080 left untouched.

## Committed-evidence hygiene

The surface `m4_direct_provision.py all` writes `reports/implementation/m4-direct-provision.json`. To avoid touching committed evidence I did **not** run the surface directly; the isolated runner replicates the `all` flow (provision→self-check→prove for one fresh merchant) and writes **only** into `RED_LOOP/runs/m4-t25-.../provision-{a,b}/`. Verified `reports/implementation/m4-direct-provision.json` sha256 `2591bad715ed963b03755c78970264f2518ebc683c3e241ab8c13cd34aa9b4df` unchanged before and after both provisions; `git status` shows it unmodified.

## Artifact written

`reports/implementation/m4-finalize-boots.json` (overwrote T24's) — `kind=M4_FINALIZE_BOOTS`, both boots with boot_id/verifier/egress/provision, `both_2600=true`, `consistent=true`, `egress_all_zero=true`, `provisioning_both_passed=true`, plus explicit `expected_passed_note` (27 vs 26) and `boundary_skips_note`.

## Live-arena restoration proof

`RED_LOOP/runs/m4-t25-20260906T221546Z/live-restoration-proof.json`: 67 running / 66 healthy, `rzp-arena bedd720758fa` + `rzp-ingress 72e15908c82e` unchanged, container-id set identical to snapshot (68=68, `container_ids_match=true`), `KONG_ENFORCE_ROUTE_POLICY=1`, kong 401 on :18080. Zero disposable leftovers (containers/networks/volumes with `env2c_`/`m4-final` prefix = 0). `down` never used on the live project.

## Evidence paths (absolute)

- `/Users/rana.singh/rzp-payouts-architecture/reports/implementation/m4-finalize-boots.json`
- `/Users/rana.singh/rzp-payouts-architecture/RED_LOOP/runs/m4-t25-20260906T221546Z/` — `live-arena-snapshot.json`, `live-restoration-proof.json`, `pre-stop-*.txt`, `clean-boot-a{,2}.log`, `clean-boot-b.log`, `provision-a{,2}.log`, `provision-b.log`, `boot-a/{junit.xml,egress.json,arena-fingerprint.json}`, `boot-b/{...}`, `provision-a/{descriptor,selfcheck,proof,m4-direct-provision-*}-*.json`, `provision-b/{...}`
- `/Users/rana.singh/rzp-payouts-architecture/RED_LOOP/runs/cleanboot-m4-final2-{a,b}/verifier-run/` — junit.xml + egress.json (in-place clean-boot evidence)

## Confidence

High: both boots' verifier + egress + provisioning (machine-derived, junit/egress/descriptor artifacts retained), committed-file integrity (sha before/after + git status), live restoration (container-id set + network ids + enforce verified after restart). The only judgement call is `both_2600` under a 27-vs-26 passed count — resolved on the real gate (failed==0, boundary skipped) and fully disclosed.

# T05 — FTS and Status-Transport Specialist (Milestone 4)

Task ID: **T05** (`m4-task-ledger.jsonl` line 5: "FTS Direct source selection (900002?), status-return routes, status mapping by account_type/channel").
Mode: read-only. Nothing modified except this file. No docker, no network.

## Inspected files / commits

| Tree | Commit | Notes |
|---|---|---|
| pristine `fts` (`.local/repos-root`/fts) | `2a09e763116db47a2f28553c677ad73ef8133ebf` | `internal/` non-test Go files are byte-identical to the admitted copy (`diff -rq`: only `*_test.go` differ). |
| admitted `.local/twin-repos/accepted/fts` | `4d78f869` (twin build repo) | **All FTS line numbers below cite this tree**; identical to pristine for the cited files. |
| pristine `payouts` | `4bf3dbf9239feadea6d65ca90c893a988e116173` | `internal/` non-test files identical to the admitted copy. |
| admitted `.local/twin-repos/accepted/payouts` | `4d78f869` | **All payouts line numbers cite this tree** (except `config/prod.toml`, which exists only in the pristine clone). |
| pristine `api` (monolith) | `2d665f918b60e917ec92be648fb5816d247f72b1` | PHP citations are to this tree. |
| pristine `mozart` | `bf468830` | Not exercised (twin uses mozart-sim; see §8). |
| pristine `kube-manifests` | (repos-root) | Used only for "which FTS workers are deployed" (§2). |
| twin | branch `milestone-3-1-claude-final` | `TWIN_SPEC/*`, `reports/implementation/*`, `ENV2_COMPOSE/{config,generated,seeds,substitutes,verifier,docker-compose.yml}`. |

---

## Finding 1 — FTS transfer creation for a Direct (current-account) merchant

### 1.1 The claim "service.go:549-552 marks DIRECT" is a metrics label, not the routing decision

`fts internal/transfer/service.go:551-556` reads `transferPayLoad[preferred_source_account_id]` only to pick the label `account.TypeDirect` vs `account.TypePool` for `metrics.TransfersCreatedCount`. It does not select a source account. (`TWIN_SPEC/route-matrix.yaml` `merchant_class_selectors.fts` and `monolith-stub/server.py:407-408` both cite this line as the selector; see §10.)

### 1.2 The real selector chain

1. `transfer_processor.go:763-766` — `getSourceAccountAndMode()` (routing v2) or legacy `getSourceAccount()`.
2. Both branch on `!utils.IsEmpty(processor.transfer.PreferredSourceAccountID)` (`:964`, `:1025`) → `getDirectSourceAccount()` (`:1535-1560`) / `getDirectSourceAccountAndMode()` (`:1468-1524`).
3. `accountService.FetchSourceAccounts(ctx, product, transfer.PreferredSourceAccountID)` (`:1479-1480`, `:1543-1544`) → `internal/account/service.go:5314-5330`:
   ```go
   conditions[AttributeFundAccountID] = fundAccountID      // :5320
   conditions[common.AttributeProduct] = prod.String()      // :5321
   ```
   **`transfers.preferred_source_account_id` is matched against `source_accounts.fund_account_id`, not `source_accounts.id`.** The twin seeds both as 900002 so the distinction is invisible there.
4. `SelectDirectSourceAccount` (`:5334-5349`): one row → use it (`:5343-5345`); several rows → `selectDirectSourceAccountByRoutingRules` (`:5356-5382`): merchant rule `direct_account_routing_rules{merchant_id,product,mode,channel}` whose `source_account_id == source_accounts.id` (`:5385-5410`), else default rule (`merchant_id NULL`) matched on `mozart_identifier` (`:5413-5448`), else `sourceAccounts[0]` (oldest id, `:5322` orders by id).
5. Mode for Direct: `getModeForPreferredSourceAccountID` (`:1440-1465`) returns `transfer.preferred_mode` unless allowed-modes contain IFT.
6. `source_account_mappings` / `account_type_mappings` / `preferred_routing_weights` are **not consulted** on the Direct branch (they belong to `merchantSourceAccountConfig()` `:1190-1215` and pool routing). `channel_information_status` is consulted for channel health (seed comment `seeds/s4/fts.sql:109-111`).

### 1.3 Attempt/queue behaviour keyed on `source_accounts.account_type`

`internal/account/source_account.go:94-95` — `TypeDirect = "direct"`, `TypePool = "pool"` (lowercase); model validation `:183` requires `In(TypeDirect, TypePool)`. Comparisons are **case-sensitive `==`**: `attempt_processor.go:4053-4054` (initiate queue scope), `:4340-4341` (check-status queue scope), `:1142` (RBL egress token bucket), `:1186` (reselect-SA exclusion); `service.go:1399` `switch sourceAccount.AccountType { case account.TypePool ... }` for `status_details`. `RegisterDirectAccounts` (`:5451-5530`) → `createNewDirectSourceAccount` (`:5552-5575`) writes `account_type = "direct"`, `bank_account_type = "CURRENT"` (`:5554-5555`). Only mapping/health tables use uppercase `POOL`/`DIRECT` (`internal/channel/health.go:99-100`, `providers/mozart/validation.go:16-17`, migration 00011 column comment).

### 1.4 What must exist in FTS DB for a fresh Direct merchant

Production onboarding path (`RegisterDirectAccounts`, `:5477-5517`): one `fund_accounts` row (over a `bank_accounts` row carrying the merchant's own current-account number/IFSC), then **one `source_accounts` row per Mozart identifier in `OnboardDirectSourceAccountsList[RBL] = {v1, v2}`** (`source_account.go:135-138`), each with `channel=RBL`, `account_type='direct'`, `bank_account_type='CURRENT'`, `product`, `fund_account_id`, `mozart_identifier`, `credentials` JSON containing `auth_username, auth_password, client_id, client_secret, corp_id` (`:159-161`), `configuration` JSON `{"beneficiary_required": false}` (`:141-149`). Then `account/service.go:678-680` enqueues `ledger_account_create` → `CreateBulkLedgerAccounts` (`:4751-4797`) → Ledger `CreateOnEvent` `current_pool_account_onboarding` keyed by `fts_fund_account_id`.
Minimum for the twin (single source account): `bank_accounts`, `fund_accounts`, one `source_accounts` row (lowercase `direct`), `direct_account_routing_rules` per mode optional when only one SA exists, `channel_information_status` UP rows for `(RBL, mode, v1, API)`. Payouts side: `banking_accounts.fts_fund_account_id` = **that `source_accounts.fund_account_id`** (payouts `pkg/fts/transfer_init_create.go:277-299`, `processor/fund_transfer_service.go:34-45`), `account_type='direct'`, `channel='rbl'` (must be in `DirectChannelsForConnectedBanking` = `[YESBANK, AXIS, ICICI, RBL, IDFC, SLICE]`, `appConstants/accounts.go:33`).

### 1.5 Is 900002 real?

**No — twin invention.** `grep -rn '\b90000[12]\b'` over pristine FTS (`*.go,*.sql,*.toml,*.yaml,*.json`) returns nothing. Origin: `ENV2_COMPOSE/seeds/s4/fts.sql:6-11,50-56` ("explicit fixed ids in the 900001+ block, well clear of default auto-increment start at 1"); per-namespace copies are `9000x1/9000x2` via `seeds/generator/generate.py:277-283` (`tr('900002' if i == 2 else '900001')`), e.g. retained run `runs/final-acceptance/route/route-direct_success.jsonl` shows `preferred_source_account_id: 900282 = source_account_primary_id = source_account_id`. `seeds/mysql/fts_seed.sql` is a stale, self-declared unverified placeholder (`sa_ARENA_M1`, non-existent `routing_rules` table) and is not the seed that runs.

### FTS tables / columns (from `internal/migrations`)

| Table (migration) | Key columns (type) | Constraints / notes |
|---|---|---|
| `transfers` (00005:14-63) | `id int(11) AI`; `merchant_id char(14)`; `source_type varchar(50)`; `source_id varchar(14)`; `preferred_mode enum(IMPS,NEFT,RTGS,IFT,UPI,CT,DUITNOW,IBG)`; `preferred_channel enum(AXIS,CITI,HDFC,ICICI,RBL,YESBANK,M2P,MCS,OCBC)`; `preferred_source_account_id int(11) NULL` ("if provided then source account selection is ignored"); `fund_account_id int(11)`; `channel`; `source_account_id int(11)` (= fund_account_id of the SA used); `source_account_primary_id int(11) unsigned` (= source_accounts.id); `amount bigint unsigned`; `mode`; `status enum(CREATED,INITIATED,PROCESSED,FAILED,REVERSED,RETRY,USER_PENDING) DEFAULT CREATED`; `utr`,`return_utr`,`bank_status_code varchar(100)`,`failure_reason`,`gateway_error_code`,`is_credited`,`is_debited`,`credited_at`,`extra_info TEXT`,`request_meta JSON` | `UNIQUE unique_source(source_type, source_id)` :62 — one transfer per payout id; `source_id` is the 14-char bare payout id. |
| `attempts` (00006:14-60) | `id int AI`; `transfer_id`; `channel`; `source_account_id int NOT NULL`; `source_account_primary_id`; `amount`; `fund_account_id`; `bank_status_code varchar(100)`; `mode`; `status enum(CREATED,INITIATED,PROCESSED,FAILED,REVERSED,RETRY,FAILED_INTERNAL)`; `utr`,`return_utr`,`gateway_ref_no varchar(50)`,`cms_ref_no`,`acknowledged_at`,`transfer_response TEXT`,`status_response TEXT`,`re_initiate_count` | `UNIQUE gateway_ref_no_unique(gateway_ref_no)` :51. |
| `source_accounts` (00007:14-29) | `id int(11) unsigned AI`; `product enum(PAYOUT,…)`; `channel enum(...RBL...) NOT NULL`; `fund_account_id int(11) NOT NULL`; `mozart_identifier varchar(40) NOT NULL`; `bank_account_type enum(NODAL,CURRENT,CORP_CARD)`; `credentials TEXT` (JSON); `configuration TEXT` (JSON); `account_type varchar(255)` | Go expects `account_type` ∈ {`direct`,`pool`,`rx_wallet`} lowercase (`source_account.go:94-96,183`). `mozart_identifier` = version segment of the Mozart URL (`v1`/`v2`/`v3`/`v5`, `:125-128`). |
| `source_account_mappings` (00011:14-41) | `operation enum(TRANSFER,BENEFICIARY)`; `merchant_id char(14) NULL`; `product`; `channel`; `mozart_identifier`; `source_account_id int` (= source_accounts.id); `source_account_type` (NODAL/CURRENT); `account_type` (POOL/DIRECT); `mode enum(...)`; `credentials TEXT`; `priority`; `routing_enabled tinyint`; `integration_type enum(API,FILE)` | Pool/merchant routing only; not read on the Direct branch. |
| `account_type_mappings` (00018:14-29) | `mode enum`; `product enum`; `account_type enum(CURRENT,NODAL)`; `merchant_id char(14) NOT NULL` | Not read on the Direct branch. |
| `direct_account_routing_rules` (00020:13-31) | `merchant_id char(14) NULL`; `product varchar(50)`; `channel varchar(50)`; `mozart_identifier varchar(50)`; `source_account_id int` (= source_accounts.id); `mode varchar(50)` | Channel validated ∈ {ICICI,RBL,YESBANK} (`direct_account_routing_rule.go:53-56`). |
| `multi_account_routing_rules` (00042) | `preferred_source_account_id int NULL` "Preferred SA ID for the channel"; `source_account_type` POOL/DIRECT; `type` DEFAULT/MERCHANT | MAR flow only. |
| `transfer_meta` (00045:14-28) | `transfer_id`; `request_id varchar(255)`; `origin_service varchar(255)`; `source_id varchar(255)` | Written only when Splitz `CreateTransferMetaRollout` is on (`service.go:399-418`); `origin_service` = `X-Origin` header or `"API"` (`service.go:1889-1911`; header captured `routing/middleware/request_context.go:18,34`). |

---

## Finding 2 — Bank adapter for RBL Direct (workers, Mozart contract, schedule)

**Task → worker → queue.** Initiate task name for RBL: `attempt_processor.go:4042-4099` — if `mozart_identifier ∈ {v1,v2}` **and** `account_type == "direct"` → `rbl::<ident>::direct::<mode>::initiate_transfer` (`:4097`; hv variants `:4065,:4071`), else `rbl::<ident>::<mode>::initiate_transfer` (`:4099`). Check-status: `:4323-4342` → `rbl::<ident>::direct::<mode>::check_transfer_status`. `task_worker_map` regexes map these to worker commands (`generated/fts/env.default.toml:401-418`: `"rbl::(v1|v2)::direct::imps::initiate_transfer" = "rbl::direct::imps::initiate_transfer"`, `:408` check-status, `:410/:416` for `(ift|neft|rtgs)` → `rbl::direct::{check_transfer_status,initiate_transfer}`), and `worker_queue_map` (`:138-151`) to Redis queues `rbl_direct_imps_initiate_transfer` etc. Layers merge (BurntSushi `toml.DecodeFile` into the same struct, `internal/config/helper.go:39`; maps merge, so the arena's 14-entry `[queue.task_worker_map]` at `env.arena.toml:87-93` does not erase the default regexes).
Compose workers: `docker-compose.yml:1386` `fts-worker-rbl-direct-imps-initiate-transfer` (`-command=rbl::direct::imps::initiate_transfer`), `:1409` `…-check-transfer-status`, plus `rbl::imps::*` (`:1340,:1363`), `rbl::initiate_transfer`/`rbl::check_transfer_status` (`:1294,:1317`), `fire_transfer_status_webhook` (`:1202`), `check_transfer_status` (`:1179`), `default` (`:1133`). **NEFT/RTGS Direct** tasks (`rbl::v1::direct::neft::*`) map to `rbl::direct::initiate_transfer` / `rbl::direct::check_transfer_status`, which are **not** in compose → Direct NEFT/RTGS would sit unconsumed unless the default worker picks them (unverified).
Deployed in prod (kube-manifests): `fts-live-worker-rbl-direct-initiate-transfer` (`rbl::direct::initiate_transfer`), `…-rbl-direct-imps-check-transfer-status`, `…-rbl-direct-imps-hv-initiate-transfer`, `…-rbl-direct-upi-check-transfer-status`, `…-rbl-direct-check-transfer-status`; concurrency `env.prod-live.toml:95-98,105-106`.

**Mozart endpoints.** URL = `[mozart].BASE_URL + "/" + MOZART_URLPART_GATEWAY + endpoint.URL` (`providers/mozart/provider.go:185-221`); RBL v1 IMPS: `MOZART_URLPART_GATEWAY = "rbl/v1"` (`env.default.toml:958-959`), endpoints `/transfer_init`, `/transfer_status` (`:792-801`, timeout 180 s each). Twin `BASE_URL = "http://mozart-sim:8085/fts"` (`env.arena.toml:224-225`) → `POST /fts/rbl/v1/transfer_init|transfer_status`. Request body: `entities.attempt{id, amount (rupee string, request.go:134-135), gateway_ref_no, mode, merchant_id, source_id…}`, `entities.source_account{channel, mozart_identifier, credentials (merged with merchant credentials, gateway/service.go:290-335)}`, `entities.fund_account.bank_account{account_number…}`. **There is no distinct "direct" Mozart gateway for RBL: Direct vs Pool differ only by which `source_accounts` row (credentials = the merchant's own RBL corp/client ids) is sent.** Response envelope `{data{bank_status_code, utr, return_utr, is_credited, is_debited, gateway_ref_no}, error{gateway_error_code, internal_error_code…}, success}`; FTS derives `meta{processed,failed,reversed,pending}` from `bank_status_code` via `providers/mozart/error_code.go` (`RETURNED` → failed/BBANK `:438-441`; `INITIATED`,`PENDING` → pending `:442-449`; `INSUFFICIENT_FUND` `:35`, `INVALID_ACCOUNT_NUMBER` `:10`, `DUPLICATE_TXN` `:26`, `CBS:188` `:127`, `MOZART_INDETERMINATE` `:144`).
**Check-status schedule** (`doStatusCheck` `:2433-2501` → `getDelayForStatusCheck` `:7055-7083` → `queue.GetBackoffTime(modeConfig.OperationRetry, count)`): RBL v1 IMPS backoff `env.default.toml:965-985` = 2×20 s, 4×60 s, 6×900 s, 16×3600 s, 10×10800 s, 6×21600 s, 3×86400 s. Manual `POST /v1/transfer/{id}/check` only for an `INITIATED` attempt (`service.go:1031-1036`; ILLEGAL_STATE otherwise).
**mozart-sim vs real contract.** mozart-sim implements scenarios `success|failure|insufficient_funds|hold|delayed_success|returned|ambiguous_with_utr|ambiguous_without_utr|duplicate|timeout` keyed by `attempt.id|source_id|merchant_id` (`server.py:28,53-96`) plus amount/`gateway_ref_no`-keyed fixtures (`:146-251`). It ignores credentials and account identity, so **Direct vs Pool is indistinguishable at the bank boundary**; `returned` only appears on a second `transfer_status` poll (`:78-84`), which FTS never performs automatically after PROCESSED.

---

## Finding 3 — Status-return routes into payouts (all transports)

Decision block: `fts service.go:1137-1144` Kafka (exclusive, returns), else `:1147-1188` direct-PS iff Splitz `CreateTransferMetaRollout` on **and** `transfer_meta.origin_service == "payouts"` **and** `[payouts_service.update_fts_fund_transfer]` non-empty, else legacy product webhook `:1181-1187`/`:1201-1207`. Retry `:1254-1268`: non-2xx → re-enqueue every `WebhookRetryInterval` (200 s) up to `WebhookRetryMaxCount` (3), then only a metric. Skipped while transfer is `RETRY` (`:1104-1111`). Body = `GetMapFromTransfer` (`:1306-1356`): `fund_transfer_id, source_id, source_type, status (externalised), utr, return_utr, bank_status_code, failure_reason, remarks, narration, mode, channel, gateway_ref_no, gateway_error_code, bank_processed_time, extra_info, status_details`; **`source_account_id` (= `transfers.source_account_id`, i.e. the SA's fund_account_id) and `bank_account_type` only when status ∈ {PROCESSED,FAILED,REVERSED,USER_PENDING}** (`:1337-1346`); `USER_PENDING` is externalised as `INITIATED` (`:164-169,:1363-1371`).

### (i) FTS → payouts direct webhook
- Route: `POST /v1/payouts/transfer_status_webhook` (`payouts internal/routing/router/payout_internal_routes.go:12-17,153-158`); BasicAuth any of `cred.API, Workflow, Xperience, FTS, VendorPayments, Settlements, Irctc`. FTS identity `[payouts_service.auth]` (`env.prod-live.toml:253`; twin `env.default.toml:834-836` → env `PAYOUTS_SERVICE_AUTH_KEY=fts`, `generate.py:329-331`).
- Config: `[payouts_service.update_fts_fund_transfer]` (prod `env.prod-live.toml:269-270` `https://payouts.razorpay.com/v1/payouts/transfer_status_webhook`, TIMEOUT 300; twin `generated/fts/env.arena.toml:262-266` injected unconditionally by `config/routes.py:60-63`). Selector: Splitz `create_transfer_meta_rollout` (`ROyjRWsBowWdWY` prod `:380`; twin `arena_fts_meta`, on only for profiles `direct`, `routes.py:74-81`) and `X-Origin: payouts` (payouts `pkg/fts/base.go:95` per ROUTE_SOURCE_EVIDENCE; monolith create_fta sends none → `origin_service=API`).
- DTO `dtos/transfer_status_webhook_request.go:7-27` (`fund_transfer_id` int64 required, `status` required, `source_id`/`source_type` pair, `source_account_id` int64, `bank_account_type`).
- Handler `fts_transfer_status_webhook.go:31-181`: Redis mutex `ResourcePayout:<id>` 30 s (`:53`); lower-cases status/channel (`:86-87`); guard `AllowedStateTransitionForTransferWebhook` (`:122-131`, map `appConstants/states.go:37-54`: initiated→{initiated,failed,processed,reversed}, processed→{processed,reversed}, reversed→reversed, failed→failed; anything else → HTTP 200 "webhook update skipped"); details update (`:258-370`: sets `fts_transfer_id` once `core.go:1975-1977`, utr, channel change **rejected for Direct** `:340-354`, allowed for Shared `core.go:2000-2003`); then status (`:372-429`): `GetPayoutStatusFromFtsStatusMapping` (`:1300-1337`, map §Status-mapping), Direct-only failed→reversed upgrade if XAS shows debit+credit (`:431-499`), terminal-repeat no-op (`:388-394`), dispatch processed/reversed/failed (`:503-652`) → `HandlePayoutStatusUpdateViaFundTransferService` (`core.go:1586-1696`) → `HandlePayoutProcessedViaFundTransferService` (`:720`), `…Failed…` (`:876-912`: refuses reversed/failed/processed → and refuses **Shared initiated→failed** `:907-912`), `…Reversed…` (`:1036-1120`, creates reversal via `ReverseMerchantPayoutViaFundTransferServiceReversedWebhook`). `VerifyPayoutFailedTransaction` (`:654-708`): **error if `payout.transaction_id` set** (`:658-665`); Direct only: error if a debit statement exists in XAS (`:685-708`). Correlation: `source_id` (bare 14-char payout id) + `fund_transfer_id`. CREATED/INITIATED = no-op (`:409-417`).
- Twin: REAL code; exercised only under `ARENA_ROUTE_PROFILE=direct` (ROUTE_COVERAGE "Direct2 capture"); default profile `monolith` leaves it dormant. Verifier stimulus `helpers/payouts_flow.py:191-206,308-312` (`inject_transfer_webhook` posts real transfer ids). Prod: deployed config exists; per-merchant Splitz rollout **UNKNOWN**.

### (ii) FTS → monolith → payouts (legacy product webhook)
- FTS: `[webhook.payout.transfer_status]` `POST https://prod-api-int.razorpay.com/v1/update_fts_fund_transfer` (`env.prod-live.toml:164-166`), auth `productConfig.Auth`. Monolith route `update_fts_fund_transfer` → `FundTransferAttemptController@updateSource` (`api/app/Http/Route.php:4021`).
- `FundTransfer/Attempt/Core.php:487-559 updateFundTransfer`: lower-cases status; finds FTA by `fts_transfer_id`, else by `(source_id, source_type, is_fts=1)` (`:503-522`, `Repository.php:339-346`) — **requires a monolith FTA row, which exists only when the monolith created the transfer**; guard `AttemptStatus::isValidStateTransition` (`Status.php:30-36`: same matrix as PS plus CREATED→…); `updateFtaWithInput` (`:930-980`: utr, mode, bank_processed_time, status, failure_reason, bank_status_code, remarks, gateway_ref_no, channel(lower), cms_ref_no); save; `updateSourceEntityByFta` (`:770-817`) builds `ftaData` incl. `source_account_id`, `bank_account_type`, `fts_transfer_id` and calls `sourceReconByFta` (`:835-875`) → `Payout\Core::updateWithDetailsBeforeFtaRecon` then `updateStatusAfterFtaRecon` (`Payout/Core.php:933-1058`): status via `Status::getPayoutStatusFromFtaStatus` (`Status.php:316-323`, map `:243-309`), Direct failed→reversed XAS check (`:939`), terminal-repeat guard (`:941-944`), PROCESSED → `handlePayoutProcessed` (`:1014`), REVERSED → `handlePayoutReversed` (`:1037`), FAILED → `handlePayoutFailed` (`:1043`), CREATED/INITIATED no-op (`:1050-1052`). For PS payouts these call `Services/PayoutService/Details.php:12,17-35` (`POST /payouts/update_payouts_details_with_fts`, body = FTA fields + `source_id`) and `Status.php:14,19-61` (`PATCH /payouts/update_payouts_with_fts`, body `{source_id, status, failure_reason, bank_status_code, fts_fund_account_id (=source_account_id as string), fts_account_type (=bank_account_type), fts_status}`).
- Payouts handlers: `HandlePayoutDetailsUpdateViaFTS` (`core.go:1926-2046`, mutex, Shared FAILED → status-detail REVERSED `:2010-2012`) and `HandlePayoutStatusUpdateViaFTS` (`core.go:1465-1577`, mutex `:1479`, **no transition-matrix guard** — relies on per-handler guards `HandlePayoutFailed :2396-2427`, `HandlePayoutProcessed :2448-2470`, `HandlePayoutReversed :2344`; `HandlePayoutInitiated :1895-1924`). Auth: same group (cred.API…). Correlation: `source_id`; `fund_transfer_id` only on the details call.
- Twin: SUBSTITUTE `monolith-stub/server.py:561-675` (no FTA row; status map `:600-615` Direct FAILED→failed / else reversed; details POST `:637-658`; INITIATED/CREATED → details only `:659-660`; single PATCH `:663-675`; `PS_RELAY_MODE=drop`, per-payout hold/delay/duplicate controls `:567-585`). This is the default profile (`env.arena.toml:171-175`). Prod: deployed (webhook URL in prod-live config); it is the golden route in retained runs.

### (iii) FTS → Kafka → payouts consumers
- Producer: `[kafka_producers.fire_transfer_status]` topic `rx-fts-status-update-events`, key = `source_id` (`service.go:1140-1142`); gate Splitz `fire_status_update_kafka` (`PYGTRQEzO39PfB`, `env.prod-live.toml:364`; producer enabled `:315-319`). Message = the flat `GetMapFromTransfer` map (no `fts_*` keys).
- Consumers (payouts `config/prod.toml:312-342`, pristine): `rx-payouts-fts-status-update-consumer-group` on `rx-fts-status-update-events`; retry group on `rx-fts-status-update-retry-events` (`:362-363`). Handler `taskHandlers/fts_status_updates.go:42-54` unmarshals into `StatusUpdateRequest`+`DetailsUpdateRequest` (`dtos/payoutUpdate.go:8-16,34-50`) — `fts_fund_account_id/fts_account_type/fts_status` stay empty; **FAILED and REVERSED return nil = ack+drop** (`:56-62`; retry `:58-64`); RecordNotFound swallowed (`:68-70`); other errors → `HandleFailedMessage` republish to retry topic (`:92-113`). Auth: consumer-group only (no HTTP identity).
- Monolith also consumes the same topic (`api/app/Services/Kafka/Consumers/FtsStatusUpdateConsumer/Consumer.php:67-109` → `FtaService::updateFundTransferAttempt`, retry topic `config/kafka_consumer.php:8`) — compensates FAILED/REVERSED **only for monolith-created FTAs** (DEV-150).
- Twin: broker + both consumers REAL (`docker-compose.yml:875-923`, restart on-failure per FD-001); producer enabled only under profile `kafka` (`routes.py:64-69`; default `enabled=false`, `env.arena.toml:155-158`). EF-001/002/003 reproduced (`m1-20260905T165255Z/profile-kafka`). Prod: consumers and producer DEPLOYED; merchant rollout NOT DETERMINABLE (expected-failures.yaml EF-001 `deployed_status`).

### (iv) Polling / source-updater
- FTS: automatic status polling only while the attempt is `INITIATED` (schedule §2); after PROCESSED, returns are discovered only via admin `POST /v1/attempts/verify` + `PATCH /v1/attempts/safe_update` (RETURNED_ROUTE_EVIDENCE; `service.go:891,897`, `manual_update_handlers_factory.go:84`), or `PUT /v1/attempts/reconcile`. Each attempt update re-fires the transfer webhook (`attempt_processor.go:2988-2994`, inside `UpdateAttemptWithResponse :2958`).
- Payouts `payout_source_updater` job (`internal/job/source_updater.go:12-21`) has a **no-op `Handle` (`:52-54` returns nil)**; it is not a status route. FTS `[payouts_service.retry_source_update]` → `POST /v1/payouts/retry/source_update` (`env.prod-live.toml:265`; route `payout_internal_routes.go:63-67`) is an FTS-initiated retry of payout source updates, not a status transport.

---

## Finding 4 — FTS status model

`state_machine.go:8-16` states `NEW, CREATED, INITIATED, PROCESSED, FAILED, REVERSED, RETRY, USER_PENDING` (attempt adds `FAILED_INTERNAL`). Transfer transitions `:82-118`: CREATED→INITIATED (also from RETRY, USER_PENDING); INITIATED→PROCESSED; {INITIATED,CREATED,RETRY,USER_PENDING}→FAILED; **PROCESSED→REVERSED**; {FAILED,INITIATED}→RETRY; INITIATED→USER_PENDING. Attempt `:38-79`: same core plus CREATED→FAILED_INTERNAL.
Emitters: bank init response (`transfer_init`) → attempt INITIATED/FAILED (meta from `error_code.go`); `transfer_status` polls → PROCESSED/FAILED; RETURNED on a later status read → classified failed/BBANK, applied only via safe manual update PROCESSED→REVERSED (`return_utr`, `bank_status_code=RETURNED`, `meta.reversed=true`).
**Webhook cadence: FTS fires the product/PS webhook (or Kafka message) on every attempt update, not only terminal** — `attempt_processor.go:2988-2994` (after every `UpdateAttemptWithResponse`, incl. INITIATED), `:4984-5003` (delayed re-fire for status_details after `SET_STATUS_DETAILS_AFTER` = 300 s, `env.default.toml:9004`), `transfer_processor.go:902-906,942-946` (internal failures), `service.go:2781,3089` (bulk/manual). Evidence: KAFKA_ROUTE_EVIDENCE INITIATED then PROCESSED messages 60 ms apart. Reversal after PROCESSED for Direct = transfer `REVERSED` + `return_utr` + `bank_status_code=RETURNED`; payouts maps `reversed` (Direct RBL) → `HandlePayoutReversedViaFundTransferService` creates a `reversals` row; Direct writes no Ledger journal (`core.go:2575-2577`; route_scenarios `returned` asserts a journal only because its fixture is Shared M1).

## Finding 5 — Pending / timeout

FTS: in-progress = transfer `INITIATED` + attempt `INITIATED` with `bank_status_code` ∈ {INITIATED, PENDING, TECHNICAL_ERROR_AMBIGUOUS, DUPLICATE_TXN, MOZART_INDETERMINATE…} (`bank_scenarios.py:24-34` lists observed codes); non-terminal webhook carries `status_details{reason, parameters.processed_by_time}` computed in `setStatusDetailsInTransferMap` (`service.go:1373-1400`; note the `TypePool`/`TypeDirect` switch is case-sensitive — see §10). Resolution: backoff polls (§2) until `completed` (`:2484-2487`), 5-month eligibility (`:3009`), per-mode SLA `PROCESSING_TIME_SLA_DIRECT = 86400` (`env.default.toml:9003`); Mozart HTTP timeout 180 s (`:794,:800`); gateway 504 → `MOZART_INDETERMINATE` pending.
Payouts: pending bank result = payout `initiated` (`appConstants/states.go:14`); `pending`/`on_hold`/`queued` are pre-dispatch states, not bank-pending. INITIATED webhooks only update details/status_details (`fts_transfer_status_webhook.go:409-417`, `core.go:2015-2016`).

## Finding 6 — FTS ↔ Ledger / XAS (Direct)

- FTS → Ledger: only `CreateOnEvent` account onboarding (`account/service.go:678-680` → task `ledger_account_create` → `CreateBulkLedgerAccounts :4751-4797`, event `current_pool_account_onboarding` keyed `fts_fund_account_id`; prod URL `env.prod-live.toml:310`; twin `[ledger].BASE_URL=http://ledger-api:8080` `env.arena.toml:128`). **FTS writes no journals and nothing statement-related.** Twin seeds the Ledger pair directly (`seeds/s4/ledger.sql:116-124`).
- FTS → XAS: read-only `GET /v1/account_statements/fetch_multiple_by_reference_numbers` from the retry pre-processor (`providers/xas/fetch_by_multiple_references.go:13`; `retry_preprocessor_service.go:310-332`). FTS publishes no transfer event consumed by x-account-statements.
- Payouts → XAS (Direct only): `PushXasSourceEventAndSyncEs` (`core.go:1765-1810`, `:1779` account_type Direct, Splitz-gated) enqueues an account-statement source event after each FTS-driven status change; payouts also reads XAS in the Direct failed→reversed/verify checks (§3 i). Twin: `xas-sink` substitute.

## Finding 7 — Health / on-hold (Direct)

FTS `downtime/notification.go:435-519`: payload `PartnerHealthEvent{type, payload{mode, channel, begin, account_type, status, source, instrument, include_merchants, exclude_merchants}}` (`:56-71`) sent to the API (`[webhook.payout.channel_status]`, prod `/v1/fts/channel/notify` `env.prod-live.toml:214`) and, iff `Feature.PSDowntimeNotificationOnHold` (`:479`; twin `env.default.toml:5571 = true`), to `[payouts_service.notify_channel_status]` (prod `https://payouts.razorpay.com/v1/notify/health/update` `:254-255`); retry `PartnerBankWebhookRetryCount` (`:500-516`). Payouts route `POST /v1/notify/health/update` BasicAuth(cred.FTS) (`internal_routes.go:11-23`) → `UpdateHealthStatusForSource` HSET `partner_bank_health[<account_type>_<channel>_<mode>]` (`partner_bank_health_manager.go:24-48,126-129`). Direct create checks `IsChannelDown{channel, account_type:"direct", mode}` (`processor/fundAccountPayoutDirect.go:570-610` → `on_hold`, `queued_reason=gateway_degraded`); release/cancel via `queued/partner_bank_hold_payouts.go:41-85` (cron `process_queued_payouts`). Twin: payouts side REAL; **FTS side effectively MISSING** — `notify_channel_status.URL` is the default placeholder `http://localhost:8080/` (`env.default.toml:837-841`) with no arena override, and nothing in mozart-sim/FTS admin flips channel health (`route_list.go:187-199` admin routes exist but no driver). route-matrix `health_source` is source-correct.

## Finding 8 — Twin substitutes vs real contracts

| Substitute | Implements | Deviation from real |
|---|---|---|
| mozart-sim (`server.py`) | `/{ns}/{gateway}/{version}/{transfer_init,transfer_status,…}`; scenario control `POST /_arena/scenario {key|merchant_id|payout_id, scenario, polls}` keyed on `attempt.id|source_id|merchant_id` (`:53-60`); grounded fixtures by amount/`gateway_ref_no`; `GET /_arena/scenarios` events. | Ignores credentials/account identity (Direct ≡ Pool); `returned` only on the 2nd status poll; no RBL-specific `bank_status_code` vocabulary beyond 8 codes; any Basic auth accepted. |
| monolith-stub `update_fts_fund_transfer` (`:561-675`) | Details POST then status PATCH with `fts_fund_account_id/fts_account_type(lower)/fts_status`; Direct FAILED→failed; invalid-transition 200; drop/hold/delay/duplicate controls. | No FTA persistence; no `isValidStateTransition` on FTA state (uses payout state); no Direct failed→reversed XAS upgrade; no monolith Kafka consumer (DEV-150). |
| monolith-stub `create_fta` (`:380-423`) | Sends `preferred_source_account_id = fixture.fts_source_account_id` iff account_type direct (`:406-409`). | Uses `fts_source_account_id` semantically (real monolith sends the banking account's `fts_fund_account_id`); no `X-Origin`; misses Shared sub-merchant case. |
| FTS seed (`seeds/s4/fts.sql`) | bank_accounts/fund_accounts/source_accounts 900001/900002, mappings, account_type_mappings, weights, direct rules, channel health. | `source_accounts.account_type='DIRECT'/'POOL'` uppercase (§10); one SA per fund account (prod RBL onboarding creates v1+v2). |
| verifier | `finish_bank` (`payouts_flow.py:332-341`) = scenario + real `/v1/transfer/{id}/check`; `inject_transfer_webhook` (`:308-312`) synthetic direct webhook with real ids; `admin_return` (`kafka_scenarios.py:173-200`, `route_scenarios.py:141-182`). | Direct return case uses admin reconciliation, never automatic detection. |

## Finding 9 — Route matrix for Direct

| # | Route | Request / event | Actor identity | Transport | Src → Dst | Payload schema | Status mapping (Direct/RBL) | Correlation | Twin support |
|---|---|---|---|---|---|---|---|---|---|
| A | PS → FTS create (PS-direct) | `POST /v1/transfer` | payouts `[fts.auth]` + `X-Origin: payouts` | HTTP | payouts-worker-fts-async-processing → fts-web | `transfer{source_id, source_type, preferred_mode, preferred_channel, preferred_source_account_id (= banking_accounts.fts_fund_account_id), amount, narration, request_meta}`, `account{fund_account_id}` | n/a | `source_id` ↔ `transfers.source_id` (UNIQUE) | native (profile direct/kafka) |
| B | PS → monolith → FTS create | `POST /v1/payouts_service/create_fta/{id}` then `POST /v1/transfer` | monolith `users.api` | HTTP | payouts → monolith-stub → fts-web | as A, no X-Origin | n/a | same | substitute (default profile) |
| C | FTS → bank | `POST /fts/rbl/v1/transfer_init`, `/transfer_status` | source_accounts.credentials | HTTP (Mozart) | fts-worker-rbl-(direct-)imps-* → mozart-sim | Mozart envelope | bank code → meta (`error_code.go`) | `attempts.gateway_ref_no` (UNIQUE) | substitute |
| D | FTS → PS direct webhook | `POST /v1/payouts/transfer_status_webhook` | FTS `[payouts_service.auth]` = payouts `cred.FTS` | HTTP, 3×200 s retry | fts-worker-fire-transfer-status-webhook → payouts-api | `TransferStatusWebhookForPayoutRequest` | initiated→initiated, processed→processed, failed→**failed**, reversed→reversed (`states.go:84-91`) | `source_id`, `fund_transfer_id` | native, profile `direct` only |
| E | FTS → monolith → PS | `POST /v1/update_fts_fund_transfer` → `POST …/update_payouts_details_with_fts` + `PATCH …/update_payouts_with_fts` | FTS `webhook.payout` auth; monolith `cred.API` | HTTP | fts → monolith-stub → payouts-api | FTS map → `DetailsUpdateRequest` + `StatusUpdateRequest{fts_fund_account_id, fts_account_type, fts_status}` | `Status.php:273-279` Direct/RBL failed→failed | `source_id` (+`fund_transfer_id` on details) | substitute (default) |
| F | FTS → Kafka → PS | `rx-fts-status-update-events` (retry `…-retry-events`) | consumer group | Kafka | fts-worker-fire-transfer-status-webhook → kafka → payouts-kafka-fts-status-updates-consumer | flat FTS map (no `fts_*` keys) | processed→processed; **failed/reversed dropped** (EF-002/003) | key = `source_id` | native, profile `kafka`; expected-failure |
| G | Admin return | `POST /v1/attempts/verify`, `PATCH /v1/attempts/safe_update` | FTS `users.api`/ART (twin `api_monolith`) | HTTP | verifier → fts-web → (D/E/F) | keyed by attempt id, `meta.reversed` | PROCESSED→REVERSED → payout reversed | attempt id | native |
| H | Health / on-hold | FTS `POST /v1/notify/health/update` | FTS `cred.FTS` | HTTP | fts-web → payouts-api Redis `partner_bank_health` | `HealthStatusUpdateRequest` | `direct_rbl_imps` key → `on_hold` | key `<acct>_<channel>_<mode>` | payouts native; FTS emitter missing |
| I | PS → XAS source event | job `x_account_statement_source_event` | — | worker/queue | payouts → xas-sink | `XasEventDetails` | Direct terminal only | payout id / utr | substitute sink |

### Status-mapping tables

FTS external status (`service.go:164-169`): `USER_PENDING`→`INITIATED`; all others verbatim.

Payouts direct webhook & Kafka (`appConstants/states.go:65-128`, lookup `fts_transfer_status_webhook.go:1300-1337` by `banking_accounts.account_type` then `payouts.channel`, fallback default):

| FTS status | default / shared | direct + {rbl, icici, axis, yesbank, idfc, slice} | direct + other channel |
|---|---|---|---|
| created | created | created | created (fallback) |
| initiated | initiated | initiated | initiated |
| processed | processed | processed | processed |
| failed | **reversed** | **failed** | reversed (fallback) |
| reversed | reversed | reversed | reversed |

Monolith relay (`api/app/Models/Payout/Status.php:243-309`, lookup `:316-323` `[balance.account_type][channel][fta]` ?? `[DEFAULT][DEFAULT]`): identical shape; DIRECT has explicit RBL/ICICI/AXIS/YESBANK/IDFC rows, empty DEFAULT (non-listed Direct channel → FAILED→REVERSED). Guards: PS `AllowedStateTransitionForTransferWebhook` (`states.go:37-54`); monolith `Attempt\Status::ALLOWED_STATE_TRANSITION` (`Status.php:30-36`); PS terminal repeat → 200 no-op; PS failed refused when `transaction_id` set (`:658-665`) or Shared+initiated (`:907-912`); Direct failed upgraded to reversed if XAS debit+credit found (`:431-499`).

### Twin deviation table

| Item | Source truth | Twin | Effect |
|---|---|---|---|
| `source_accounts.account_type` casing | `"direct"/"pool"` lowercase (`source_account.go:94-95,183`) | `'DIRECT'/'POOL'` (`seeds/s4/fts.sql:54-55`, rendered `seeds/generated/s4/fts.sql:21-22`) | Direct branch still routes (lookup is by fund_account_id, `:5320`), but `== account.TypeDirect` checks fail: attempt tasks named `rbl::v1::imps::*` (pool workers/queues), egress token bucket off, `status_details` switch misses, reselect-SA not excluded. Runtime queue evidence not retained (unresolved). |
| Direct source account count | prod RBL onboarding creates v1 **and** v2 SAs per merchant (`:135-138`) | one SA (900002/v1) | Default-rule selection by mozart_identifier never exercised. |
| NEFT/RTGS Direct workers | `rbl::direct::initiate_transfer`, `rbl::direct::check_transfer_status` deployed | absent from compose (`docker-compose.yml:1294-1426`) | Direct NEFT tasks map to unconsumed worker names unless default worker drains them (unverified). |
| FTS → PS health notify | `notify_channel_status` → payouts | `http://localhost:8080/` placeholder (`env.default.toml:837-841`) | Route H emitter missing. |
| Monolith Kafka consumer | consumes same topic, relays for monolith-created FTAs | none (DEV-150) | Kafka profile = worst case. |
| mozart-sim identity | credentials/account per SA | ignored | Cannot detect wrong-SA routing at bank boundary. |
| FTS webhook timeout | 300 s | 300 s (DEV-006 fixed) | — |

## Finding 10 — Contradictions with earlier docs

1. `TWIN_SPEC/route-matrix.yaml` `merchant_class_selectors.fts` ("preferred_source_account_id present => DIRECT (service.go:549-552)") and `monolith-stub/server.py:407-408` cite the metrics label; the selector is `transfer_processor.go:964/1025` and the id resolves against `source_accounts.fund_account_id` (`account/service.go:5320`).
2. `route-matrix.yaml` `ps_direct_route.twin`: "not exercised (fts arena config lacks `[payouts_service.update_fts_fund_transfer]`)" — stale: `config/routes.py:60-63` injects it for every profile (`generated/fts/env.arena.toml:262-266`) and ROUTE_COVERAGE records a real direct callback under profile `direct`. What gates it is the `arena_fts_meta` Splitz variant, off in the default `monolith` profile.
3. `ENV2_COMPOSE/seeds/mysql/fts_seed.sql` (named in the task brief) is not the live seed; `seeds/s4/fts.sql` is.
4. `route-matrix.yaml` lists "payout-source-updater" among polling routes; its job handler is a no-op (`job/source_updater.go:52-54`).
5. Seed casing vs Go constants (above) — `seeds/s4/fts.sql:119-125` shows the author hedged with both casings for `channel_information_status` but not for `source_accounts`.
6. Confirmed (no contradiction): EF-001..003 mechanisms and line numbers (`core.go:2472-2475,2575-2577,2589-2595`; `fts_status_updates.go:60-62`); RETURNED classification `error_code.go:438-441`; `fts_to_payout_status_map` `states.go:65-128`; state-machines.yaml `fts_transfer` transitions; health_source claim.

## Unresolved

- Which FTS worker/queue actually processed M2's attempts in retained runs (no scoped task names in `reports/implementation/runs`); needs a fresh run with worker logs or `SELECT account_type FROM source_accounts` plus Redis queue inspection.
- Production per-merchant rollout of Splitz `fire_status_update_kafka` (PYGTRQEzO39PfB), `create_transfer_meta_rollout` (ROyjRWsBowWdWY) and payouts `fts_request_from_payouts_service` — UNKNOWN (no repository fixture; expected-failures.yaml Q1/Q2). Only "deployed config/consumers exist" is known.
- Whether `fts-worker-default` drains `rbl::direct::{initiate_transfer,check_transfer_status}` (Direct NEFT/RTGS) in the twin.
- The monolith's Kafka consumer group name (custom config empty; base config not read).
- Exact RBL `bank_status_code` vocabulary for real Direct returns (only mozart testdata precedent; not read here).

## Confidence

High: §1 selector chain and fund_account_id semantics, §3 handlers/guards/maps, §4 cadence, 900002 provenance, config values (all read from source). Medium: §2 twin worker mapping consequence of the casing mismatch (code path certain; runtime not observed). Low: prod merchant rollout of any non-legacy transport.

## Handoff instructions

**Provisioner — records to create in FTS for a fresh Direct merchant `M` with current account `ACC`/IFSC:**
1. `bank_accounts` (id `B`, `merchant_id=M`, `account_type='CURRENT'`, `account_number=ACC`, `ifsc_code`, `product='PAYOUT'`).
2. `fund_accounts` (id `F`, `merchant_id=M`, `account_type='BANK_ACCOUNT'`, `account_id=B`, `default_channel='RBL'`).
3. `source_accounts` (id `S`, `product='PAYOUT'`, `channel='RBL'`, `fund_account_id=F`, `mozart_identifier='v1'`, `bank_account_type='CURRENT'`, **`account_type='direct'` lowercase**, `credentials='{"auth_username":..,"auth_password":..,"client_id":..,"client_secret":..,"corp_id":..}'`, `configuration='{"beneficiary_required":false}'`). Optionally a second row with `mozart_identifier='v2'` to mirror prod onboarding.
4. `direct_account_routing_rules` per mode (`merchant_id=M, product='PAYOUT', channel='RBL', mozart_identifier='v1', source_account_id=S, mode`) — required only if >1 SA per `F`.
5. `channel_information_status` UP rows for `(RBL, IMPS|NEFT, 'v1', 'API')` (already seeded globally).
6. Payouts `banking_accounts`: `account_type='direct'`, `channel='rbl'`, `fts_fund_account_id = str(F)` (not `S`). Ledger: FtsReceivable/FtsPayable pair with `fts_fund_account_id=[F]`, `fund_account_type=current` (mirrors `CreateBulkLedgerAccounts`). Do **not** seed `source_account_mappings`/`account_type_mappings` for Direct (unused) unless a Shared fallback is wanted.

**Tests — how to drive each route and what to observe:**
- Route D (direct HTTP): boot `ARENA_ROUTE_PROFILE=direct`; create payout; assert `transfer_meta.origin_service='payouts'`, payouts log `TransferStatusWebhookTransferAttemptEntity`, `payouts.fts_transfer_id`, then bank outcome via mozart-sim `/_arena/scenario {key: attempt.id}` + `POST /v1/transfer/{id}/check`; observe `payouts.status` per the Direct map (failed stays `failed`, no `reversals` row, no Ledger journal). Synthetic stimulus: `inject_transfer_webhook` with real `fund_transfer_id`/`source_account_id=F`/`bank_account_type='CURRENT'`.
- Route E (monolith relay, default profile): observe `monolith-stub /_arena/log` events `update_fts_fund_transfer_inbound` → `update_payouts_details_with_fts` → `payout_status_relay{status:'failed'}`; drop/hold/duplicate via `/_arena/relay` controls.
- Route F (Kafka): `ARENA_ROUTE_PROFILE=kafka`; run `kafka_scenarios.py` cases `failed_dropped_direct`, `reversed_dropped_direct` and expect EF-002/EF-003 (payout unchanged, message acked).
- Return after PROCESSED (any profile): `admin_return` sequence (`/v1/transfer/{id}/check` → 400 ILLEGAL_STATE; `/v1/attempts/verify`; `/v1/attempts/safe_update` with `meta.reversed`) → FTS REVERSED, payouts `reversed` + `reversals` row, no journal for Direct.
- Pending: mozart-sim `hold`/`delayed_success`; observe attempt `INITIATED`+code, payout `initiated`, `payout_status_details` after 300 s delayed webhook; `check` twice for `delayed_success`.
- Queue attribution (to close the casing question): after a Direct payout, `redis-cli LLEN fts_rbl_direct_imps_initiate_transfer` vs `fts_rbl_imps_initiate_transfer`, or grep worker logs for `rbl::v1::direct::imps`.
- Health (Route H): not drivable today; needs an FTS admin `channel_health_events` driver or a direct `POST /v1/notify/health/update` with `cred.FTS` and `payload{account_type:'direct', channel:'rbl', mode:'IMPS', status:'downtime'}` then a Direct create → `on_hold`/`gateway_degraded`.

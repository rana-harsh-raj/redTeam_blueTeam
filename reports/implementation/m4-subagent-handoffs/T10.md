# T10 — BAS Ingestion Engineer (Milestone 4, live arena `env2_compose`)

Task ID: **T10**. Written 2026-09-06/07 (UTC evening 2026-09-06). Branch `milestone-4-direct-reconciliation`, nothing committed by me.

Outcome: the REAL payouts worker `rbl_banking_account_statement` (accepted binary, unchanged) now runs in the arena and ingests
RBL current-account statements end to end; the REAL ART route links them; the REAL `VerifyPayoutFailedTransaction` guard was
exercised. Proof `RED_LOOP/surface/m4_bas_ingest.py`: **7/7 steps (a)–(g) PASS** on run
`RED_LOOP/runs/m4-bas-ingest-20260906T194947Z/` (summary `reports/implementation/m4-bas-ingest.json`).

Labels: **real** = accepted service binary + real route; **real-worker/substitute-bank** = REAL payouts worker fed by
mozart-sim (`account_statement`) + bankingaccounts-stub (credentials); **substitute** = twin-only.

---

## 1. Inspected files (line refs)

Accepted payouts source (`.local/twin-repos/accepted/payouts`, HEAD 4d78f86):
- `internal/app/bankingAccountStatement/processor/rbl_gateway.go` :35-95 (fetch → local dedupe → DB dedupe → save), :97-166 (attempt loop, `HasMoreTxns`, `UpdateStatementFetchedTill(paginationKeyTs-1d)`), :168-231 (`FetchPage`: NoRecords ⇒ nil; with `next_key` ⇒ advance 4 weeks, clear key, recurse), :264-293 (`GetRequestDataForMozart`: creds via banking-accounts, `attempt.id = unix ts`, `transaction_type "B"`), :324-353 (`ModifyRequest`: date window from `statement_fetched_till_date`, 365 d cap, or `next_key`), :355-411 (`ParseBankResponse`: 60 s posted-date guard, **`records[len(records)-1]` unconditional index ⇒ panic on an empty successful file**), :413-460 (`UnmarshalBankResponse`: `Status=Failure && Status_Desc=="No Records Found"` before `Validate()`; header line must equal `RblAccStmtHeaderLine`; split on `\n`), :462-515 (9-cell row parse, rupees→paise, category map), :517-662 (DB dedupe tuple `{account_number, posted_date, type, bank_serial_number, amount, bank_transaction_id, channel}` via `FetchByTxnIds(txnIds, accNo, created_at >= min txn date)`), :664-703 (UTR/GRN regexes).
- `processor/rbl_request.go` (RblRequest/attempt/source_account/credentials), `processor/rbl_statement.go` (response structs + validators: `File_Data` required, `Status oneof Success Failure`), `processor/rbl_constants.go` (header line, category map, regexes, `RblTxnTsOffset=60`), `processor/base.go` :55-97 (`SaveTransactions`: per-row create + BASD update of pagination_key/closing balance/metadata).
- `internal/app/bankingAccountStatement/core.go` :235-287 (`FetchAccountStatement`: mutex, ACTIVE BASD by (account_number, channel), `last_statement_attempt_at`, processor factory), :289-319 (row create), :321-432 (`ProcessPostRecon`), :434-486 (`SendUpdatesToPayoutSource` → `GetBasReconPayoutUpdateRequest`), :627-663 (`NotifyLedgerOnLinking`: external ⇒ `da_ext_debit|credit`), :673-733 (`ExtractEntityInfoFromStatement` = third CSV field, 14 chars; `linkBASToEntity`), :741-800 (`createLedgerJournal` → SNS `api-ledger-journal-create-live`); `service.go:57` (base64 decode of `data`); `repo.go:54-74`; `model.go` (columns, `Sign="bas_"`).
- `internal/app/bankingAccountStatementDetails/core.go` :26-35 (defaults 6/95/3600/direct), :153-174 (DTO defaults), :210-249 (`CheckIfAccountNumberSatisfiesSelectionCriteria`), :251-273 (dispatch); `repo.go:26-62`; `model.go:100-150` (`GetStatementFetchedTillDate` "" when metadata missing, `SetStatementClosingBalance` stamps `_change_at`).
- `internal/app/dtos/bankingAccountStatementFetch.go`, `dtos/bankingAccountStatementProcess.go`, `dtos/basdtos/bas_linking_data.go`, `dtos/transfer_status_webhook_request.go`.
- `internal/app/payouts/core.go` :1768-1880 (`PushXasSourceEventAndSyncEs`, `isXasSourceEventExperimentEnabled`), :7203-7300 (`UpdatePayoutAfterBASRecon`); `internal/app/payouts/fts_transfer_status_webhook.go` :540-720 (`UpdateFailedStatusWithTransferStatusWebhook` → `VerifyPayoutFailedTransaction` :654-720 — **only on the FTS-direct route `/v1/payouts/transfer_status_webhook`**, `payout_internal_routes.go:155`).
- `internal/config/config.go:260-292` (Job struct: `rbl_banking_account_statement`, `x_account_statement_source_event`), `internal/config/merchant_list_config.go:21-52` (`IsEnabled()` = `EnabledEnv=="true"`; empty whitelist ⇒ all), `internal/job/x_account_statement_source_event.go:78-100`, `pkg/worker/manager.go:204-221`, `pkg/mozart/client.go` (`{"entities": input}` body, path `/{ns}/{gw}/{ver}/{action}`, 60 s account_statement timeout), `pkg/mozart/response.go`, `pkg/bankingAccountService/fetch_banking_creds.go` (route + `data{id,corp_id,user_id,urn,credentials}`), `pkg/sns/sns.go:39-86` (aws-sdk-go-v2, ARN from `[sns].aws_account_id`, no endpoint override), `pkg/api/bas_recon_payout_update.go:13-24,60-61`, `internal/trace/*.go` (message constants used in assertions).
- ledger `internal/job/job_sqs/journal_create.go:205-220` (SNS-envelope tolerant constructor) — for the (e) note only.

Twin: `ENV2_COMPOSE/config/generate.py` (whole), `config/routes.py`, `config/arena.yaml:66-74`, `config/templates/base/payouts/arena.toml` ([job] :399-423, [auth] :114-155, [api] :155, [mozart] :252, [banking_account_service] :278, [configs] :614-660, [xas] :682-705, [sns] :531), `config/templates/base/fts/env.arena.toml:141-148`, `scripts/up.sh` (steps 1-3, materialize at :45), `secrets/materialize.py` (whole; refuses in-use volumes :141), `seeds/localstack/init-queues.sh`, `scripts/cron-driver/driver.py`, `substitutes/_common/base_stub.py`, `substitutes/mozart-sim/{server.py,CONTRACT.md}`, `substitutes/bankingaccounts-stub/{server.py,CONTRACT.md}`, `substitutes/monolith-stub/server.py` (read-only: relay control :679-730, `_banking_statement_payout_update` :1026-1040, ROUTES), `docker-compose.yml` (payouts worker blocks :759-815, cron-driver :1947, mozart-sim :1891, bankingaccounts-stub :1755, anchors :24-75), `RED_LOOP/red_loop/{provisioner.py,provisioner_direct.py,config.py}`, `RED_LOOP/surface/m4_direct_provision.py`, `reports/implementation/m4-subagent-handoffs/T03.md` (Findings 1, 2, 3, 4(c)(d), 5, 8, 9, §10), `T07.md` (bankingaccounts-stub note), `m4-decisions.md` D-006/D-007.

## 2. Changes (file → purpose)

| File | Change |
|---|---|
| `ENV2_COMPOSE/config/templates/base/payouts/arena.toml` | `[job].rbl_banking_account_statement = "rbl_banking_account_statement"`, `[job].x_account_statement_source_event = "x_account_statement_source_event"`; `[configs.account_statement_source_event]` → literal `enabled_env="true"`, `whitelist_env="{{XAS_SOURCE_EVENT_WHITELIST}}"`, `blacklist_env=""` (no `env|` resolver exists in the accepted payouts tree — grep is empty — and the arena sets no `PAYOUTS_CONFIGS_*` env; literal values are what `MerchantListConfig.IsEnabled()` compares); `[xas].host = "http://xas-sim:8080"`. |
| `ENV2_COMPOSE/config/templates/base/fts/env.arena.toml` | `[xas].host = "http://xas-sim:8080"` (was xas-sink). |
| `ENV2_COMPOSE/config/generate.py` | token `XAS_SOURCE_EVENT_WHITELIST` from host env `ARENA_XAS_SOURCE_EVENT_WHITELIST` (default `""` = every Direct merchant; the producer additionally gates on `account_type=direct` + terminal status + utr/grn/cms present). **A fresh merchant needs no config change**; set the env only to RESTRICT. |
| `ENV2_COMPOSE/seeds/localstack/init-queues.sh` | queues `rbl_banking_account_statement`, `x_account_statement_source_event`. |
| `ENV2_COMPOSE/docker-compose.yml` | new service `payouts-worker-rbl-banking-account-statement` (profile core, `PAYOUTS_WORKER_NAME=rbl_banking_account_statement`, mirrors the other worker blocks). Nothing else touched. |
| `ENV2_COMPOSE/scripts/cron-driver/driver.py` | job `bas_fetch_initiate` → `POST /v1/cron/banking_account_statement/fetch/initiate {"channel":"rbl","account_rate_limit":50,"inactive_duration_limit":315360000,"account_type":"direct"}` every 300 s (`CRON_INTERVAL_BAS_FETCH_INITIATE`, `CRON_BAS_*` env); logs `dispatched_accounts`; on demand `docker exec env2_compose-cron-driver-1 python3 /app/driver.py --once bas_fetch_initiate`. |
| `ENV2_COMPOSE/substitutes/mozart-sim/server.py` (+ `CONTRACT.md`) | action `account_statement` (`POST /razorpayx/rbl/v2/account_statement` — path verified in `pkg/mozart/client.go` + `appConstants/request.go:67`): Mozart envelope, `FetchAccStmtRes{Header, AccStmtData.File_Data=base64 CSV}`, exact header line, no trailing newline, per-account queue via `POST/GET /_arena/statement` (rows; options `no_records`, `failure`, `http_error`, `next_key`, `page_size` paging, `duplicate`, `sticky`; `clear`); empty queue ⇒ "No Records Found"; never Success-with-zero-rows (would panic the real parser). |
| `ENV2_COMPOSE/substitutes/bankingaccounts-stub/server.py` (+ `CONTRACT.md`) | `GET /merchant/{mid}/banking_account_by_account_number/{acct}/credentials` → `data{id, corp_id, user_id, urn, credentials{5 keys}}` (synthetic); `merchants.json` re-read on mtime change (fixes the T07 restart requirement) + `POST /_arena/reload`. |
| `RED_LOOP/red_loop/bas_fixtures.py` (new, stdlib) | `ensure_basd_row`, `make_basd_selectable`, `read_basd`, `statement_row`, `enqueue_statement`/`set_statement_options`/`clear_statement`/`inspect_statement`, `trigger_fetch` (http via FastCron cred, or `via="exec"`), `read_bas_rows`, `wait_for_bas_rows`, `dedup_key`, `wait_for_worker_message`, `art_entry`, `art_process_batch` (cred.API basic, body per T03 4(c)), `get_payout_row`, `get_transaction_link`, `monolith_relay_log`, `ledger_journal_count`, `fts_transfer`, `docker_logs`/`log_messages`. |
| `RED_LOOP/surface/m4_bas_ingest.py` (new) | proof (a)–(g); merchant via `--descriptor`, `--campaign` (T09 `provision_direct_merchant` + self-check), or `--fixture-m2` (additive BASD, dev only; NOT used for the recorded evidence). |
| `ENV2_COMPOSE/config/declared-deviations.yaml` | DEV-167 (mozart account_statement substitute), DEV-168 (credentials stub), DEV-169 (BASD by provisioner + selection inputs synthetic), DEV-170 (cron-driver trigger/cadence), DEV-171 (source-event gate literal + PS→SNS ledger publish blackholed). `_miniyaml` parses (88 entries). |

## 3. Container operations performed (all `env2_compose`, UTC 2026-09-06)

| When | Operation | Why |
|---|---|---|
| 19:12 | `config/generate.py` + `preflight/preflight.py` (PASSED) + in-place re-materialization of ONLY volumes `rzp-arena-config-payouts` and `rzp-arena-config-fts` using `secrets/materialize.py`'s own INSTALL/VERIFY container scripts (script kept in scratchpad, not in repo) | `materialize.py` refuses volumes held by running containers; a full arena stop was not acceptable with T09/T11 live. Services read TOML at boot only. `docker exec payouts-api grep` confirmed the new keys in `/app/config/arena.toml`. |
| 19:12 | `awslocal sqs create-queue rbl_banking_account_statement`, `x_account_statement_source_event` | live queues (init-queues.sh covers fresh boots). |
| 19:13 | `docker compose build mozart-sim bankingaccounts-stub` | new routes. |
| 19:15:03 | `up -d --no-deps mozart-sim bankingaccounts-stub cron-driver` (cron-driver was unchanged → separately `restart cron-driver` 19:15:50); `up -d --no-deps payouts-worker-rbl-banking-account-statement`; `restart payouts-api payouts-worker-fts-async-processing payouts-worker-fts-async-hv-processing payouts-kafka-fts-status-updates-consumer payouts-kafka-fts-status-updates-retry-consumer` | new config: `[job]` keys are read by the dispatcher (payouts-api) and the source-event producer (api + FTS status consumers). mozart-sim recreate dropped its in-memory scenario table (T09 sets scenarios per test). |
| 19:20 | `provision_direct_merchant("m4bas-t10-1")` via T09's module (restarts monolith-stub, bankingaccounts-stub, dcs-stub, splitz-stub, kong-lite — T09's own recipe) | fresh Direct merchant **ARENAD81988839** (balance ARENADB1988839, account 2323000081988839, FTS 998839), self-check 29/29. |

NOT restarted (carry the pre-change config until the next boot, harmless): the other 11 payouts workers, all fts containers (the fts `[xas].host` change only matters once xas-sim is wired), ledger, cfa, x-balances. Container count 66 → 67 (+ xas-sim by T11 = 68 now). No `compose down`, no volume deletion, `REGEN_SECRETS` unset.

Additive data written (flag): BAS row `TYrcyLj4IdW0ks` (utr ARENASMOKE0001) + BASD metadata/gateway_balance fix-up on T09's merchant **ARENAD84708817** during the fixture smoke test; all proof rows on my own merchant ARENAD81988839 (18 BAS rows across 6 runs, payouts, reversals none). Fixture merchants ARENAM00000001/2/3 untouched (the `--fixture-m2` path exists but was never executed).

## 4. Commands + results

```
python3 ENV2_COMPOSE/config/generate.py            # OK, 6 service configs; diff vs previous output = exactly the 4 template hunks
python3 ENV2_COMPOSE/preflight/preflight.py         # PREFLIGHT PASSED
docker exec env2_compose-localstack-1 sh -c "awslocal sqs create-queue --queue-name rbl_banking_account_statement; ..."   # QueueUrl returned
docker compose ... up -d --no-deps payouts-worker-rbl-banking-account-statement   # healthy in 23 s; log "starting worker"
docker exec env2_compose-cron-driver-1 python3 /app/driver.py --once bas_fetch_initiate
   -> /v1/cron/banking_account_statement/fetch/initiate -> 200 {"dispatched_accounts":[...11 accounts...]}
docker logs env2_compose-payouts-worker-rbl-banking-account-statement-1 | grep -o '"message":"[^"]*"' | sort | uniq -c
   -> processing job rbl_banking_account_statement / BANKING_ACCOUNT_STATEMENT_FETCH_REQUEST / BANKING_ACCOUNT_SERVICE_REQUEST+RESPONSE /
      MOZART_SERVICE_REQUEST+RESPONSE / ..._FETCH_NO_RECORDS / JOB_PROCESSED_SUCCESSFULLY   (queue polled, real handler ran)
python3 RED_LOOP/surface/m4_bas_ingest.py --campaign m4bas-t10-1                         # first run 4/7 (see §7 for the two substitute gaps found)
python3 RED_LOOP/surface/m4_bas_ingest.py --descriptor RED_LOOP/runs/m4-bas-ingest-20260906T192052Z/merchant-descriptor.json
   -> runs ...193603Z 7/7, ...194017Z 5/7 (payouts-api restarted by another agent at 19:47:18 mid-run: webhook status 0, cron 0 dispatch),
      ...194947Z 7/7  (FINAL, reports/implementation/m4-bas-ingest.json)
```

### Per-step evidence (final run 194947Z, merchant ARENAD81988839)

| Step | Fidelity | Result | Key observations |
|---|---|---|---|
| (a) Direct payout via kong, mozart `success` | real | PASS | payout `TYs9dPJtyRBWWG` processed, `payouts.utr = ARENA0000000269` = FTS `transfers.utr` (transfer 269); ledger journals for merchant 0; REAL producer logged `X_ACCOUNT_STATEMENT_SOURCE_EVENT_PAYLOAD` + `..._QUEUE_PUSH_SUCCESS` (queue drained by T11's xas-sim, 0 visible). |
| (b) matching debit row → cron → worker → BAS row | real-worker/substitute-bank | PASS | `trigger` 200 with the account in `dispatched_accounts`; one row `TYs9jgd22DMOfn` {utr ARENA0000000269, amount 5000, type debit, merchant/account correct, bank_transaction_id M4B557E86FE, category customer_initiated, bank_serial_number 1, balance 9935615, entity_type NULL}; BASD `pagination_key 1788723531_ → 1788724072_`, `statement_closing_balance 9940615 → 9935615`, `metadata.statement_fetched_till_date = posted_date−1 day` (first-ever fetch on a fresh BASD advanced 31-08-2026 → 06-09-2026, run 192052Z). |
| (c) replay identical CSV row | real-worker/substitute-bank | PASS | rows 16 → 16; worker `BANKING_ACCOUNT_STATEMENT_FETCH_DEDUPE_CHECK_DUPLICATES_FOUND` (`rbl_gateway.go:617`, skipped_record_count 1); dedup key `{account_number 2323000081988839, posted_date 1788724072, type debit, bank_serial_number "1", amount 5000, bank_transaction_id M4B557E86FE, channel rbl}`. |
| (d) ART `Reconciled` | real | PASS | `POST /v1/banking_account_statement/process/batch` (cred.API) → `{InputCount 1, ProcessedBASCount 1}`; BAS `entity_type=payout, entity_id=<pid>`; REAL PS → monolith-stub `POST /v1/banking_account_statement/payout_update` **200** (`BAS_UPDATE_PAYOUT_REQUEST_SUCCESS`) → relay → PS `UPDATE_PAYOUT_AFTER_BAS_RECON_REQUEST` + `MAIL_AND_SMS_VIA_API_REQUEST`; `payouts.transaction_id == bas id`; monolith-stub `/_arena/log` kind `banking_statement_payout_update` {request 6 fields, status 200}; ledger journals 0 and no `CREATE_LEDGER_JOURNAL*` line (PS posts nothing for Direct payouts — commented code, asserted). |
| (e) ART `Unreconciled` on unmatched debit (777 paise) | real | PASS | BAS `TYs9tgyJj7J0HS` → `external`; PS built and tried to publish `LedgerJournalCreateRequest{transactor_event da_ext_debit, transactor_id bas_…, identifiers {banking_account_stmt_detail_id: basd_ARENADS1988839}, notes {balance_id, transaction_id}, tenant X, mode live}` (`CREATE_LEDGER_JOURNAL_ASYNC_REQUEST`) then `CREATE_LEDGER_JOURNAL_ASYNC_FAILED`: `operation error SNS: Publish, exceeded maximum number of attempts, 3 ... Post "https://sns.ap-south-1.amazonaws.com/": proxyconnect tcp: dial tcp 127.0.0.1:9: connection refused` → `BANKING_ACCOUNT_STATEMENT_LEDGER_CREATE_FAILURE`; link still committed (`..._LINK_SUCCESS`). Ledger never sees it (journal count 0), so `ErrAccountDiscoveryFailure` was NOT reachable — the failure is one hop earlier (DEV-171). |
| (f) statement-first ordering | real (guard) + synthetic caller | PASS | payout `TYsA2iGr3xG5tu` held `initiated` (mozart `hold`), statement row + ART link delivered first → `transaction_id = TYsA6w9NST6lOh` via the REAL PS→monolith→PS hops; monolith relay held (`/_arena/relay-control hold`); bank resolved failed (attempt scenario `failure` + FTS `/v1/transfer/{id}/check`) → FTS `FAILED`; payout still `initiated`; then the FTS body posted to the REAL FTS-direct route `POST /v1/payouts/transfer_status_webhook` (auth `[auth.fts]`) → **HTTP 500** `{"error":{"code":"server_error",...}}`, log `ATTEMPT_TO_MOVE_PAYOUT_TO_TERMINAL_STATE_HAVING_TRANSACTION` (transaction_id, payout_id, url) — status stays `initiated`, transaction_id kept. **Then** relay released → monolith legacy relay → PS `PAYOUT_STATUS_CHANGED_TO_FAILED`: the payout moved to `failed` despite `transaction_id` (see §7 finding F-T10-1). |
| (g) `no_records` / `failure` | real-worker/substitute-bank | PASS | mozart-sim request log outcomes `no_records` / `failure` for the account; worker `BANKING_ACCOUNT_STATEMENT_FETCH_NO_RECORDS` (no fields — correlated via mozart-sim) / `BANKING_ACCOUNT_STATEMENT_FETCH_RETRIES_EXHAUSTED` (`RblAccountStatementInvalid`, retry limit 0), `JOB_PROCESSED_SUCCESSFULLY`, rows unchanged (18), worker `running healthy`. |

Evidence paths: `RED_LOOP/runs/m4-bas-ingest-20260906T194947Z/{m4-bas-ingest.json, merchant-descriptor.json}` (descriptor holds the merchant secret; git-ignored dir), earlier runs `…192052Z` (fresh provisioning + first-fetch metadata advance, 4/7 pre-fix), `…193603Z` (7/7), `reports/implementation/m4-bas-ingest.json` (no secrets).

## 5. Deviations (declared in `config/declared-deviations.yaml`)

DEV-167 mozart account_statement substitute · DEV-168 credentials stub · DEV-169 BASD by provisioner, synthetic selection inputs (`gateway_balance` held ≠ closing, `inactive_duration_limit` 10 y) · DEV-170 cron-driver trigger/cadence · DEV-171 source-event gate literal + PS→SNS ledger publish blackholed.

## 6. Instructions for a fresh empty-volume boot

Automatic via `scripts/up.sh`: secrets kept/generated → `config/generate.py` renders the new `[job]`/`[configs]`/`[xas]` keys (honour `ARENA_XAS_SOURCE_EVENT_WHITELIST` if set) → `materialize.py` → LocalStack `init-queues.sh` creates both queues → `--profile core` starts `payouts-worker-rbl-banking-account-statement` → cron-driver ticks `bas_fetch_initiate` every 300 s (dispatches nothing until BASD rows exist). mozart-sim/bankingaccounts-stub images carry the new routes (compose `build:`).

The provisioner (T09) must, per Direct merchant: insert the BASD row **with** `metadata = {"statement_fetched_till_date": "<DD-MM-YYYY, recent>"}` and `gateway_balance != statement_closing_balance` (or call `bas_fixtures.ensure_basd_row(descriptor)` which repairs an existing T09 row in place: today's T09 insert leaves `metadata NULL` ⇒ the worker asks the bank for 01-01-1970..1971 in 5 paged attempts, and equal balances ⇒ the account is only dispatched by the `others` remainder rule); keep `merchants.json` current (bankingaccounts-stub now re-reads it, no restart needed). Tests: `bas_fixtures.enqueue_statement` → `trigger_fetch()` → `wait_for_bas_rows` → `art_process_batch`. xas-sim compose wiring (coordinator/T11) is the only remaining piece for `[xas].host` to resolve.

## 7. Findings / unresolved

- **F-T10-1 (source behaviour, real)**: `VerifyPayoutFailedTransaction` exists only in `HandleTransferStatusWebhookForPayout` (FTS-direct route). The arena's `route_profile=monolith` reports FTS status through monolith-stub's legacy relay to PS, which moved a payout with `transaction_id` set to `failed` (step f after release). Whether the production monolith relay path (`api Payout/Core.php` FTA update for PS payouts) has an equivalent statement-first guard is UNKNOWN from T03; candidate contradiction for `m4-contradictions.md`. The twin can exercise the guard only on the direct route (as done) or with `direct_status` profiles.
- **Substitute gap found and fixed by T11 during my run**: monolith-stub registered only `/payouts/banking_account_statement/payout_update`, but REAL PS posts `{api.host}/v1/banking_account_statement/payout_update` → 404 `BAS_UPDATE_PAYOUT_REQUEST_FAILURE invalid_status_code` (runs 192052Z/192905Z; my script carried a flagged "bridge" for that hop, no longer needed since monolith-stub 19:32 rebuild; the bridge code path stays dormant and self-labels if ever used).
- PS external-statement ledger leg (`da_ext_*`) cannot reach the REAL ledger: `pkg/sns` has no endpoint override; fix would be `AWS_ENDPOINT_URL_SNS=http://localstack:4566` on payouts-api (+ `[sns].aws_account_id=000000000000`, topic `api-ledger-journal-create-live` subscribed to `journal_create` — ledger's constructor tolerates the SNS envelope). payouts-api compose env is outside T10 ownership → coordinator decision.
- `PAYOUT_XAS_SOURCE_EVENT_FAILED` is logged for **failed** Direct payouts (no utr/grn/cms ⇒ `ErrorPayoutUtrNotFoundForAccountStatementEvent`) — real source behaviour, relevant to T11's expectations.
- Worker log `BANKING_ACCOUNT_STATEMENT_FETCH_NO_RECORDS` carries no fields; per-account correlation needs mozart-sim's request log.
- Materializing config into a live arena required bypassing `materialize.py`'s in-use guard (scratchpad script); if this recurs, a `--groups` option on `materialize.py` (or documenting the stop/start sequence) would be cleaner.
- The `[configs.account_statement_source_event]` literal rendering means production's `env|PAYOUTS_CONFIGS_*` placeholders are not exercised (DEV-171).

## 8. Confidence

High for (a)–(d), (g): real worker, real routes, real DB rows, log evidence per step. High for (f)'s guard (real route + real error class), medium for its production relevance (F-T10-1). Medium for (e): the PS side is fully observed; the ledger side is unreachable by design of the arena network. Concurrency caveat: T09/T11 restarted shared containers during two of six runs; only the clean runs are cited.

# EVIDENCE_INDEX delta — E44+ (2026-09-04 access-expansion pass)

New commits this pass: api `2d665f918b60e917ec92be648fb5816d247f72b1`, edge `da9ff5b22b7d0e4dbecfba5d3305ff8469c60de0`, terraform-kong `8ffe965bce7868d150344433d3bfbf3e630e228b`, authz `8687f48f0b760b6bab6c711133079d4b534d391b`, workflows `080d71a51b777c89af586c92ac4b5f683a473a7c`, batch `3839dd77d061422c144b7409af401415d598c54b`, stork `800b719030aa5051c76630fa7c2020aa2a1f8348`, mozart `bf4688300d4896500fb9e9f422ee28d80d81b6e6`, dcs `fca59e3e68305683b94b8042ab64e2b9845ecd29`, splitz `fa6f4c7ee13db2b1111887fea69b497e58ecc0e5`, governor `875eabaf2d5f6be5b7fcffcdb2047cfc8e28050c`, recon `6974fec90f78712af77aea3ce8e3858836fd16fe`, payments-upi `b23ec7ea9126561500ae34791c4ef8efe55e6f14`, shield-sdk `1a4cbf7691734a781034295c6bcb21877d263fff`, kube-manifests `9226a892fd767b0e31dd4282b0b4919298f53fda`, alert-rules `c372beabedf833a2a2c7aec862a4f78ce19e5d00`, spinacode `da46a5239144c61049c96729b76813b59f6a349c`, knowledge-base `70f78efa8b269398df392998b0b7f7c63c968607`, goutils `efc794188bbd12318602763b8e8cc321e1793309`. Raw lane reports: `findings/20..27` (plus `22b`) in the session scratchpad.

| # | Claim | Repo | File / symbol (line) | Slack / doc | Confidence |
|---|---|---|---|---|---|
| E44 | Two independent proxy mechanisms for `/v1/payouts`: Splitz-gated "direct" bypass (`X-Payouts-Service-Proxy: 0`, no monolith business logic) vs classic business-logic-first path (`X-Payouts-Service-Proxy: 1`) | api | `PayoutController.php:126-217`; `DirectToPayoutsServiceGate.php:81-124`; `Service.php:561-8202`; `Create.php:47-217` | — | confirmed |
| E45 | Monolith `payouts` table is schema-present and actively read/written (approve/reject sync+async, `ApprovedPayoutProcessor`, BAS-recon fallback, manual batch status update, IRCTC-Axis flow); coexists with a physically distinct PS database reached via `Connection::PAYOUT_SERVICE_DATABASE` | api | `Service.php:1179,1832,1919,5030`; `Core.php:10286-10318,8158`; `app/Jobs/ApprovedPayoutProcessor.php:80`; `Base/Repository.php:1456-1472` | #potential_outages 2026-09-03 | confirmed |
| E46 | Dual-write direction corrected: PS-authoritative row read via `Connection::PAYOUT_SERVICE_DATABASE`, upserted into the monolith's own local `payouts` table (`setConnection` forces local `live`/`test` connection); only a bookkeeping watermark is written into PS's own database | api | `app/Models/Payout/DualWrite/Payout.php:24-71,250,294`; `DualWrite/Base.php:36`; `Core.php:10219-10274,10762-10793` | — | confirmed |
| E47 | FTS-webhook relay `/v1/update_fts_fund_transfer` has **no retry** on the downstream Payouts-Service call; the FTA row is committed to the local DB before the relay attempt; a 180s Redis mutex (gated by Splitz `NON_TERMINAL_MIGRATION_HANDLING`) serializes concurrent classic/PS status mutation during migration | api | `FundTransferAttemptController.php:64-74`; `Attempt/Core.php:487-583,835-875`; `Core.php:160,4636-4653` | — | confirmed |
| E48 | No FAV-freshness gate exists anywhere in the Payout module — exhaustive grep across `Core.php`/`Service.php`/`Validator.php`/`Processor/*.php`, one non-gate hit (an admin bulk tool marking a FAV record failed) | api | `Service.php:5023-5028`; full grep | — | confirmed (absence) |
| E49 | Admin permission enforcement (`AdminAccess::policyChecker`) is local, DB-backed (admin→role→permission in the monolith's own schema); the only "AuthZ" client found (`AuthzEnforcerClient`) lives under `Services/Mock`, not a confirmed production integration | api | `app/Http/Middleware/AdminAccess.php:285,300,307-312,317,322-328`; `app/Services/Mock/AuthzEnforcerClient.php` | — | confirmed (mechanism); unresolved (real remote-AuthZ client existence) |
| E50 | `bas_recon_payout_update` is allowlisted only under the `payouts_service` internal-app credential — **PS, not XAS**, is the caller; a reversal is created only for a local (non-PS) payout currently `FAILED` | api | `app/Http/Route.php:19379-19382`; `Core.php:10286-10328` | — | confirmed |
| E51 | `payouts-proxy-cutover` is instantiated only in `base/payouts-proxy-cutover/` (devstack); no prod/stage/us/sg environment diverts any payout route; `payouts_with_otp` is excluded from the diverted set even in devstack | terraform-kong | `base/payouts-proxy-cutover/config.tf`; `prod/api/api.tf:663-725`; `prod/api-dashboard/templates/merchant-dashboard.tf:2254-2257` | — | confirmed |
| E52 | No `authType` claim exists in the passport JWT payload — `auth_type`/`auth_flow` is Kong-internal request context only, never serialized into the token | edge | `kong-plugins/kong-plugin-upstream-jwt/kong/plugins/upstream-jwt/access.lua:86-205`; `common.lua:500-517` | — | confirmed |
| E53 | No `rate-limiting` plugin is wired to any payouts/contacts/fund_accounts route anywhere across `terraform-kong` and `edge` | terraform-kong, edge | full-repo grep, both repos | — | confirmed (absence) |
| E54 | No loop-guard (`X-PS-Proxied` or equivalent) exists for the direct Edge→Payouts-Service divert design in either repo | terraform-kong, edge | full-repo grep, both repos | — | confirmed (absence) |
| E55 | `authz-enforcer` runs `shadow_mode=true` **and** the public-API payout route is explicitly whitelisted → never blocks in practice on `api.razorpay.com/v1/payouts`; the dashboard-merchant surface (`payouts_with_otp`) runs enforced (`shadow_mode=false`) | edge, terraform-kong | `kong-plugin-authz-enforcer/handler.lua:46-72`; terraform-kong plugin table | — | confirmed |
| E56 | AuthZ's own `config/prod.toml` `watchResourceGroups` **excludes `x_platform`** while stage/bvt/dev-serve include it | authz | `config/prod.toml` vs `config/{stage,bvt,dev-serve}.toml` | — | confirmed |
| E57 | The "CAC"-derived role `payout_approve_reject_view` is a second, independently-evolved policy set from `xplatform.csv`'s `/v1/payouts/{id}/approve`-style resources, using different resource-name strings (`approve_payout` vs `/v1/payouts/{id}/approve`) | authz | `scripts/policies/12_02_2024_rx_xplateform_cac.csv`, `01_03_2024_rx_xplateform_cac_update.csv` | — | confirmed |
| E58 | Workflow Service: Cadence is the real approval engine (every instance is a live Cadence workflow execution); zero Passport/AuthZ calls anywhere in the approve/reject path; approver identity is trusted via string-match of caller-supplied `actor_property_key/value` against the pending state's stored rules — no independent role re-verification | workflows | `internal/client/types/approval/workflow.go:64-160,523-693`; `internal/entities/action/interactor.go:300-410`; `internal/client/types/common/helper.go:29-47` | — | confirmed |
| E59 | Workflow expiry bounded purely by Cadence `ExecutionStartToCloseTimeout` (= config `WorkflowExpireTime` hours, min ~4 months if set, default 10 years); no code path fires any callback on expiry (zero `NewTimer`/`TimedOut`-handling code) | workflows | `internal/entities/workflow/starter.go:62-74,160-192`; `internal/entities/config/validation.go:357`; absence across `internal/client/types/approval/*.go` | — | confirmed (mechanism); probable (real-world consequence: a timed-out workflow's DB row and the payout are left indefinitely unresolved) |
| E60 | `enable_approval_via_oauth` does not exist anywhere in Workflow Service (exhaustive grep); WFS's entire DCS feature-flag surface is exactly two flags (`skip_approval_if_creator_is_checker`, `disable_wf_config_dimensions_for_s2p`) | workflows | `internal/dcs/features/features.go:8-24`; `grep -rniE "otp\|oauth\|2fa\|mfa" internal/ pkg/ cmd/` | — | confirmed (absence) |
| E61 | Workflow Service has zero bulk RPCs (repo-wide grep for "bulk" returns no hits); Batch's `payout_approval` batch type calls Payouts Service `bulk_approve` directly, bypassing WFS entirely | workflows, batch | grep across `workflows/` (whole repo); `batch/src/main/resources/payout_approval.json:1-179`; `PayoutApprovalProcessorImpl.java` | — | confirmed (absence + cross-confirmation) |
| E62 | Batch's classic bulk-payout-create (`payout.json`) derives idempotency key as `"batch_"+BatchEntry.id` with **no Redis mutex**; sibling `v2/payouts_*_bene_*` and Payout Links batch types calling the same PS endpoints use a SHA1-row-hash + `RedisMutexLock(300s TTL)` + 24h idempotency-key cache | batch | `BulkApiCallDataProcessorImpl.java:106,163-166`; `CacheIdempotencyEnabledBulkApiCallDataProcessorImpl.java:104-172`; `BatchEntry.java:29-31,55-58` | — | confirmed |
| E63 | Batch's own `core/AGENTS.md` claim of a "distributed mutex in BatchCore" is stale/false — zero `RedisMutexLock`/`getMutexLock` usage in `BatchCore.java` or `core/`/`batchengine/` outside the unrelated per-row idempotency processor | batch | `src/main/java/com/razorpay/batch/core/AGENTS.md:28` (claim); `grep -rn "RedisMutexLock\|getMutexLock" src/main/java` (absence) | — | confirmed (contradicts doc) |
| E64 | Batch does **not** poll `GET /v1/payouts?batch_id=...` on Payouts Service — the `payouts/bulk` call is synchronous, so Batch already has each row's outcome from the HTTP response; Batch's own caller instead polls Batch's own `GET /batch/{id}` | batch | `BatchController.java:96-107`; `payout.json:157-166`; grep for a `batch_id` polling pattern (absence) | — | confirmed |
| E65 | Stork delivery backoff is exponential (`factor=5, maxTime=86400s`/24h, `maxAttempt=15`; extended per-owner schedule to 72h/23 attempts); **no dedupe** anywhere — `messages.event_id` has no unique constraint, zero `dedup*`/`idempoten*` hits in webhook/channel packages | stork | `internal/config/config.go:883-928`; `configs/default.toml:961-970`; `internal/migrations/00003_create_messages_table.go:15-26` | — | confirmed |
| E66 | Mozart's wire response envelope carries no `meta`/processed/failed field — FTS computes them locally from `bank_status_code` via two Go maps: `defaultErrorCodes` (~140 entries) and `transferErrorCodes` (transfer_init-only overrides) | fts, mozart | `fts/internal/providers/mozart/response.go:3-113`; `mozart` `error_code.go:229-952,217-226` (via fts import) | — | confirmed |
| E67 | Mozart ships a first-class built-in mock mode (`mozart -mock`) running the identical route/auth/config pipeline as prod against canned fixtures, exact-field-match scenario selection; hundreds of pre-existing fixtures under `app/testdata/fts/**` | mozart | `main.go:442-457`; `app/mock/mock.go:120-146`; `app/mock/mappings.go`; `app/mock/controller.go:30-114` | — | confirmed |
| E68 | IDFC has no plain NEFT/IMPS gateway in Mozart's `fts` namespace — only `idfc_upi` v1 (UPI-only) exists | mozart | `app/routes/available_routes.go:2513-2622` (full-file grep) | — | confirmed (absence) |
| E69 | DCS storage: DynamoDB primary single-table (`PK=entity#<type>#<id>`, `SK=kv/<namespace>/...`) + Aurora MySQL secondary (legacy proxy-v1 feature table + proxy-v2 dual-write mirror); `write_via_client`/`read_enabled_via_dcs` per-flag legacy-vs-DCS authority gate; `dual_write_enabled` + Splitz `dcs_kv_dual_write_enabled` govern reverse sync, fail-closed | dcs | `internal/storage/dynamodb/key_builder.go:14-116`; `internal/storage/aurora/aurora.go:12-16`; `internal/proxy/features/service.go:340-352,552-611`; `internal/kv/dualwrite.go:27,120-233,276-343` | — | confirmed |
| E70 | All 8 in-scope Splitz experiment IDs/names (`TUnTsUB8Os2kX0`, `Qnc6b4fg1Hi36k`, `Sg7tEnMoxCbTzM`, `TMp5VcIFYOHK2m`, `PEVE4sUaVG6Drw`, `ORHtoFyuKLWUSw`, `CreateTransferMetaRollout`, `FireStatusUpdateKafka`) have **zero** definitions anywhere in `splitz`/`proto/splitz`/`config-proto` — architecturally DB-only, admin-managed at runtime | splitz | exhaustive grep: `splitz/` (source, migrations, test fixtures, specs), `proto/splitz/`, `config-proto/` | — | confirmed (absence) |
| E71 | Governor is a selection/scoring engine only (govaluate rule chains → boolean `Evaluation`+`Score`, not fee amounts); Payouts configures 5 separate rule chains (basic/product/bank/mode/amount-range); Charge Collections' `Rule` message holds the actual fee numbers | governor, payouts | `governor/app/controllers/governor.go:371-414`; `governor-executor/modals/external.go:18-40`; `payouts/pkg/ccSdk/config.go:22-29`; `proto/charge_collections/pricing/v1/pricing_api.proto:14-49` | — | confirmed |
| E72 | Shield's contract has 3 actions (`allow`/`block`/`review`); Payouts only branches on `"block"` — `review` is treated identically to `allow` (fail-open), confirmed at the call site; 200ms timeout, Hystrix-style circuit breaker | shield-sdk, payouts | `shield-sdk/constants/http.go:13-15`; `payouts/internal/app/payouts/core.go:6883,223`; `payouts/config/default.toml:312-333` | — | confirmed |
| E73 | Recon (ART) has a live, authenticated write path into FTS: `PATCH/POST /v1/attempts/:action`, `PUT /v1/attempts/reconcile`, `ARTAuth` Basic credential, human-gated by a FinOps re-upload of an RX Workflow file (not automatic on every match) | recon, fts | `recon/app/post_recon_service/workflow.py:28,69-120`; `recon/app/workflow_kafka_consumer/processor/reconciliation_workflow.py:1-45`; `fts/internal/routing/router/route_list.go:172-181`; `fts/internal/constants/authusers.go:6` | — | confirmed |
| E74 | RKG's payouts platform bundle (`knowledge-base`, PR #157): 143 nodes / 232 edges, 7 service nodes matching our closure's service set; no factual contradictions against `PAYOUTS_SERVICE_GRAPH.json`; complementary gaps identified in both directions (e.g. `fts→x-account-statements` retry-preprocessor edge missing from ours; reverse `fts→payouts` health-callback edge missing from RKG) | knowledge-base | `knowledge/domains/payouts/bundle.yaml:1-25`; `registry/nodes.yaml`, `registry/edges.yaml` | — | confirmed |
| E75 | UPS VPA mapper (`POST /v1/payments/validate/account`) is called by payouts directly with header `X-Origin: payouts`, which unlocks plaintext VPA in the response specifically for payouts; FTS instead calls the monolith's own `/v1/payment/validate/vpa`, not UPS directly | payments-upi, payouts, fts | `payments-upi/internal/app/validate/processor/response.go:64`; `payouts/pkg/upiService/vpa_mapper.go:18-83`; `fts/internal/gateway/api.go:21-60`; `fts/config/env.prod-live.toml:246-250` | — | confirmed |
| E76 | Payouts `/v1/cron/*` is not a k8s `CronJob` — a Traefik `IngressRoute` + `Middleware` IP-whitelists the external FastCron SaaS; per-endpoint cadence lives only in FastCron's own dashboard, outside every cloned repo | kube-manifests | `templates/payouts/templates/ing-v2.yaml` | — | confirmed |
| E77 | Real k8s `CronJob` resources exist only for ledger/cfa/batch/stork among the 15 profiled services; ledger's two CronJob templates hardcode `suspend: true` in every environment, no in-repo un-suspend mechanism found | kube-manifests | `templates/ledger/templates/` (`verify-warm-storage-cronjob-*`, `split-account-balance-update-cronjob-*`); `templates/{cfa,batch,stork}/templates/` | — | confirmed |
| E78 | `alert-rules` repo is accessible (corrects an initial "unavailable" report within the same pass); FTS webhook-update-failure alerting is aggregate-only (>50 or >10 failures per 15 min by status); stuck-payout alerts are scoped to the IDFC/IMPS channel and the xpayroll source only, no generic per-payout "stuck" alert | alert-rules | `rules/prod-rules/fts_rules.yaml:~2330-2375,4252`; `rules/prod-rules/payouts_rules.yaml:1894-2247` | — | confirmed |
| E79 | Private Go module access confirmed working end-to-end (`go mod download` succeeded for payouts/ledger/fts/cfa/x-balances); payouts' own proto/rpc generation mechanism fully documented: sparse-checkout of `scripts/proto_modules` from `razorpay/proto` + `buf generate` (not a BSR module dependency) | payouts (via ACCESS_DELTA.md, goutils) | `payouts/Makefile`; `payouts/scripts/proto_modules`; `payouts/buf.gen.yaml` | — | confirmed |
| E80 | `goutils/passport` has two live major versions in production use: v3 (static per-`kid` public keys — e.g. payouts' `apiv1`/`edgev1`) and v4 (JWKS-host, fetched once at boot, no periodic refresh); `ImpersonationClaims` has **no JSON tags** in v4 source (upstream bug — the actual wire shape is `{"Type":...,"Consumer":...}`, not `"type"`/`"consumer"`) | goutils, payouts, ledger | `goutils/passport/passport.go`; `goutils/passport/helpers.go`; `payouts/pkg/passport/handler.go`; `ledger/internal/provider/passport/passport.go` | — | confirmed |

## Reconciliations made during this synthesis pass

- Finding 22 (kube-manifests/Spinnaker/alerts) initially reported `alert-rules` as never cloned/inaccessible; the root agent's addendum (finding 22b) corrects this — the repo is accessible and closes UNRESOLVED_QUESTIONS item 20. E78 reflects the corrected state.
- An earlier draft of the monolith dual-write section (finding 20 §8) had two independently-arrived-at, conflicting accounts of write direction; the account in E46 is the one produced by reading `app/Models/Payout/DualWrite/Payout.php` in full and supersedes the conflicting draft.
- Finding 27 clarifies that recon (ART) does **not** call the monolith's `bas_recon_payout_update` endpoint (that is PS's own call, per E50) — recon's write path is a structurally separate mechanism (`v1/attempts/:action`, per E73) against FTS directly. Do not conflate the two "reconciliation can write" claims — they are different callers hitting different endpoints for different purposes.

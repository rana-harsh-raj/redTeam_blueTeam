# T03 — BAS/XAS/Reconciliation and Ledger Direct-accounting Investigator (Milestone 4, Phase 1)

Task ID: **T03**. Mode: READ-ONLY (no docker, no network). Written 2026-09-07.

Path conventions in this report:
- `$R` = pristine clones root (`.local/repos-root` → `/private/tmp/claude-502/-Users-rana-singh/cca012eb-0163-4ad1-8765-4fb6bafddfe7/scratchpad/rzp-payouts-architecture`). `payouts/…` means `$R/payouts/…`, `api/…` = PHP monolith, `xas/…` = `$R/x-account-statements/…`.
- `$A` = admitted build copies `.local/twin-repos/accepted/{payouts,ledger,fts,x-balances}` (single git tree, HEAD `4d78f86`).
- `ENV2` = `/Users/rana.singh/rzp-payouts-architecture/ENV2_COMPOSE`.
- Labels: **CONFIRMED** = read in source at the cited line; **INFERRED** = derived from more than one source but not a single literal; **UNKNOWN** = not derivable from repos.

---

## 1. Inspected files / commits (clone HEADs)

All clones are **shallow single-commit clones** (`git log` shows one commit each) — history/blame of the deprecations below cannot be dated from git; where a date is given it is decoded from Slack permalink timestamps embedded in code comments.

| Repo | HEAD | Date | Files read (primary) |
|---|---|---|---|
| payouts | `4bf3dbf` | 2026-09-03 | `internal/app/bankingAccountStatement/{core,service,repo,model,linking_params}.go`, `…/processor/{factory,base,rbl_gateway,rbl_constants,rbl_request,rbl_statement}.go`, `internal/app/bankingAccountStatementDetails/{core,model,repo,status}.go`, `internal/app/accountStatement/core.go`, `internal/app/payouts/core.go` (1500-1880, 2540-2600, 4100-4160, 7203-7900, 8429-8550, 9800-9890), `internal/app/payouts/fts_transfer_status_webhook.go` (590-760), `internal/app/payouts/constants.go`, `internal/job/{rbl_banking_account_statement,x_account_statement_source_event}.go`, `internal/routing/router/{cron_routes,bas_internal_routes,bas_dev_admin_routes,payout_internal_routes}.go`, `internal/controllers/bankingAccountStatementController.go`, `internal/app/dtos/{ledger,payout_update_bas_entity_request,bankingAccountStatement*}.go`, `internal/app/dtos/basdtos/*.go`, `pkg/api/bas_recon_payout_update.go`, `pkg/xAccountStatement/*.go`, `pkg/sourceupdater/event.go`, `pkg/bankingAccountService/fetch_banking_creds.go`, `internal/app/common/appConstants/{constants,events,entity,features,mutex_keys,config_keys,request}.go`, `internal/app/common/dualWrite/banking_account_statement.go`, `internal/app/merchant/core.go` (280-330, 482-505), `internal/database/migrations/20230128010157_*`, `20230128010223_*`, `20240625175332_*`, `config/prod.toml` (163-190, 470-600, 738) |
| ledger | `471ff4d` | 2026-09-02 | `internal/journal/ledger_config/seed_data/{direct_account_x,shared_account_x}.go`, `internal/account/seed_data/direct_account_x.go`, `internal/common/{constant,utils,config}.go`, `internal/database/migrations/{create_accounts,create_journal,create_ledger_entries,create_ledger_entry_details,create_account_details,create_idempotency,alter_transactor_id_column,modify_idempotency_table,add_tenant_in_journal}`, `internal/journal/{core,validation}.go`, `internal/journal/ledger_config/validation.go`, `internal/journal/ledger_builder/core.go`, `internal/account_discovery/core.go`, `internal/account/{core,server,repo,model,status}.go`, `internal/account/onboarding/direct_merchant_onboarding/core.go`, `internal/job/job_sqs/journal_create.go`, `proto/platform/ledger/account/v1/account_api.proto`, `config/prod-live.toml`, `test/fixture/constant.go` |
| x-account-statements (XAS) | `e73fd5a` | 2026-08-31 | `internal/job/{source_processing,statement_enrichment,save_statements_push_for_enrichment,new_merchant_onboarding}/*.go`, `internal/source_events/{model/dto.go,service/service.go}`, `internal/database/model/{account_statement_source_events,banking_account_statements}.go`, `internal/database/migrations/*`, `internal/account_statements/enrich/service/{service,params}.go`, `…/enrich/service/enrichers/payout_enricher.go`, `internal/account_statements/repo/repo.go`, `internal/account_statements/fetch/service/service.go`, `internal/account_statements/fetch/channels/banks/rbl/rbl.go`, `internal/account_statements/get/service/service.go`, `internal/account_statements/dual_write/service/service.go`, `internal/gateway/api/{params.go,service/service.go}`, `internal/api_controller/account_statement_fetch/server/server.go`, `internal/onboarding_events/model/*.go`, `config/prod.toml` |
| proto | `5268257` | 2026-09-04 | `x/x-account-statements/{statement,source_event,statements_fetch,accounts}/v1/*.proto` |
| api (monolith) | `2d665f9` | 2026-09-04 | `app/Http/Route.php` (BAS routes, allowlists), `app/Models/Payout/Core.php` (9985-10040, 10286-10410, 13754-13810), `app/Models/BankingAccountStatement/Core.php` (300-340, 3020-3400, 3530-3570, 4004-4120, 4952-4980, 5021-5280), `app/Models/BankingAccountStatement/Details/{Core,Entity}.php`, `app/Models/BankingAccountStatement/Processor/Rbl/Gateway.php`, `app/Models/Transaction/Processor/Ledger/Payout.php`, `app/Models/Merchant/Balance/Ledger/Core.php`, `app/Models/Feature/{Service,Constants}.php`, `app/Models/Payout/Repository.php` (881-960), `app/Jobs/RblBankingAccountStatement.php`, `app/Jobs/BankingAccountStatementSourceLinking.php`, `app/Services/XAccountStatements.php`, `app/Models/Merchant/RazorxTreatment.php` |
| fts | `2a09e76` | 2026-09-01 | `internal/transfer/retry_preprocessor_service.go`, `internal/providers/xas/*` (listing) |
| recon | `6974fec` | 2026-09-02 | `scripts/data_fetching/RBL_DIRECT_XAS/statement.py` |
| mozart | `bf468830` | 2026-09-03 | `app/config/razorpayx/rbl/v2/account_statement.json` |
| banking-accounts | `c3fc1fe` | 2026-09-02 | `internal/applications/events.go`, `config/default.toml` |
| x-balances | `1a21c0f` | 2026-08-18 | grep only (no BAS ownership) |
| x-bank-statement-sync | `0379e97` | 2025-09-28 | whole tree: `README.md` + 2 CI workflows. **No source code.** |
| ledger-sdk | `2eae892` | 2026-07-29 | `handler.go` |
| kube-manifests | `9226a892` | 2026-09-04 | `cde/payouts/values.yaml`, `cde/api/values.yaml`, `prod/x-account-statements/values.yaml` |
| Twin | `ENV2_COMPOSE` @ branch `milestone-3-1-claude-final` (3a044f8) | | `docker-compose.yml`, `generated/payouts/arena.toml`, `generated/fts/env.arena.toml`, `seeds/localstack/init-queues.sh`, `seeds/generated/s4/{ledger,apidb}.sql`, `scripts/up.sh`, `scripts/cron-driver/driver.py`, `substitutes/{xas-sink,monolith-stub,bankingaccounts-stub,mozart-sim,ledger-gate,splitz-stub}/server.py`, `verifier/verifiers/test_v05..v08`, `verifier/helpers/payouts_flow.py`, `TWIN_SPEC/*.yaml`, `reports/fidelity/raw/11_statements_recon_repair.md` |

Accepted twin copy check: `$A/payouts/internal/app/bankingAccountStatement` and `internal/job/rbl_banking_account_statement.go` are byte-identical to the pristine clone except that `_test.go` files were stripped (`diff -rq`). `$A/payouts/internal/app/payouts/core.go:7203-7395` is identical, including the commented-out ledger call (see Finding 6).

---

## 2. Findings

### Finding 1 — Bank-account-statement (BAS) ingestion: who fetches RBL current-account statements

**Three independent ingestion implementations exist in source, all for the same RBL "account_statement" Mozart action.** Which one FastCron actually drives per channel/merchant in production is **UNKNOWN** from repos (cadence lives in FastCron SaaS; kube shows all three worker fleets deployed).

#### 1(a) Payouts Service (PS) — `rbl_banking_account_statement` worker (the worker named in the brief)

| Step | Symbol | Citation (payouts@4bf3dbf) |
|---|---|---|
| Trigger | `POST /v1/cron/banking_account_statement/fetch/initiate`, group `/v1/cron`, `middleware.BasicAuth(cred.FastCron)` | `internal/routing/router/cron_routes.go:15,26-27` |
| Request DTO | `{channel (required), account_rate_limit, reserve_percentage, inactive_duration_limit, account_type}`; defaults 6 / 95 % / 3600 s / `direct` | `internal/app/dtos/bankingAccountStatementFetch.go:7-13`; `bankingAccountStatementDetails/core.go:26-28,153-174` |
| Account selection | `FetchBasDetailsByChannelOrderByLastStatementAttemptAt(channel, account_type)`; selects up to `reserve% × rate_limit` accounts satisfying `CheckIfAccountNumberSatisfiesSelectionCriteria` (gateway_balance ≠ statement_closing_balance, or balance matched but gateway_balance changed after last attempt; excludes inactive > limit) → rule `balance_changed_rule`, remainder `others` up to `rate_limit` | `bankingAccountStatementDetails/core.go:55-151, 210-249`; rule constants `appConstants/constants.go:321-322` |
| Dispatch | `DispatchAccountForStatementFetch` → `provider.GetWorker(ctx).Perform(job.NewRblBankingAccountStatementJob(ctx, req), delay)`; delay from Redis key `{rbl_banking_account_statement_default_dispatch_delay}` (default 0) | `bankingAccountStatementDetails/core.go:176-208, 251-273`; `appConstants/config_keys.go:9` |
| Job | `worker.Job{Name:"rbl_banking_account_statement", QueueName: cfg.Job.RblBankingAccountStatement, MaxRetries:7, Timeout:1800s}`; payload `StatementFetchForAccountNumberRequest{channel, account_number, balance_id, attempt_number, dispatch_delay, rule}` (JSON keys same, snake_case) | `internal/job/rbl_banking_account_statement.go:16-24, 33-40, 67-78` |
| Queue name (prod) | `prod-payouts-rbl-banking-account-statement-live` | `config/prod.toml:177` |
| Worker binary | `cmd/workers/main.go` with `PAYOUTS_WORKER_NAME=rbl_banking_account_statement`; k8s `payouts_worker_rbl_banking_account_statement_live_replicas: 1` (HPA 1-8) | `kube-manifests/cde/payouts/values.yaml:112,175-176` |
| Handler | `Core.FetchAccountStatement`: Redis mutex `banking_account_statement_fetch_<account>_<channel>` 300 s; loads ACTIVE BASD by (account_number, channel); sets `last_statement_attempt_at`; processor via factory key `<channel>_banking_account_statement_processor` | `bankingAccountStatement/core.go:235-287`; `appConstants/mutex_keys.go:4`; `processor/factory.go:47-60`; `appConstants/constants.go:310` |
| Registered processors | **RBL only** (`RegisterProcessor("rbl_banking_account_statement_processor", …)`); channel literal `"rbl"` | `processor/rbl_gateway.go:49-51`; `appConstants/constants.go:64` |
| Bank credentials | banking-accounts service `GET /merchant/{mid}/banking_account_by_account_number/{acct}/credentials` → map with `auth_username, auth_password, client_id, client_secret, corp_id` | `pkg/bankingAccountService/fetch_banking_creds.go:14,69`; `processor/rbl_gateway.go:264-293`; `appConstants/constants.go:342-343` |
| Bank API contract | **Mozart**: `SendMozartRequest(namespace="razorpayx", gateway="rbl", version="v2", action="account_statement", RblRequest)`; `RblRequest{attempt{id=unix-ts, transaction_type:"B", from_date, to_date (fmt 02-01-2006) \| next_key}, source_account{account_number, credentials{…}}}`; date window ≤ 365 d from BASD metadata `statement_fetched_till_date`; with pagination key → `next_key` only | `processor/rbl_gateway.go:168-231, 264-293, 324-353`; `processor/rbl_request.go:3-27`; `appConstants/request.go:67-73`; Mozart config `mozart/app/config/razorpayx/rbl/v2/account_statement.json` (RequestMapper fields `TranID, Corp_ID, Acc_No, From_Dt, To_Dt, NextKey`; success criterion `Status=='Success' \|\| (Status=='Failure' && Status_Desc=='No Records Found')`) |
| Response shape | `{FetchAccStmtRes:{Header:{Code, Corp_ID, Status ∈ Success\|Failure, Status_Desc, TranID, account_no, from_date, to_date, next_key}, AccStmtData:{File_Data: base64 CSV}}}`; CSV header must equal `TRAN_ID,PTSN_NUM,TRAN_DATE,PSTD_DATE,TRAN_TYPE,C/D,TRAN_PARTICULAR,TRAN_AMT,TRAN_BALANCE`; 9 columns per row | `processor/rbl_statement.go:9-32`; `processor/rbl_constants.go:26`; `processor/rbl_gateway.go:413-460, 462-515` |
| Parsing | amounts rupees→paise; category map `TCI→customer_initiated, TBI→bank_initiated, CNP→cash_withdrawal, CNR→cash_deposit, else others`; `D/C→debit/credit`; `PSTD_DATE` fmt `02-01-2006 15:04:05`, `TRAN_DATE` fmt `02-01-2006` (IST); UTR regex from description (credit: `^(?P<utr>[0-9]{12})_IMPS`, `^(RTGS/\|NEFT/SFMS RTN/\|NEFT/\|UPI/\|R/UPI/\|R-\|IMPS )(?P<utr>.*?)(/\|-\| )`; debit: `^(RTGS/\|NEFT/)(?P<utr>.*?)(/)`, `^(UPI/)(?P<utr>.*?)(/)`, `^(?P<utr>.*?)-`); GRN regex `\sRZP([0-9A-Z]{10})$`; rows with `posted_date ≥ request_time−60s` skipped and next_key suppressed | `processor/rbl_constants.go:14-22,37-61`; `processor/rbl_gateway.go:355-411, 664-703` |
| Paging | attempt limit 5, retry limit 0, "has more" if ≥5000 rows or `to_date < today`; pagination key persisted as `"<last_posted_ts>_<bank next_key>"` (regex `^[1-9][0-9]{9}_`); "No Records" with next_key → advance `statement_fetched_till_date` by 4 weeks and clear key | `processor/rbl_constants.go:6-12,24,60`; `processor/rbl_gateway.go:97-166, 206-231, 247-262, 295-316` |
| Dedupe | local: struct-equality on whole `BankTransaction`; DB: batches of 200, `FetchByTxnIds(bank_transaction_id IN …, account_number, created_at ≥ min txn date)`, compare tuple **{account_number, posted_date, type, bank_serial_number, amount, bank_transaction_id, channel}** | `processor/rbl_gateway.go:517-662`; `bankingAccountStatement/repo.go:54-74` |
| Persist | DB txn: `CreateAccountStatementsInBatches` (row-by-row `Create`) + BASD update `{pagination_key, statement_closing_balance=last row balance, statement_closing_balance_change_at, metadata}` | `processor/base.go:55-97`; `bankingAccountStatement/core.go:289-319` |
| Tables written | `banking_account_statement` (PS MySQL) and `banking_account_statement_details` (update only) | DDL `internal/database/migrations/20230128010157_banking_account_statement_table.go:17-47`, `20230128010223_banking_account_statement_details_table.go:16-36` |
| Side effect | model hook `DualWrite` → `dualWrite.ProcessForBAS` → `async_dual_write` job (to monolith) | `bankingAccountStatement/model.go:202-204`; `common/dualWrite/banking_account_statement.go:13-38` |

Important: **PS never creates `banking_account_statement_details` rows**. Grep for BASD `Create` in payouts returns nothing; the monolith writes them directly into the PS database (`api/app/Models/BankingAccountStatement/Details/Core.php:219-237` `createBasDetailsInPayoutService/updateBasDetailsInPayoutService` → `insertIntoPayoutServiceDB`). CONFIRMED.

#### 1(b) API monolith — its own RBL fetch (still live per kube)

- FastCron routes `banking_account_statement/process` (`api/app/Http/Route.php:4182`, in `cron` app group `:6542`), admin `banking_account_statement/admin/process` (`:4579`), channel fetch (`:4185`).
- Job `RZP\Jobs\RblBankingAccountStatement` queue key `rbl_banking_account_statement_fetch` (`api/app/Jobs/RblBankingAccountStatement.php:44`), handle → `BAS\Core::fetchAccountStatementV2` (`:173`) → `getProcessor(channel …)` → `Processor\Rbl\Gateway::fetchAccountStatementDetails` → Mozart `sendMozartRequest` v1 (`Processor/Rbl/Gateway.php:280`) / v2 (`:445-479`). Dispatch: `dispatchBankingAccountStatementJob` (`BankingAccountStatement/Core.php:4952-4980`).
- k8s: `api_sqs_rbl_banking_account_statement_fetch_live_replicas: 3`, `api_sqs_banking_account_statement_processor_live_replicas: 5`, `api_sqs_banking_account_statement_source_linking_live_replicas: 1` (`kube-manifests/cde/api/values.yaml:340-342,427`). CONFIRMED deployed; whether its cron is enabled is UNKNOWN.

#### 1(c) x-account-statements (XAS) — multi-bank fetch (RBL, AXIS, ICICI, IDFC, YESBANK, SLICE)

- HTTP: `POST /v1/banking_account_statement/fetch/initiate` body `{channel, account_rate_limit?, reserve_percentage?, inactive_duration_limit?, account_type?}` (same shape as PS) and `POST /v1/banking_account_statement/fetch/manual {account_id, pagination_key?, start_timestamp?, end_timestamp?, save_statements}` (`proto/x/x-account-statements/statements_fetch/v1/statements_fetch_api.proto:12,16`; `statements_fetch.proto:7-25`; server `xas/internal/api_controller/account_statement_fetch/server/server.go:60-110`, enqueue `:202`).
- Jobs/queues (`xas/config/prod.toml:99-112`): `FetchStatementsForAccount=prod-x-account-statements-fetch-sqs`, `…-fetch-ybl-sqs`, `…-fetch-idfc-sqs`, `…-fetch-axis-sqs`, `…-fetch-icici-sqs`, `…-fetch-slice-sqs`, `SaveStatementsPushForEnrichment=prod-x-account-statements-save-and-enrich-sqs`, `StatementEnrichment=prod-x-account-statements-enrichment-sqs`, `SourceProcessing=prod-x-account-statement-source-event`, `AccountStatementDualWrite` (Kafka `mysql_cdc_events_prod_rx_account_statements_account_statements`), `AccountsDualWrite` (Kafka `…_accounts`), `APIToXasMerchantMigration=prod-x-account-statements-migration`, `NewMerchantOnboarding=prod-x-account-statements-account-activation-event`. That is 11 SQS queues + 2 Kafka topics (13 `[Job]` keys).
- RBL adapter: Mozart attributes `razorpayx/rbl/v2/account_statement` (`xas/internal/account_statements/fetch/channels/banks/rbl/rbl.go:459-466`), same base64-CSV parsing and UTR/GRN extraction (`:477-515`), `DedupeFilters {account_number, posted_date, type, bank_serial_number, amount, channel, bank_transaction_id}` (`:740-750`); fetch service dedupe → `CreateInBatches` → one `statement_enrichment` job per saved row (`xas/internal/account_statements/fetch/service/service.go:165-230, 265-290`).
- Tables: `account_statements` (DDL `xas/internal/database/migrations/20240702122008_create_table_account_statements.go`; adds `fetched_at_nano`), `accounts` (`20240702161426…`; `UNIQUE(account_number, channel)`, `balance_id`), `source_events` (`20241029152308…`).
- k8s: workers fetch/ybl/idfc/axis/icici/slice + `x-account-statements-worker-source-event` (`kube-manifests/prod/x-account-statements/values.yaml:43-208`, `:96-99`); `crons: []` (commented `fetch-statement` cron `:220-224`) → trigger is external via `traefik-external` ingress (`:4`) — cadence UNKNOWN.
- x-bank-statement-sync: **empty repository** (README + CI only). Nothing to model.
- banking-accounts: provides the credentials route consumed by PS/monolith; XAS host in `banking-accounts/config/default.toml:129`; RBL/IDFC onboarding state machines emit activation events (`banking-accounts/internal/applications/onboarding/{rbl,idfc}/events.go`) — the publisher to `prod-x-account-statements-account-activation-event` was not located by grep (UNKNOWN; payload model in `xas/internal/onboarding_events/model` fields `event_type, timestamp, account_id, merchant_id, business_id, account_type, partner_bank, activated_at, status, balance_id, currency, account_number`).

### Finding 2 — `banking_account_stmt_detail_id`

- **Entity**: a `banking_account_statement_details` (BASD) row = statement-fetch state for one (merchant_id, balance_id, account_number, channel) current account. Columns (PS DDL `20230128010223…:16-36`): `id char(14) PK, merchant_id char(14), balance_id char(14) (idx), account_number varchar(48), channel varchar(32), status varchar(64) [active|archived|inactive], statement_closing_balance bigint, gateway_balance bigint, statement_closing_balance_change_at int, gateway_balance_change_at int, last_statement_attempt_at int, balance_last_fetched_at int default 0, pagination_key varchar(255), account_type varchar(20) default 'direct', created_at, updated_at int, metadata json`. Monolith entity constants mirror these plus `last_reconciled_at` (`api/app/Models/BankingAccountStatement/Details/Entity.php:23-37`).
- **ID format**: 14-char Razorpay UniqueId; signed/public form `basd_<14>` (`payouts/internal/app/bankingAccountStatementDetails/model.go:34,67-69` `Sign="basd_"`; monolith `getPublicId()` used in `api/app/Models/Merchant/Balance/Ledger/Core.php` `createXLedgerAccountForDirect`). Generated by the monolith at CA onboarding (route `banking_account_statement/details` `Route.php:4193`, comment `:3597` "creates balance and banking_account_statement_details"), dual-written into PS DB (`Details/Core.php:219-237`).
- **Definitions/uses**:
  - payouts: constant `AttributeBASDetailsID = "banking_account_stmt_detail_id"` (`appConstants/entity.go:243`); used as **ledger journal identifier** in `bankingAccountStatement/core.go:741-742` (external statements; value `basd.GetSignedID()`) and `payouts/core.go:7818-7841` (`GetDefaultDirectPayoutPayloadForLedger`: `Identifiers["banking_account_stmt_detail_id"] = BASDId` where `BASDId = BASD core.GetBasDetailsID(payout)` = BASD row looked up by `payout.balance_id` → `GetSignedID()` `bankingAccountStatementDetails/core.go:587-593`, `repo.go:64-76`); field `BankingAccountStmtDetailID json:"banking_account_stmt_detail_id,omitempty"` on `PayoutStatusUpdateEvent` (`pkg/sourceupdater/event.go:70`) — **never assigned anywhere** (grep), so source-updater events carry it empty. CONFIRMED.
  - ledger: `common.BankingAccountStmtDetailID = "banking_account_stmt_detail_id"` (`ledger/internal/common/constant.go:172`, in identifier allow-list `:194-195`); `DAMerchantIdentifier = {"banking_account_stmt_detail_id": "$banking_account_stmt_detail_id"}` used by every Direct-X account discovery config (`ledger_config/seed_data/direct_account_x.go:48-94`); DA sub-account name suffix (`internal/account/onboarding/direct_merchant_onboarding/core.go:41-51`); fixture `{"banking_account_stmt_detail_id":["basd_samplebankId22"]}` (`test/fixture/constant.go:137`).
  - monolith: `Transaction/Processor/Ledger/Base.php:53`, `Transaction/Statement/Ledger/Statement/Service.php:33`, `Merchant/Balance/Ledger/Core.php:51`, `Services/Ledger.php:96`, ledger onboarding payload `Feature/Service.php:665,716`, `Services/ChargeCollections.php:174`.
  - XAS: **not used** (grep empty). XAS keys statements by `balance_id`/`account_number`.
- **Attachment to payout/transaction**: not a payout column. Payout↔statement linkage is `payouts.transaction_id = <bas_id>` (PS `payouts/core.go:7276-7284`; monolith `Payout/Core.php:10320-10325`). The BASD id is resolved at journal-build time from `payout.balance_id`. INFERRED from the two citations.

### Finding 3 — `x_account_statement_source_event`

| Aspect | Value | Citation |
|---|---|---|
| Producer | payouts only (no producer in api/fts/x-balances; grep) | `payouts/internal/job/x_account_statement_source_event.go:21-30, 78-100` |
| Queue | PS config key `[job].x_account_statement_source_event` = `prod-x-account-statement-source-event`; XAS `Job.SourceProcessing` = same string | `payouts/config/prod.toml:188`; `xas/config/prod.toml:105` |
| PS job def | `Name=appConstants.XAccountStatementSourceEvent, MaxRetries 5, Timeout 120s`; pushed with `PerformWithoutSerialization(job, rawJSON, 0)` i.e. the SQS body is the bare JSON of `XasEventDetails` | job file `:21-30, 85-87` |
| PS payload | `XasEventDetails{entity_id, entity_type, utr, event_created_timestamp, event_id, gateway_ref_no, cms_ref_no, status, mode, amount, balance_id, fund_account_id, contact_id, name, contact, email, payout_purpose, contact_type}` | job file `:37-56` |
| Payload builder | `AccountStatementEventPayload`: `entity_id = payout.ID` (unprefixed 14-char), `entity_type="payout"`, `utr`, `event_created_timestamp=now`, `event_id = id.New()` (14-char), `status=payout.status`, `mode`, `amount`, `balance_id`, `payout_purpose`, fund-account/contact from CFA/API cache, `gateway_ref_no=payout.gateway_ref_no`, `cms_ref_no` from payout meta `FundTransferServiceInfoMeta` | `payouts/internal/app/payouts/core.go:8455-8550` |
| Push sites | `PushXasSourceEventAndSyncEs` after FTS-driven status update (`HandlePayoutStatusUpdateViaFTS` `:1536`, `HandlePayoutStatusUpdateViaFundTransferService` `:1631`); failures only logged (`:1538-1545`) | `:1768-1857` |
| Gates | (1) `payout.GetPublicBankingAccount().GetAccountType() == "direct"` only (`:1786-1788`); (2) `isXasSourceEventExperimentEnabled`: config `[configs.account_statement_source_event]` env whitelist/blacklist else Splitz `account_statement_source_event` (prod id `P9jMHiqpMI81A5`, variant name `account_statement_source_event`) (`:1859-1880`; `config/prod.toml:477,528-531`); (3) terminal status ∈ {failed, processed, reversed} (`constants.go:27-31`; `:1796`); (4) at least one of utr/grn/cms else `ErrorPayoutUtrNotFoundForAccountStatementEvent` (`:1817-1819`) | |
| Manual re-push | `POST /v1/payouts/retry/source_update` → `PushXasSourceEvent`; validation: `source_id`, `transfer_id>0`, one of utr/grn/cms, status ∈ valid set | `payout_internal_routes.go:65-66`; `core.go:1697-1766, 4109-4130` |
| Consumer | XAS job `source_processing` (`MaxRetries 3, Timeout 60s`), constructor `json.Unmarshal → model.SourceEvent`; optional ES push; `sourceeventservice.Handle` | `xas/internal/job/source_processing/source_processing.go:25-38, 80-116`; k8s worker `x-account-statements-worker-source-event` (`kube-manifests/prod/x-account-statements/values.yaml:96-99`) |
| Consumer schema | `SourceEvent{entity_id, entity_type, utr, event_create_timestamp, event_id, gateway_ref_no, cms_ref_no, status, mode, amount, balance_id, fund_account_id?, contact_id?, name?, contact?, email?, payout_purpose?, contact_type?}` | `xas/internal/source_events/model/dto.go:11-31` |
| **Schema mismatch** | PS emits `event_created_timestamp`; XAS reads `event_create_timestamp` → XAS stores `0` into `source_events.event_create_timestamp` (NOT NULL int). Nothing in XAS consumes the timestamp for matching, so functionally inert, but a twin substitute must not "fix" it silently. CONFIRMED (both files) | job file `:41` vs `dto.go:15` |
| Validation | `utr != "" \|\| gateway_ref_no != "" \|\| cms_ref_no != ""` else `SourceEventInvalid` (no retry benefit) | `dto.go:54-56`; `service.go:61-66` |
| Persistence / dedup key | `source_events` row; **`UNIQUE KEY (event_id, entity_type)`**; on unique violation the stored row for (event_id, entity_type) is used (`getEventForRetry`) and processing continues. No dedup on `entity_id`: a re-emitted event with a new `event_id` creates a second row | `service.go:68-88, 140-156`; DDL `20241029152308…` (`UNIQUE KEY account_statements_event_id_entity_type_index (event_id, entity_type)`) |
| DDL | `source_events(id char(14) PK, entity_id char(14) NOT NULL, entity_type varchar(64), utr varchar(255), gateway_reference_number varchar(50), cms_reference_number varchar(255), mode varchar(64), amount bigint unsigned, status varchar(64), balance_id char(14), created_at/updated_at bigint, event_create_timestamp int, event_id varchar(64))`, indexes on (utr,balance_id), (gateway_reference_number,balance_id), (cms_reference_number,balance_id) | same migration |
| Ordering | none assumed; both arrival orders handled: `EnrichWithSource` (event after statement) and `EnrichWithStatement` (statement after event) | `xas/internal/account_statements/enrich/service/service.go:44-91, 93-148` |
| Pull fallback | XAS `POST /v1/source_event/fetch` → `FetchEvents(source, entity_ids)` → gateway `SourceEventFetch` → monolith `GET /v1/payouts_internal/{id}/source_event_info` (Basic `APISERVICE_AUTH_*`) → `getSourceEventInfoForStatementEnrichment` returns the same field set (for PS payouts reads utr/status from the FTA row) | `proto/…/source_event/v1/fetch_api.proto:12`; `xas/internal/source_events/service/service.go:93-138`; `xas/internal/gateway/api/service/service.go:37`; `api/app/Http/Route.php:2353` (allow-listed under `payouts_service` group `:19379/19422`); `api/app/Models/Payout/Core.php:13754-13810` |

### Finding 4 — Matching / reconciliation (who matches, keys, no-match, duplicates, ordering, conflicts)

**Three matchers exist. XAS is the matcher for XAS-ingested statements; the monolith matches its own fetches; PS only links on instruction (ART) — its UTR auto-linker is a stub.**

#### 4(a) XAS (`xas/internal/account_statements/enrich/service/service.go`)
- Statement-driven `EnrichWithStatement(statementID)` (`:44-91`): account row by (account_number, channel) → `balance_id`; `FetchEntityForEnrichment(Entity=source_event, BalanceID, Amount, UTR, GRN, CMS)`; identifiers tried **in order UTR → gateway_ref_number → cms_ref_number (= bank_transaction_id)** (`:190-218`); each query is `WHERE <key>=? AND balance_id=? AND amount=?` (`repo.go:327-341` for UTR; GRN/CMS analogous). No match → `markAsExternal` (`entity_type='external'`, `:328-343`).
- Event-driven `EnrichWithSource` (`:93-148`): accounts by `balance_id` → statements by `<key>=? AND account_number=? AND amount=?` (`repo.go:268-287`) in the same key order; guards: 0 → no-op; >2 → `TooManyStmtsForUtr` no-op; two of same `type` → `UnexpectedStmtType` no-op (`:343-357`); otherwise enrich each (typically one debit + one credit).
- Write: `PayoutEnricher.EnrichStatement`: `entity_type = payout_reversal` if statement `type=credit` else `payout`; `entity_id = event.entity_id`; refuses to relink a statement already linked to a *different* non-external entity (`enrichers/payout_enricher.go:38-63`).
- **Keys: {utr | gateway_ref_number | bank_transaction_id} + balance_id/account_number + amount. No merchant_id, currency, mode or date window.** CONFIRMED.
- Outbound: CDC (Kafka) → `dual_write.Update` → `EnrichmentUpdate` POST monolith `/v1/banking_account_statement/payout_update` `{bas_id, entity_id, entity_type, merchant_id, transaction_date, converted_from_external, utr, grn, cms_ref_no}` only when `entity_type ∈ {payout, payout_reversal}` (`dual_write/service/service.go:113-170`, `gateway/api/params.go:38-49`, `gateway/api/service/service.go:34-35,108-127`). Also invocable without Kafka via `POST /v1/statement/dual_write` (`proto/…/statement_api.proto:12` → `DualWriteForStatement` `:173-190`).

#### 4(b) Monolith (`api/app/Models/BankingAccountStatement/Core.php`)
- `saveAccountStatementV2` (`:3043-3140`) → `linkAccountStatementRecord` (`:3143-3175`, DB txn: `processSourceEntity`, associate source + transaction, `updatePostedDate`) → `processSourceEntity` (`:3302-3350`): credit → `processReversal`, debit → `processPayout`; any `remarks` or null → `processExternal`; `validateBalance` (`:3348`).
- Debit payout lookup `fetchExistingPayoutForAccountStatement` (`:4004-4120`): `payout.fetchFromUtr(utr, amount, balance_id)` (`Payout/Repository.php:881-893`: `WHERE balance_id=? AND amount=? AND utr=?`, reads PS DB when `shouldSkipApiFallback`); already-linked payouts (transaction_id set) are alerted and excluded; exactly one unlinked → link; >1 unlinked → external with remarks (Slack `rx_ca_rbl_alerts`); fallback by `bank_transaction_id`/CMS and by GRN (last 10 chars after `RZP`) within `posted_date − 4h` (`:4085-4120`).
- Credit before debit: `reversePayoutForCreditBeforeDebit` (`:3176-3250`), Redis list of UTRs.
- **PS-payout branch**: when Splitz `payout_service_txn_recon` (`Merchant/RazorxTreatment.php:364`) is enabled and `payout.is_payout_service`, monolith calls PS `updatePayoutAfterBASRecon` with `{bas_id=<monolith transaction id>, entity_id, entity_type:'payout', merchant_id, transaction_date, converted_from_external}` and **skips its own ledger event** for that debit (`:3538-3560`, `:3081-3091`).
- ART/manual: `updateSourceLinking` (`:5078`, route `banking_account_statement/source/update` `Route.php:4227-4228`), `retryBasSourceLinkingForProcessedPayout` (`:5121`, job `BankingAccountStatementSourceLinking`).

#### 4(c) PS (`payouts/internal/app/bankingAccountStatement/core.go`)
- ART-driven `ProcessPostRecon` via `POST /v1/banking_account_statement/process/batch` (`bas_internal_routes.go:11-25`, `BasicAuth(cred.API, cred.Workflow)`), monolith front route `banking_account_statement/batch/process` (`Route.php:2434`, in `$internal` `:6027` and `batch` app group `:18735`). Body: array of `{data: base64(JSON), idempotent_id}`; decoded `{banking_account_statement_id, recon_status ∈ Reconciled|Reversed|Reconciled_Reversed|Unreconciled, entity_id: "<a>,<b>,<14-char id>", entity_type}` (`dtos/bankingAccountStatementProcess.go:7-12`; `basdtos/bas_linking_data.go:10-15`; `core.go:673-687`; `appConstants/constants.go:332-335`). Logic `:321-432, 707-733`: lock BAS row; if already `payout`+debit → `BankingAccountStatementEntityAlreadyLinkedToBas`; Reconciled/Reconciled_Reversed → `payout` (debit) / `payout_reversal` (credit) unless entity already linked to a BAS of same type (`IsEntityLinkedToBAS` `:689-705`) → `external`; else `external`. Post-commit hook: external → `NotifyLedgerOnLinking` (Finding 6); payout/reversal → `SendUpdatesToPayoutSource` (`:434-485`) → monolith `POST /banking_account_statement/payout_update` (`pkg/api/bas_recon_payout_update.go:14-24`, body `{bas_id, entity_id, entity_type, merchant_id, transaction_date, converted_from_external}`) which the monolith forwards back to PS for PS payouts.
- Auto UTR linker `ProcessBankingAccountStatementForPayoutID` (`:80-114`): `FetchByUtrForLinking` queries `utr=? AND amount=? AND channel=?` (`repo.go:27-37`) then filters merchant_id + account_number, skips linked, errors on >1 debit or >1 credit — but `LinkingParams.Validate/PlaceLocks/Link` are **stubs returning nil** (`linking_params.go:44-56`). Dead path. CONFIRMED.
- Dev-admin manual link `POST /v1/dev_admin/banking_account_statement/link {banking_account_statement_id, entity_id, entity_type ∈ payout|payout_reversal|external}` (`bas_dev_admin_routes.go:20-21`; `core.go:487-591`; cannot override an existing non-external link).
- PS read API for statements `GET /v1/banking_account_statement?merchant_id&channel&utr|cms_ref_no|gateway_ref_number|source_id` (`bas_internal_routes.go:28-42`, `BasicAuth(cred.FTS)`; OR-filter `repo.go:88-124`).

#### 4(d) Conflict rules (statement vs transfer status)
- PS `UpdatePayoutAfterBASRecon` (`payouts/core.go:7203-7395`): `entity_type=payout` → idempotent if `transaction_id == bas_id` (`:7233-7239`); `shouldSkipUpdateDueToMismatch` (`:9850-9887`; Splitz `xas_source_event_utr_and_grn_match_experiment`=`RZfCkJYmCWWCPG`, variant `xas_se_utr_grn_match` `:9800-9815`): if request carries utr/grn/cms and **none** match the payout's stored `utr`/`gateway_ref_no`/cms → skip silently (metric `BASReconUTRGRNMismatchCount`). `entity_type=payout_reversal` → if payout `failed`: create reversal entity + state-machine `EventReversed` (**statement credit converts failed→reversed**, `:7297-7318`); else attach `transaction_id` to the existing reversal (error `ReversalEntityNotFound` if none, `:7320-7345`). Then `payout.processed`/`payout.reversed` mail+SMS via API (`:7370-7386`).
- Statement-before-status: FTS "failed" is refused when the payout already has a `transaction_id` or when XAS holds a **debit** statement for it: `VerifyPayoutFailedTransaction` (`fts_transfer_status_webhook.go:654-720`; Shared skips; query XAS `GET /v1/account_statements/fetch_multiple_by_reference_numbers?type=debit&account_number&channel&amount&utr&gateway_reference_number&cms_reference_number` via `accountStatement/core.go` + `pkg/xAccountStatement/fetch_by_multiple_references.go:15,70-78`; XAS handler `get/service/service.go:425-470`, filters by type & channel, API-DB fallback disabled `:475-478`).
- FTS retry gate: `retry_preprocessor_service.go:249-262`: credit (or both) present → allow retry; only debit → `CodeDebitEntryExistInStatement` (refuse); none → `CodeMissingDebitAndCreditEntryInStatement`.
- Duplicate statements: PS/XAS dedupe tuples above; PS has a one-off data-fix migration deleting 46 duplicate BAS ids (`20240625175332_delete_duplicate_bas.go`) — evidence that dedupe failed at least once in prod.
- Unmatched pool: XAS `entity_type='external'`; monolith `External` entity + `transaction`; PS `external` (+ `da_ext_*` journal). No automatic retry schedule for unmatched rows was found in PS or XAS (XAS re-tries on every new source event or statement arrival; recon/ART re-uploads drive `batch/process`). UNKNOWN: FastCron cadences.

### Finding 5 — XAS reconciliation "repair" outbound updates

| Hop | Route | Auth | Body | Citation |
|---|---|---|---|---|
| XAS → monolith | `POST https://prod-api-int.razorpay.com/v1/banking_account_statement/payout_update` | HTTP Basic `APISERVICE_AUTH_USERNAME/PASSWORD` (app-name resolvable only as `payouts_service` by elimination — INFERRED) | `{bas_id, entity_id, entity_type, merchant_id, transaction_date, converted_from_external, utr, grn, cms_ref_no}` | `xas/config/prod.toml:93-97`; `xas/internal/gateway/api/service/service.go:34-35,108-127`; `api/app/Http/Route.php:4837` (`payouts_service` group `:19379`) |
| monolith → PS | `POST {PS}/v1/payouts/banking_account_statement/payout_update` (same body, forwarded verbatim; only when payout is null or `is_payout_service`) | Basic `API` credential | same | `api/app/Models/Payout/Core.php:10305-10318`; `payouts/internal/routing/router/payout_internal_routes.go:126-133` (`BasicAuth(API, Workflow, Xperience, FTS, VendorPayments, Settlements, Irctc)`) |
| PS → monolith (from PS ART linking) | `POST {API}/banking_account_statement/payout_update` | PS→API service creds (`cred.API`) | `{bas_id, entity_id, entity_type, merchant_id, transaction_date, converted_from_external}` (no utr/grn/cms) | `payouts/pkg/api/bas_recon_payout_update.go:14-74` |
| XAS trigger without Kafka | `POST /v1/statement/dual_write` → `DualWriteForStatement(statementID)` → `EnrichmentUpdate` | XAS internal Basic (`Server.Auth.Internal` user `internal`) | `{id}` | `proto/x/x-account-statements/statement/v1/statement_api.proto:12`; `xas/internal/account_statements/dual_write/service/service.go:173-190`; `xas/config/prod.toml:117-124` |
| Admin manual link | monolith `POST banking_account_statement/source/update` (+`/validate`) | admin permission | `{…}` validated by `Validator::SOURCE_UPDATE` | `Route.php:4227-4228`; `BankingAccountStatement/Core.php:5078-5120` |

No queue message from XAS to PS/ledger exists; XAS never calls Ledger (grep `ledger` in `xas/internal` = 0 files). CONFIRMED.

### Finding 6 — Ledger Direct accounting (`direct_account_x`)

Files: rules `ledger/internal/journal/ledger_config/seed_data/direct_account_x.go` (864 lines); parent accounts `ledger/internal/account/seed_data/direct_account_x.go`; Shared counterparts `…/shared_account_x.go` (identifier `banking_account_id`, `shared_account_x.go:58`).

**Transactor events** (`ledger/internal/common/constant.go:507-518`): `da_payout_processed`, `da_payout_reversed`, `da_fee_payout_processed`, `da_fee_payout_reversed`, `da_payout_processed_recon`, `da_payout_reversed_recon`, `da_ext_debit`, `da_ext_credit`, `da_ext_payout_processed`, `da_ext_payout_reversed`, `da_ext_fee_payout_processed`, `da_ext_fee_payout_reversed`. **There is no `da_payout_initiated` and no `da_payout_failed` event.** (Shared: `payout_initiated/processed/failed/reversed` `:487-490`, configs `shared_account_x.go:169-178, 287-370`.) Payouts' allow-list of DA events it can emit: `payouts/core.go:7663-7666` (12 events incl. charge-collections variants).

**Journal rule tables** (Tenant `X`; Rule = `{transactor_event}` plus optional `fee_accounting=reward`, `data_movement_strategy=DALegacyDataMigration`):

| Config id | Rule | Entries (Dr/Cr, formula) | Lines |
|---|---|---|---|
| `XDAPayoutProcessed` | `da_payout_processed` | Dr MerchantBalance(amount); Cr VendorPayable(amount); Dr CommissionReceivable(commission); Cr CommissionIncome(commission − tax); Cr OutputGST(tax) | `:220-253` |
| `XDAPayoutProcessedRecon` | `da_payout_processed_recon` | Dr VendorPayable(amount); Cr MerchantCash(amount) | `:383-401` |
| `XDAPayoutReversed` / `…ReversedRecon` | mirrors | `:256-289`, `:403-421` |
| `XDAExtDebit` | `da_ext_debit` | Dr MerchantBalance(amount); Cr MerchantCash(amount) | `:609-627` |
| `XDAExtCredit` | `da_ext_credit` | Cr MerchantBalance; Dr MerchantCash | `:588-606` |
| `XDAExtToPayoutProcessed/Reversed` | `da_ext_payout_*` | 7-entry re-class (ext → payout) | `:292-381` |
| Reward / Fee / Legacy variants | `fee_accounting=reward`, `da_fee_*`, legacy | `:133-140, 423-585, 629-837` |

Direct success = **two** journals per payout in the live monolith path: `da_payout_processed` then `da_payout_processed_recon` (`api/app/Models/BankingAccountStatement/Core.php:5208-5209`; PS mirror `payouts/core.go:7563-7591` — commented path).

**Account roles / discovery configs** (`direct_account_x.go:57-115`; all except reward accounts carry identifier `{"banking_account_stmt_detail_id":"$banking_account_stmt_detail_id"}`):

| Role | account_category | account_type | fund_account_type | Identifier |
|---|---|---|---|---|
| DAMerchantBalanceAccount | liability | payable | merchant_da | banking_account_stmt_detail_id |
| DAVendorPayableAccount | liability | payable | merchant_da_vendor | same |
| DACommissionIncomeAccount | revenue | cash | merchant_da | same |
| DAOutputGSTAccount | liability | payable | da_gst | same |
| DACommissionReceivableAccount | asset | receivable | merchant_da | same |
| DAMerchantCashAccount | asset | cash | merchant_da | same |
| DARewardAccount | expense | cash | reward | none |
| DAMerchantRewardAccount | liability | payable | reward | merchant_id |
| DACashAccountLegacy | asset | cash | da_cash_account | none |

Literal strings: `merchant_da`, `merchant_da_vendor`, `da_gst`, `da_cash_account`, `reward` (`common/constant.go:230,319-322`); `payable/receivable/cash` (`:654-657`); categories `asset/liability/revenue`, business `real/nominal` (`account_category/category.go:5-13`).

**Hierarchy**: 7 parent accounts (merchant ids `Gg614JldVI2nDA` etc., names "Direct Merchant Balance Account" …) created via `AccountAPI/CreateInBulk{account_data_identifier:"direct_account_x"}` (`account/seed_data/direct_account_x.go:53-189`; names `common/constant.go:702-707`, ids `:779+`), each with `onboarding_events [direct_account_onboarding, direct_merchant_onboarding]` and `entities {fund_account_type:[…], account_type:[…]}`. Merchant sub-accounts via `AccountAPI/CreateOnEvent{merchant_id, events:[{name:"direct_merchant_onboarding", entities:{banking_account_stmt_detail_id:["basd_<14>"]}}], merchant_balance_opening_balance?, merchant_reward_opening_balance?, currency}` (`proto/platform/ledger/account/v1/account_api.proto:15,72-83,92-96`; `account/server.go:138-200` mutex on merchant_id+event+currency; `account/core.go:1430-1500` `createSubAccountsUnderParent`: status = parent status, `entities = parent entities ∪ event entities`, name `"<parent name> - <mid> - <basd id>"` via `direct_merchant_onboarding/core.go:41-51`; `MinBalance = nil` for `direct_merchant_onboarding` (`core.go:1812-1824`, `:1712`) → **DA merchant balance may go negative; ledger is a mirror, not the balance authority (x-balances/BASD gateway_balance is)**. INFERRED from `isSetMinBalanceNull` + twin spec.
Producer of the onboarding call in prod: monolith `Feature/Service.php:640-720` (`ledgerAccountCreateRequestForDirect*`, on feature `da_ledger_journal_writes` assignment `:358`) → `Merchant/Balance/Ledger/Core.php createXLedgerAccountForDirect` (event `direct_merchant_onboarding` `:31`, entity key `banking_account_stmt_detail_id` `:51`, payload `getLedgerAccountCreatePayload` incl. `tenant X, mode, idempotency_key uuid, merchant_id, events[]`, via SNS or Twirp `createAccountsOnEvent`). No other repo calls it (grep). CONFIRMED.

**Discovery at journal time**: `account_discovery/core.go:49-110` replaces `$banking_account_stmt_detail_id` from the journal's `identifiers` (`common.IsVariable` `utils.go:198-200`) and queries `FindByEntitiesAndMerchantID` (`account/repo.go:140-147`: `account_details.entities @> {...}` jsonb + merchant_id + tenant) → `ErrAccountDiscoveryFailure` if absent; the SQS job treats that as a **data error and drops the message without retry** (`job_sqs/journal_create.go:277-283`, together with unique-constraint / validation / not-found).

**Idempotency** (three layers): (1) `journal_create` SQS job: Redis mutex on `idempotency_key`, `FetchOrCreateIdempotency` on table `idempotency(idempotency_key VARCHAR(36), request_hash, entity_id…)` — duplicate key with entity → no-op (`job_sqs/journal_create.go:224-260`; DDL `20210527121244…`, `20221209182605…`); (2) `ValidateJournalExist(transactor_id, transactor_event)` → `ErrRecordAlreadyExist` (`journal/validation.go:82, 351-368`) — application-level only, **no DB unique index** on `journal(transactor_id, transactor_event)` (indexes `journal(transactor_id)`, `journal(id)` `20201001011143…:34-49`; `transactor_id VARCHAR(50)` `20210416152026…`); (3) `validateAPITransactionID` (`:134, 384-399`). PS generates a fresh `uuid.New()` per publish (`payouts/core.go:7826`, `bankingAccountStatement/core.go:766`) so layer (1) never dedups PS re-publishes; layer (2) is the effective guard.

**Conservation**: ledger_config validation enforces Σdebits = Σcredits and the accounting equation per config (`ledger_config/validation.go:285-300`); entries are built from the matched config (`ledger_builder/core.go:40-93`).

**Caller / transport / payload**: SNS topic `api-ledger-journal-create-live` (`payouts/appConstants/constants.go:381`) → SQS `prod-ledger-journal-create-live` (`ledger/config/prod-live.toml:394`; twin queue `journal_create`, `ENV2/seeds/localstack/init-queues.sh`) → `JournalCreate` job. PS struct `dtos/ledger.go:3-20` ≡ ledger `Journal` `job_sqs/journal_create.go:56-75`:
`{tenant:"X", mode:"live", idempotency_key:uuid, merchant_id, currency, amount, base_amount, commission, tax, transactor_id, transactor_event, transaction_date, notes:"{balance_id, transaction_id:\"bas_<basid>\"}", identifiers:"{banking_account_stmt_detail_id:\"basd_<id>\"[,product_id]}", additional_params:"{[fee_accounting:reward][,account_type]}", api_transaction_id?}` (`payouts/core.go:7628-7846`: `transactor_id = payout.GetSignedID()` = `pout_<id>` for processed, `reversal.GetLedgerSignedID()` for reversed; `transaction_date = payout.updated_at` (processed) / `input.transaction_date` (recon); `api_transaction_id = payout.transaction_id` for `da_payout_processed`).

**WHO ACTUALLY POSTS the Direct journal today — three sources, two of them disabled:**
1. **PS** `UpdatePayoutAfterBASRecon` → `SendLedgerEventPostBasLinking` is **commented out** at `payouts/internal/app/payouts/core.go:7284` and `:7356`, with comment `:7209-7217` "Deprecating sending Direct Account/Current Account Event to Ledger as X-Account Statement have Merchant Account Statement and Recon run on those statement" (Slack permalinks `p1770719696…` / `p1770795070…` = 2026-02-10/11). Identical in `$A/payouts`. CONFIRMED. PS still posts **external** DA journals (`da_ext_debit/da_ext_credit`, `transactor_id="bas_<id>"`) from `bankingAccountStatement/core.go:627-663, 735-798` on ART linking to `external` — live.
2. **Monolith** `payoutUpdateByBASRecon` (the XAS-triggered route) has `sendLedgerEventPostBasLinking` **commented out** (`api/app/Models/Payout/Core.php:10327, 10359`, same deprecation comment `:10292-10299`). CONFIRMED.
3. **Monolith statement-fetch linking path is LIVE**: `saveAccountStatementV2` → `sendToLedgerPostSourceEntityProcessing` (`BankingAccountStatement/Core.php:3081-3091`, `:5175-5228`) → `processLedgerPayoutForDirect` (`:5243-5278`; gates: `applications.ledger.enabled`, payout not Shared/primary, not `HIGH_TPS_COMPOSITE_PAYOUT`, merchant feature `da_ledger_journal_writes` (`Feature/Constants.php:1466`)) → `Transaction/Processor/Ledger/Payout.php::pushTransactionToLedgerForDirect` → `pushToLedgerSns`. Also via `Payout/Core.php:10407` (`sendExtToPayoutEventToLedger` `:10003-10016`). **Exception**: for PS-owned payouts with a debit statement when Splitz `payout_service_txn_recon` is on, the monolith skips ledger and calls PS ("we make a call to ps which already does this" `:3081-3083`) — but PS has it commented out → **for those merchants no Direct payout journal is posted by anyone**. INFERRED from the three citations; owner confirmation needed (Unresolved Q1).

**Direct failure must NOT produce a Shared-style reversal journal — guards:**
- PS `HandlePayoutProcessed`: "skip ledger processing for CA payouts / will be done after BAS linking / Only shared banking send processed event" → `if !EqualFold(bankAcc.GetAccountType(), "shared") { return nil }` before enqueuing `LedgerProcessedEventFailureType` (`payouts/core.go:2571-2577`).
- PS DA path refuses Shared and vice-versa: `SendToLedgerPostSourceEntityProcessing`/`ProcessLedgerPayoutForDirect` return `SharedPayoutForDirectAccountLedgerCall` for Shared (`:7548-7555`, `:7648-7654`); feature gate `da_ledger_journal_writes` (`:7656-7661`, `appConstants/features.go:21`).
- Ledger has **no** `da_payout_failed`/`da_payout_initiated` config; a `payout_failed`/`payout_reversed` (Shared) journal requires discovery by `banking_account_id`, which a DA merchant does not have (`shared_account_x.go:58`) → would fail discovery. INFERRED.
- Direct FTS FAILED maps to payout `failed` (no reversal row) per `TWIN_SPEC/acceptance-invariants.yaml:66` and `expected-failures.yaml:153` (claims already verified by V08/V18 in M3.1; V08 asserts zero journal delta for M2 — `ENV2/verifier/verifiers/test_v08_ledger_accounting_failed_direct.py:6-15`). The only Direct "reversal" journal is `da_payout_reversed(+_recon)`, emitted **only on a matched credit statement** (monolith `Core.php:5213-5227`; PS commented). CONFIRMED.

Comparison with the Shared journals the twin already exercises (V05-V07: `payout_initiated`, `payout_processed`, `payout_reversed`, `payout_failed`, transactor `pout_<id>`/`rvrsl_<id>`, `helpers/payouts_flow.py:117-171, 315-318`): Shared journals are FTS-status-driven and posted by PS synchronously (`initiated` at create) or via `payout_update_failure_handling` queue; Direct journals are statement-driven, keyed by `banking_account_stmt_detail_id`, carry `notes.transaction_id = bas_<id>`, use two journals per success, and today are posted by the **monolith**, not PS.

### Finding 7 — Minimum ledger records for a synthetic Direct merchant (so `da_payout_processed` + `_recon` can post)

Prerequisites in tenant `X` (Postgres `ledger`):
1. **Ledger config** `direct_account_x` (already loaded in twin by `ENV2/scripts/up.sh:149-156` via `LedgerConfigAPI/CreateInBulk{ledger_config_data_identifier:"direct_account_x"}`; rows in `ledger_config`, `UNIQUE(tenant, transactor_event_name)` `20210810170948…:33`).
2. **Parent accounts** — 6 needed for the two success journals (Balance, VendorPayable, CommissionReceivable, CommissionIncome, OutputGST, MerchantCash); simplest is the real seed: `POST /twirp/rzp.ledger.account.v1.AccountAPI/CreateInBulk {"account_data_identifier":"direct_account_x"}` with header `Ledger-Tenant: X` (`account/server.go:719`; seed `account/seed_data/direct_account_x.go:55-58`). **Not done in the twin today** (`ENV2/seeds/generated/s4/ledger.sql` has no `merchant_da` rows; only M2's FTS current receivable/payable pair `:72-82`).
3. **Merchant sub-accounts** — real path: `POST /twirp/rzp.ledger.account.v1.AccountAPI/CreateOnEvent {"merchant_id":"<mid14>","events":[{"name":"direct_merchant_onboarding","description":"…","entities":{"banking_account_stmt_detail_id":["basd_<14>"]}}],"currency":"INR","merchant_balance_opening_balance":"<paise>"}` headers `Ledger-Tenant: X`, `idempotency-key: <uuid>`. Creates one sub-account per parent carrying `direct_merchant_onboarding`.
   Resulting column constraints (from migrations): `accounts(id CHAR(14), merchant_id CHAR(14), status VARCHAR(20)='ACTIVATED', balance NUMERIC(20,6), min_balance NULL (DA), negative_balance NULL, created_at/updated_at INTEGER)` (`20201001011117…`); `account_details(id CHAR(14), account_id CHAR(14) UNIQUE, account_name VARCHAR(255) UNIQUE where deleted_at IS NULL, merchant_id, parent_account_id CHAR(14), currency 'INR', account_category ∈ asset|liability|revenue, business_category ∈ real|nominal, entities jsonb = {"account_type":["payable"],"fund_account_type":["merchant_da"],"banking_account_stmt_detail_id":["basd_<14>"]}, tenant 'X')` (`20201022184734…`, `20210826041040…`).
4. **Journal inputs**: `identifiers` must contain exactly the `basd_<14>` used at onboarding; `merchant_id` must equal the sub-account merchant_id; `amount/commission/tax` non-negative strings; `transactor_id` ≤ 50 chars; unique `(transactor_id, transactor_event)`.
5. Merchant feature `da_ledger_journal_writes` on (PS reads API-DB `features` table + DCS: `payouts/internal/app/merchant/core.go:482-501`; twin apidb seed only has `in_flight_reservation_enabled` `ENV2/seeds/generated/s4/apidb.sql:5`).

### Finding 8 — Minimal synthetic bank-statement record(s) and the exact injection point for the *real* code path

**Real PS path (RBL worker → Mozart)** — inject at Mozart, not at the DB:
- Mozart-sim must implement `POST /razorpayx/rbl/v2/account_statement` (route shape `/{namespace}/{gateway}/{version}/{action}` per `ENV2/substitutes/mozart-sim/server.py:4`) accepting `RblRequest` and returning Mozart envelope `data = {"FetchAccStmtRes":{"Header":{"Status":"Success","Status_Desc":"","TranID":"<attempt.id>","Corp_ID":"<corp>","account_no":"<acct>","from_date":"…","to_date":"…","next_key":"<opaque>"},"AccStmtData":{"File_Data":"<base64 CSV>"}}}`.
- CSV (exact header + one debit row for a processed Direct payout):
  `TRAN_ID,PTSN_NUM,TRAN_DATE,PSTD_DATE,TRAN_TYPE,C/D,TRAN_PARTICULAR,TRAN_AMT,TRAN_BALANCE`
  `S12345678,1,07-09-2026,07-09-2026 10:15:00,TCI,D,<UTR>-IMPS/PAYOUT RZP<GRN10>,50.00,99950.00`
  Constraints: `TRAN_ID` non-empty (→ `bank_transaction_id` varchar(255)); `PTSN_NUM` numeric; posted date must be `< now − 60 s` else the row is skipped; amount in rupees with decimals (→ paise); for IMPS debit the UTR is everything before the first `-` (12 digits for RBL IMPS credits), NEFT/RTGS `NEFT/<UTR>/…`, UPI `UPI/<UTR>/…`; GRN via trailing ` RZP<10 alnum>`; `TRAN_TYPE` in the category map or falls to `others`.
- Resulting `banking_account_statement` row (PS MySQL): `id char(14) (auto), merchant_id=<BASD.merchant_id>, entity_id NULL, entity_type NULL, channel 'rbl', account_number, bank_transaction_id 'S12345678', type 'debit', utr '<UTR>', amount 5000, currency 'INR', description, category 'customer_initiated', bank_serial_number '1', balance 9995000, balance_currency 'INR', transaction_date <unix midnight IST>, posted_date <unix>, gateway_ref_number '<GRN10>', created_at/updated_at`.
- Preconditions: (i) a `banking_account_statement_details` row `{id: <14>, merchant_id, balance_id: <M's balance>, account_number, channel:'rbl', status:'active', account_type:'direct', gateway_balance, statement_closing_balance, metadata:{"statement_fetched_till_date":"DD-MM-YYYY"}}` (twin has none; monolith normally inserts it); (ii) `bankingaccounts-stub` must serve `GET /merchant/{mid}/banking_account_by_account_number/{acct}/credentials` returning `{credentials:{auth_username, auth_password, client_id, client_secret, corp_id}}` (today only the shield route exists, `ENV2/substitutes/bankingaccounts-stub/server.py:45-46`); (iii) PS arena config must define `[job].rbl_banking_account_statement` (absent from `ENV2/generated/payouts/arena.toml:399-423` → `Job.RblBankingAccountStatement` is empty, `payouts/internal/config/config.go:275`), an SQS queue of that name (`init-queues.sh`), and a worker container `PAYOUTS_WORKER_NAME=rbl_banking_account_statement`; (iv) trigger `POST /v1/cron/banking_account_statement/fetch/initiate {"channel":"rbl"}` with the FastCron Basic credential (add to `cron-driver` `JOBS`, `ENV2/scripts/cron-driver/driver.py:37-45`).
- **Matching after ingestion (real PS path)**: PS does not auto-match. The real instruction path is ART → monolith `banking_account_statement/batch/process` → PS `POST /v1/banking_account_statement/process/batch` with `[{"data": base64({"banking_account_statement_id":"<bas14>","recon_status":"Reconciled","entity_id":"x,y,<pout14>","entity_type":"payout"}), "idempotent_id":"<any>"}]` (auth `cred.API` or `cred.Workflow`). Effect: BAS row linked, PS → monolith-stub `payout_update` → relayed back to PS `UpdatePayoutAfterBASRecon` → `payouts.transaction_id = <bas14>`, mail/SMS; **no ledger journal** (deprecated in PS). To exercise the ledger leg the twin must either (a) reproduce the monolith's `sendToLedgerPostSourceEntityProcessing` in the monolith-stub (posting the two SNS journal messages with the payload of Finding 6), or (b) accept that the real PS code posts nothing for Direct payouts and assert that.
- **XAS-shaped substitute record** (if the twin models XAS instead of PS ingestion): `accounts{id14, status:'active', merchant_id, account_number, account_type:'direct', channel:'RBL' (XAS channel enum casing per `xas/internal/enum/channel`, not re-verified), balance_id, statement_metadata}` (`UNIQUE(account_number, channel)`), `account_statements{…same as PS BAS plus fetched_at_nano bigint}`, and the inbound `source_events` message from PS (Finding 3). Match succeeds when `utr` (or `gateway_ref_number`/`bank_transaction_id`) equals the payout's, `amount` equals payout amount, and `accounts.balance_id == payout.balance_id`. Then substitute POSTs monolith-stub `/v1/banking_account_statement/payout_update` with the 9-field body (monolith-stub relay already accepts extra fields, `server.py:743-764`).
- ID/format cheat-sheet: payout `pout_<14>`; BAS `bas_<14>`; BASD `basd_<14>`; XAS/PS `event_id` 14 chars (XAS column varchar(64)); UTR varchar(255) (12-digit for IMPS); `gateway_ref_number` varchar(48) (PS/XAS statements) vs `gateway_reference_number` varchar(50) (XAS source_events); amounts `bigint unsigned` paise; currency `char(3)='INR'`; dates unix seconds `int`.

### Finding 9 — Twin presence (ENV2_COMPOSE/docker-compose.yml @ 3a044f8)

| Worker / service (prod) | Twin | Buildable from clone? | Notes / what a substitute must preserve |
|---|---|---|---|
| PS `payouts-api` + 14 workers (`webhook_event, queued_payout, schedule_payout, on_hold_payout, fts_async_processing, fts_async_hv_processing, payout_create_failure_handling, payout_update_failure_handling, transaction_create, generic_processing, async_dual_write, x_balances_balance_refresh, payout_source_updater, partner_bank_hold_payouts`) | PRESENT | REAL | `docker-compose.yml:482-859` |
| PS worker `rbl_banking_account_statement` | **MISSING** (no container; config key absent; no queue) | REAL — same binary, add `PAYOUTS_WORKER_NAME`, `[job].rbl_banking_account_statement`, queue, worker container | Needs mozart-sim `account_statement`, bankingaccounts-stub credentials route, BASD rows, cron trigger |
| PS producer `x_account_statement_source_event` | MISSING config key (`arena.toml [job]` lacks it) and queue; experiment off by default (`splitz-stub` default variant `off`, `server.py:39`; env `PAYOUTS_CONFIGS_ACCOUNT_STATEMENT_SOURCE_EVENT_*` unset) | REAL (config + queue only) | Queue must be named identically in PS and the XAS substitute |
| PS routes `/v1/cron/banking_account_statement/fetch/initiate`, `/v1/banking_account_statement/process/batch`, `/v1/payouts/banking_account_statement/payout_update`, `/v1/dev_admin/banking_account_statement/link`, `GET /v1/banking_account_statement` | PRESENT in binary (real routes) but unexercised; `cron-driver` has no BAS job | REAL | `driver.py:37-45` |
| ledger `journal_create` worker | PRESENT (`ledger-worker-journal-create`, queue `journal_create`) | REAL | `direct_account_x` ledger_config loaded (`up.sh:151`); **DA parent/sub accounts NOT seeded** |
| ledger `AccountAPI/CreateInBulk direct_account_x` + `CreateOnEvent direct_merchant_onboarding` | MISSING (not invoked in `up.sh`; generator emits no DA rows, `seeds/generator/generate.py:41-42` seeds only shared) | REAL (two Twirp calls) | Must run after ledger-api health, before Direct journals |
| XAS service (fetch workers, enrichment, source-event consumer, dual-write, HTTP API) | MISSING — `xas-sink` is health-only (`substitutes/xas-sink/server.py` 17 lines; `CONTRACT.md`) | Go/Foundation service, builds against private modules already resolvable (`goutils`, `proto`); needs MySQL + SQS; **Kafka CDC dual-write not reproducible** without Maxwell → use `POST /v1/statement/dual_write` hook instead; ES optional (`FeatureFlags.SyncElasticsearchDoc`) | If substituted, preserve: source-event JSON schema (incl. the `event_create_timestamp` mismatch), `(event_id, entity_type)` dedup, match keys (utr→grn→cms + balance/account + amount), external marking, ≤2/dual-type guards, outbound 9-field `payout_update` POST, `fetch_multiple_by_reference_numbers` GET semantics (type/channel filter) used by PS `VerifyPayoutFailedTransaction` and FTS retry-preprocessor |
| fts `retry-transfer-preprocessor` | PRESENT (`fts-worker-retry-transfer-preprocessor`) but `[xas].host=http://xas-sink:8080` → every XAS call errors (`CodeXASFetchAccountStatementByMultipleReferencesRequestFailed`, `retry_preprocessor_service.go:310-348`) | REAL | Needs XAS substitute GET route |
| Monolith BAS module (RBL fetch jobs, `saveAccountStatementV2` matcher, `payoutUpdateByBASRecon`, `batch/process` front route, `source_event_info`, DA ledger emitter, BASD creation, PS DB dual-write) | MISSING except `payout_update` relay (`monolith-stub/server.py:743-764`) | PHP monolith — NOT buildable in twin scope | Substitute must preserve: relay semantics (done), `batch/process` pass-through to PS, `source_event_info` GET (14-field body), BASD row insertion into PS DB, and — if Direct ledger fidelity is wanted — the two-journal SNS emission with the payload/gates of Finding 6 |
| mozart RBL v2 `account_statement` | MISSING in `mozart-sim` (only transfer actions, `server.py:59-151`) | Mozart is Node/PHP-ish config engine — not buildable (BOM blocker) | Substitute must preserve request shape, Success/Failure+"No Records Found" semantics, base64 CSV, next_key paging, 60 s posted-date skip |
| banking-accounts credentials route | MISSING (`bankingaccounts-stub` serves only `/payouts/shield/merchant/`) | Go service buildable, but a one-route stub suffices | Return 5 credential keys |
| recon/ART dispatcher | MISSING | Python/Spark — substitute (already recommended in lane 11) | Only needs to issue `batch/process` bodies |
| ledger-gate | PRESENT (proxy; forwards `idempotency-key`) | — | `substitutes/ledger-gate/CONTRACT.md:9-15` |
| SQS queues for BAS/XAS | MISSING (`init-queues.sh` has none of `rbl_banking_account_statement`, `x_account_statement_source_event`, XAS queues) | trivial | |

### Finding 10 — Contradictions with earlier documents

| # | Claim | Source | What source code says | Status |
|---|---|---|---|---|
| C-T03-1 | "Ingestion (XAS)… all bank calls go through Mozart"; "XAS owns matching, not the monolith and not PS directly" | `reports/fidelity/raw/11_statements_recon_repair.md` §1 | PS has a complete RBL statement ingestor (`payouts/internal/app/bankingAccountStatement/*`, worker `rbl_banking_account_statement`) and the monolith has its own (`api/app/Jobs/RblBankingAccountStatement.php`, `Core.php:3043-3140` matcher). XAS is one of three. Lane 11 mis-scoped. | CORRECTED (Finding 1, 4) |
| C-T03-2 | "Direct/current accounts are accounted via direct_account_x keyed by bank statement rows" / "direct_account_x accounting keyed by banking_account_stmt_detail_id" | `TWIN_SPEC/architecture.yaml:27,61`; `route-matrix.yaml:74` | True for ledger configs, but PS never posts them (commented `payouts/core.go:7284,7356`) and the XAS-triggered monolith route also doesn't (`Payout/Core.php:10327,10359`). Only the monolith's own statement-fetch linking posts DA journals (`BankingAccountStatement/Core.php:5175-5228`). | SHARPENED |
| C-T03-3 | "XAS queue sink — present because payouts/ledger/FTS enqueue statement-relevant events that a real XAS would consume" | `ENV2/substitutes/xas-sink/CONTRACT.md` | Only PS enqueues to XAS (one queue), only for Direct accounts with the `account_statement_source_event` experiment on. Ledger and FTS never enqueue to XAS (FTS *calls* XAS over HTTP). | CORRECTED |
| C-T03-4 | "xas 12 queues" | `reports/INITIAL_PAYOUTS_ENVIRONMENT_BOM.md:29`; `PAYOUTS_ARCHITECTURE.md:76` | XAS `[Job]` has 13 keys = 11 SQS queues + 2 Kafka CDC topics (`xas/config/prod.toml:99-112`). | MINOR |
| C-T03-5 | components.yaml lists XAS repo as "x-account-statements, x-bank-statement-sync" | `TWIN_SPEC/components.yaml:177` | `x-bank-statement-sync` is an empty repo (README + CI). | CORRECTED |
| C-T03-6 | runtime-topology says only the *worker container* for `rbl_banking_account_statement` is missing | `TWIN_SPEC/runtime-topology.yaml:87`; `components.yaml:15` | The arena config also lacks the `[job].rbl_banking_account_statement` **and** `x_account_statement_source_event` keys and the queues; the binary would dispatch to an empty queue name. | SHARPENED |
| C-T03-7 | "Direct… ledger processed/reversed skipped in PS ('after BAS linking')" implying it happens later in PS | `TWIN_SPEC/architecture.yaml:61` | The "later" PS step is deprecated; nothing in PS posts it. | SHARPENED |
| C-T03-8 | Lane 11 twin comparison: "`UpdatePayoutAfterBASRecon` (PS mirror) … MISSING in twin" | lane 11 twin table | The PS route is real and present in the twin binary; only the monolith-side caller and XAS are missing; monolith-stub already relays it. | CORRECTED |
| C-T03-9 | synthetic-data-model: "Direct skips per-merchant ledger accounts" | `TWIN_SPEC/synthetic-data-model.yaml:52` | Correct for PS code; in prod, DA merchants with `da_ledger_journal_writes` do get 7 ledger sub-accounts via monolith onboarding. Twin M2 therefore cannot receive any DA journal even if one were emitted (discovery failure → dropped). | SHARPENED |
| C-T03-10 | PS→XAS event field `event_created_timestamp` vs XAS `event_create_timestamp` | (new) | Production schema mismatch; XAS persists 0. | NEW |

---

## 3. Table of tables / columns / ID formats

| Store | Table | Key columns (type) | ID / format | Writer(s) | DDL |
|---|---|---|---|---|---|
| PS MySQL | `banking_account_statement` | `id char(14) PK, entity_id char(14), entity_type varchar(64) ∈ payout\|payout_reversal\|external, channel varchar(32) ('rbl'), merchant_id char(14), account_number varchar(48), bank_transaction_id varchar(255), type varchar(32) ∈ debit\|credit, utr varchar(255), amount bigint unsigned (paise), currency char(3)='INR', description/category varchar(255), bank_serial_number varchar(128), balance bigint, balance_currency char(3), transaction_date int, posted_date int, gateway_ref_number varchar(48), created_at/updated_at int`; idx on account_number, created_at, updated_at, merchant_id, bank_transaction_id, entity_id, utr | `bas_<14>` (signed) | PS RBL worker; PS `process/batch`, `dev_admin/link` (entity cols) | `payouts/internal/database/migrations/20230128010157…` |
| PS MySQL | `banking_account_statement_details` | see Finding 2 | `basd_<14>` | **monolith** (direct insert into PS DB); PS updates | `…20230128010223…` |
| PS MySQL | `payouts.transaction_id` | `= bas id` after linking | | PS `UpdatePayoutAfterBASRecon` | |
| Monolith MySQL | `banking_account_statement`, `banking_account_statement_details`, `banking_account_statement_pool_rbl`, `external`, `transactions`, `reversals` | mirror of the above + `last_reconciled_at`, transaction linkage | same | monolith BAS jobs, PS dual-write | (PHP entities) |
| XAS MySQL | `account_statements` | as PS BAS + `fetched_at_nano bigint`; idx incl. (utr, account_number), (account_number, posted_date) | `char(14)` | XAS fetch service | `xas/…/20240702122008…` |
| XAS MySQL | `accounts` | `id char(14), status, merchant_id, account_number varchar(48), account_type default 'direct', channel varchar(32), balance_id char(14), statement_metadata json`; `UNIQUE(account_number, channel)` | | XAS `new_merchant_onboarding` job / `POST /v1/accounts` | `…20240702161426…` |
| XAS MySQL | `source_events` | Finding 3; `UNIQUE(event_id, entity_type)` | `event_id varchar(64)` (PS sends 14) | XAS `source_processing` | `…20241029152308…` |
| XAS MySQL | `banking_account_statement` (read-only fallback copy of monolith table; fallback now disabled) | | | migration job | `xas/internal/database/model/banking_account_statements.go:10` |
| Ledger PG | `accounts` | `id CHAR(14) PK, merchant_id CHAR(14), status VARCHAR(20), balance/min_balance/negative_balance NUMERIC(20,6) NULL` | 14-char | `AccountAPI` | `20201001011117…` |
| Ledger PG | `account_details` | `account_id UNIQUE, account_name UNIQUE(live), merchant_id, parent_account_id, currency CHAR(3), account_category, business_category, entities jsonb (gin), tenant VARCHAR(50)` | | `AccountAPI` | `20201022184734…`, `20210826041040…` |
| Ledger PG | `journal` (partitioned) | `id CHAR(14), merchant_id, amount/base_amount NUMERIC(20,6), currency, transactor_id VARCHAR(50), transactor_event VARCHAR(60), transaction_date INT, tenant VARCHAR(50)`; idx transactor_id, id, merchant_id, created_at; **no unique (transactor_id, transactor_event)** | `transactor_id` = `pout_<14>` / `rvrsl_<14>` / `bas_<14>` | `journal_create` job | `20201001011143…`, `20210416152026…`, `20210925113658…`, `20210826041041…` |
| Ledger PG | `ledger_entries` (partitioned) | `journal_id, account_id, amount, base_amount, type ∈ debit\|credit, balance (added), balance_updated` | | same | `20201001011149…`, `20210628101139…`, `20211020100711…` |
| Ledger PG | `ledger_entry_details` | `journal_id, ledger_entry_id, notes jsonb` | | same | `20201001011240…` |
| Ledger PG | `idempotency` (partitioned) | `idempotency_key VARCHAR(36), request_hash VARCHAR(64), entity, entity_id` | uuid | job | `20210527121244…`, `20221209182605…` |
| Ledger PG | `ledger_config` | `UNIQUE(tenant, transactor_event_name)` | `XDAPayoutProcessed` … | `LedgerConfigAPI/CreateInBulk` | `20210810170948…:33` |

## 4. Queue / topic / event table

| Name (prod) | Twin name | Driver | Producer → Consumer | Payload | Dedup / retry |
|---|---|---|---|---|---|
| `prod-payouts-rbl-banking-account-statement-live` | **absent** (key missing) | SQS | PS BASD core → PS worker `rbl_banking_account_statement` | `StatementFetchForAccountNumberRequest` | MaxRetries 7, timeout 1800 s, Redis mutex per account |
| `prod-x-account-statement-source-event` | **absent** | SQS | PS `PushXasSourceEventAndSyncEs` → XAS `source_processing` | `XasEventDetails` (raw JSON) | PS MaxRetries 5; XAS MaxRetries 3; dedup `(event_id, entity_type)` |
| `prod-x-account-statements-fetch-sqs` (+ ybl/idfc/axis/icici/slice) | absent | SQS | XAS `fetch/initiate` → XAS fetch worker | account fetch params | per-channel |
| `prod-x-account-statements-save-and-enrich-sqs`, `…-enrichment-sqs` | absent | SQS | XAS fetch → enrichment | `{statement_id}` | |
| `mysql_cdc_events_prod_rx_account_statements_account_statements` / `…_accounts` | absent | Kafka (CDC) | Maxwell → XAS `dual_write` → monolith `payout_update` | CDC row | not reproducible; use `POST /v1/statement/dual_write` |
| `prod-x-account-statements-account-activation-event` | absent | SQS | (banking-accounts, UNKNOWN publisher) → XAS `new_merchant_onboarding` | onboarding model | |
| `api-ledger-journal-create-live` (SNS) → `prod-ledger-journal-create-live` (SQS) | topic absent; queue `journal_create` present | SNS→SQS | PS (`SNSTopicLedgerJournalCreateLive`), monolith `pushToLedgerSns` → ledger `journal_create` | `Journal` JSON (Finding 6) | idempotency table + `ValidateJournalExist` |
| `prod-payouts-async-dual-write-live` | `payouts-async-dual-write` | SQS | PS BAS model hook → monolith dual-write | `{bas_id, entity:"bas"}` | |
| `prod-api-payout-source-updater-live` | `payout-source-updater` | SQS | PS status events (carries empty `banking_account_stmt_detail_id`) | `PayoutStatusUpdateEvent` | |
| monolith `rbl_banking_account_statement_fetch`, `banking_account_statement_processor`, `banking_account_statement_source_linking` | absent | SQS (Laravel) | monolith crons → monolith jobs | | kube replicas 3/5/1 |

## 5. Worker table (present / missing / buildable)

See Finding 9 table. Summary: PRESENT = PS 14 workers, ledger `journal_create`, fts `retry-transfer-preprocessor` (but XAS-blind); MISSING-REAL-BUILDABLE = PS `rbl_banking_account_statement` (+ config/queue/cron trigger), ledger DA account seeding (2 Twirp calls); MISSING-SUBSTITUTE = XAS (or REAL Go build without CDC), mozart RBL `account_statement`, banking-accounts credentials, monolith BAS module (BASD creation, matcher, DA ledger emitter, `batch/process` front), recon/ART dispatcher.

## 6. Minimum synthetic record sets

**S-A. Direct merchant (per merchant M):**
- payouts: `banking_accounts` row (already in twin for M2), `banking_account_statement_details` row `{id:<14>, merchant_id:M, balance_id:<M bal>, account_number:<acct>, channel:'rbl', status:'active', account_type:'direct', gateway_balance:<paise>, statement_closing_balance:<paise>, gateway_balance_change_at:<ts>, statement_closing_balance_change_at:<ts>, balance_last_fetched_at:0, metadata:{"statement_fetched_till_date":"<DD-MM-YYYY>"}}`.
- apidb-stub `features`: `(entity_id:M, entity_type:'merchant', name:'da_ledger_journal_writes')`.
- ledger: `AccountAPI/CreateInBulk{direct_account_x}` once; `AccountAPI/CreateOnEvent{merchant_id:M, events:[{name:'direct_merchant_onboarding', entities:{banking_account_stmt_detail_id:['basd_<id>']}}], currency:'INR'}` per M.
- x-balances/XAS accounts (only if XAS is modelled): `accounts{account_number, channel, balance_id, merchant_id, account_type:'direct', status:'active'}`.

**S-B. One Direct payout + statement (success):** payout `pout_P` processed with `utr=U`, `amount=A`, `gateway_ref_no=G`; statement row as in Finding 8 with `utr=U, amount=A, type='debit', account_number=<acct>, channel='rbl', gateway_ref_number=G`; source event `{entity_id:P, entity_type:'payout', utr:U, event_id:<14>, status:'processed', mode, amount:A, balance_id:<M bal>, gateway_ref_no:G, cms_ref_no:''}`.

**S-C. Return credit (failed → reversed):** second statement row `type='credit', utr=U (or return UTR), amount=A`; XAS/monolith links `payout_reversal` → `payout_update{entity_type:'payout_reversal'}` → PS creates reversal + `reversed`.

**S-D. Ledger journals expected (if the monolith emitter is substituted):** `da_payout_processed{transactor_id:'pout_P', identifiers:{banking_account_stmt_detail_id:'basd_<id>'}, notes:{balance_id, transaction_id:'bas_<bas id>'}, api_transaction_id:'<bas id>'}` then `da_payout_processed_recon` (same transactor_id). Entries per Finding 6.

## 7. Contradictions — see Finding 10 (C-T03-1 … C-T03-10). Recommend adding C-T03-1/2/3/6/8 to `reports/implementation/m4-contradictions.md`.

## 8. Unresolved questions

1. **Which component posts Direct (DA) ledger journals in production today** for PS-owned Direct payouts under `payout_service_txn_recon`: code shows none (PS commented, monolith defers). Needs owner confirmation (Slack threads `CNXASR0H3/p1770719696465979`, `p1770795070793089`). Also whether `da_ledger_journal_writes` is still assigned to live merchants.
2. **Which RBL fetcher is active** (PS worker vs monolith `RblBankingAccountStatement` vs XAS) and the FastCron cadence for `/v1/cron/banking_account_statement/fetch/initiate` (PS), `banking_account_statement/process` (monolith), `/v1/banking_account_statement/fetch/initiate` (XAS). kube shows all three fleets deployed.
3. Rollout of Splitz `account_statement_source_event` (P9jMHiqpMI81A5), `xas_source_event_utr_and_grn_match_experiment` (RZfCkJYmCWWCPG), `payout_service_txn_recon`, and env whitelist `PAYOUTS_CONFIGS_ACCOUNT_STATEMENT_SOURCE_EVENT_*`.
4. Publisher of `prod-x-account-statements-account-activation-event` (not found in banking-accounts/api by grep).
5. XAS channel enum casing (`RBL` vs `rbl`) for `accounts.channel` — not re-verified this pass (`xas/internal/enum/channel`).
6. Whether `ProcessBankingAccountStatementForPayoutID` (PS UTR auto-linker with stub `Link()`) is wired to any job (no caller found besides interface) — appears dead.
7. App-name behind XAS `APISERVICE_AUTH_USERNAME` in the monolith (INFERRED `payouts_service`).

## 9. Confidence

- Findings 1(a), 2, 3, 4(a-c), 5, 6 (configs, payload, guards, deprecations), 9: **HIGH** (direct source reads, line-cited).
- Finding 1(b)/(c) cadence and "who is live": **LOW** (kube proves deployment only).
- Finding 6 "nobody posts DA journal for PS-recon merchants": **MEDIUM** (three code facts, no runtime evidence).
- Finding 7/8 record sets: **HIGH** for schema/constraints; **MEDIUM** for XAS channel casing and for `TRAN_TYPE`/description conventions beyond the regexes.

## 10. Handoff instructions for the implementer (Phase 3, Direct + statement + reconciliation)

1. **Do not fake the Direct journal from PS.** The real PS binary will not emit `da_payout_*` for payouts (commented code). Any twin assertion "Direct success posts `da_payout_processed`" must be attributed to a **monolith substitute** behaviour, gated exactly as `processLedgerPayoutForDirect` (`applications.ledger.enabled`, not shared, feature `da_ledger_journal_writes`, not HIGH_TPS), emitting two SNS/SQS messages with the Finding 6 payload; and it must be skipped when `payout_service_txn_recon` is on for the merchant (then nothing posts — replicate the gap, and register it as an expected-failure/observation, not a defect of the twin).
2. **PS ingestion (REAL)**: add `[job].rbl_banking_account_statement = "rbl_banking_account_statement"` and `x_account_statement_source_event = "x_account_statement_source_event"` to `config/templates/base/payouts/arena.toml [job]`; create both queues in `seeds/localstack/init-queues.sh`; add container `payouts-worker-rbl-banking-account-statement` (`PAYOUTS_WORKER_NAME=rbl_banking_account_statement`); add cron-driver job `bas_fetch_initiate → POST /v1/cron/banking_account_statement/fetch/initiate {"channel":"rbl"}`; seed BASD rows for Direct merchants (Finding 6/S-A) via the seeds generator; extend `bankingaccounts-stub` with the credentials route; extend `mozart-sim` with `razorpayx/rbl/v2/account_statement` (scenario table keyed by account_number: rows to emit, `No Records Found`, next_key, failure).
3. **Matching**: choose one of (a) XAS substitute consuming `x_account_statement_source_event` + holding statements (fed either by PS BAS table read-through or by its own mozart-sim fetch), matching per Finding 4(a), POSTing `payout_update` (9 fields) to monolith-stub; or (b) REAL XAS build (Go; MySQL; SQS; ES off; dual-write via `POST /v1/statement/dual_write` from a small "CDC-shim" that watches `account_statements.entity_type` changes). Either way turn on PS experiment via `PAYOUTS_CONFIGS_ACCOUNT_STATEMENT_SOURCE_EVENT_ENABLED_ENV=true` (whitelist Direct merchants) and set XAS `[xas].host` for PS and FTS to the substitute so `VerifyPayoutFailedTransaction` and the FTS retry-preprocessor gate become real.
4. **ART path (REAL PS route)**: exercise `POST /v1/banking_account_statement/process/batch` directly (auth `cred.API`) to prove linking + `payout_update` relay + `transaction_id` set; assert **no** journal from PS; assert `da_ext_debit` journal when `recon_status=Unreconciled` (real PS external path) — this needs the DA ledger accounts (item 5) or it will be dropped as `ErrAccountDiscoveryFailure`.
5. **Ledger DA accounts**: in `scripts/up.sh` after `LedgerConfigAPI/CreateInBulk`, call `AccountAPI/CreateInBulk{"account_data_identifier":"direct_account_x"}` then, per Direct merchant, `AccountAPI/CreateOnEvent` with `direct_merchant_onboarding` and the merchant's `basd_<id>`; verify `account_details.entities @> {"banking_account_stmt_detail_id":["basd_…"]}` exists for the 6 needed roles.
6. **Verifiers to add**: (i) statement-before-failed guard: debit statement present → FTS `failed` webhook returns error and payout stays `processed/initiated`; (ii) credit statement → `failed` payout becomes `reversed` with reversal `transaction_id=bas id`; (iii) dedupe: replaying the same bank CSV yields no new BAS rows (tuple in Finding 1); (iv) source-event dedup on `(event_id, entity_type)`; (v) `da_payout_processed` + `_recon` exist with balanced entries iff monolith-substitute path enabled; V08 remains valid (Direct failure → zero journals).
7. Record C-T03-* in `m4-contradictions.md`; add L-00x for "monolith DA ledger emitter substituted" and "XAS CDC not reproduced".

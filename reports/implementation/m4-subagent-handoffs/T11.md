# T11 — XAS Substitute and Ledger Direct-Accounting Engineer (Milestone 4)

Task ID: **T11**. Live arena `env2_compose` (66+ containers, kong 18080, enforcement=1), branch
`milestone-4-direct-reconciliation`, nothing committed (coordinator commits). Written 2026-09-07.

Fidelity labels: **real** = accepted binary executed the step; **substitute** = a stub in
`ENV2_COMPOSE/substitutes` executed it under a written contract; **substitute_injected** = the harness
inserted the row/event (always in the exact shape the real producer would have written).

## 1. Inspected files

| Repo / tree | Files (line ranges read) |
|---|---|
| `reports/implementation/m4-subagent-handoffs/T03.md` | Findings 3, 4(a), 4(d), 5, 6, 7, 8, 9; §10 items 1, 3, 5, 6 |
| `reports/implementation/m4-decisions.md` | D-006, D-007 |
| `x-account-statements@e73fd5a` (`$R/x-account-statements`) | `internal/account_statements/enrich/service/{service.go (all), params.go, enrichers/payout_enricher.go}`, `internal/account_statements/repo/repo.go:255-345`, `internal/account_statements/dual_write/service/service.go:100-290`, `internal/gateway/api/{params.go:30-60, service/service.go:25-135}`, `internal/account_statements/get/service/service.go:395-485`, `internal/account_statements/get/validate/validate.go:174-215`, `internal/source_events/{model/dto.go, service/service.go:50-160}`, `rpc/.../statement_api.pb.gw.go` (route path) |
| `proto@5268257` | `x/x-account-statements/statement/v1/{statement_api.proto, statement.proto:67-110}`; `ledger/proto/platform/ledger/account/v1/account_api.proto:1-135` |
| `payouts@4bf3dbf` / `$A/payouts` (byte-identical for every file read: `accountStatement/core.go`, `fts_transfer_status_webhook.go`, `payouts/core.go`) | `internal/app/payouts/core.go:1768-1882, 7203-7400, 7540-7850, 9800-9890`, `internal/app/payouts/fts_transfer_status_webhook.go:120-175, 380-475, 590-760`, `internal/app/accountStatement/core.go:123-160, 447-640`, `internal/app/dtos/{payout_update_bas_entity_request.go, transfer_status_webhook_request.go}`, `internal/job/x_account_statement_source_event.go:30-100`, `internal/routing/router/payout_internal_routes.go:16,140-165`, `internal/app/common/appConstants/states.go:37-80`, `pkg/xAccountStatement/fetch_by_multiple_references.go`, `pkg/api/bas_recon_payout_update.go:14,61`, `pkg/sns` (PublishMessage), `internal/app/reversals/model.go:30,100` |
| `fts@2a09e76` | `internal/providers/xas/{fetch_by_multiple_references.go, base.go, config.go}` (response struct with string int64s) |
| `ledger@471ff4d` / `$A/ledger` | `internal/job/job_sqs/{journal_create.go (all), base.go:140-160}`, `internal/account/seed_data/direct_account_x.go:40-200`, `internal/account/onboarding/direct_merchant_onboarding/core.go`, `internal/account/{status.go, core.go:331,871, server.go:545-560}`, `internal/journal/validation.go`, `internal/account_discovery/core.go` (no status gating) |
| `api@2d665f9` (monolith) | `app/Models/BankingAccountStatement/Core.php:3070-3095, 3530-3565, 5175-5290`, `app/Models/Transaction/Processor/Ledger/Payout.php:235-425, 696-830`, `app/Models/Transaction/Processor/Ledger/Base.php:180-218`, `app/Http/Route.php:4837` |
| `goutils` | `worker/message.go` (raw-message Load/GetJob) |
| Twin | `ENV2_COMPOSE/substitutes/{_common/base_stub.py,_common/faults.py,monolith-stub/*,ledger-gate/*,xas-sink/*,splitz-stub/server.py:1-60,Dockerfile}`, `docker-compose.yml` (monolith-stub, xas-sink, ledger-worker-journal-create, x-runtime-security), `scripts/{up.sh, provision-monolith-db.sh}`, `seeds/localstack/init-queues.sh`, `generated/payouts/arena.toml` ([api],[ledger],[job],[queue],[xas],[configs.account_statement_source_event],[splitz]), `generated/fts/env.arena.toml` ([xas]), `generated/ledger/{arena,default}.toml` ([queue.sqs], journal_create), `verifier/helpers/payouts_flow.py` (ledger helpers), `config/declared-deviations.yaml` (format, DEV-142..151), `RED_LOOP/red_loop/{provisioner.py:46-100,187-225, provisioner_direct.py (all), config.py}`, `RED_LOOP/surface/m4_direct_provision.py`, `reports/implementation/m4-direct-provision.json`, run descriptors under `RED_LOOP/runs/m4-direct-provision-*` |
| Live arena | `docker network ls`, container inventory, `docker inspect monolith-stub`, LocalStack queue/topic/subscription listing, PS MySQL `SHOW COLUMNS` (payouts, banking_account_statement, banking_account_statement_details, reversals), ledger Postgres parents/sub-accounts, running PS/FTS config (`/app/config/*.toml` inside containers) |

## 2. Changes (all uncommitted, in this checkout)

| Path | Change |
|---|---|
| **NEW** `ENV2_COMPOSE/substitutes/xas-sim/server.py` | XAS substitute (stdlib, base_stub): SQS consumer of `x_account_statement_source_event`, source_events with `UNIQUE(event_id, entity_type)`, PS/XAS timestamp field-name mismatch reproduced and logged, matching utr→grn(UPPER)→cms + account/balance + amount, external marking, >2 / dual-type guards, relink refusal, 9-field outbound `payout_update` (Basic auth from the monolith secret volume), `POST /v1/statement/dual_write`, `GET /v1/account_statements/fetch_multiple_by_reference_numbers` (validation + type/channel filter + grpc-gateway string int64s), `/_arena/{state,reset?merchant_id,accounts,statements/sync,source_events/enqueue,source_events}`, shared faults. |
| **NEW** `ENV2_COMPOSE/substitutes/xas-sim/awslite.py` | stdlib LocalStack SQS (JSON protocol) / SNS (query protocol) client with a SigV4-shaped constant-signature header (LocalStack does not verify signatures — verified live with `Signature=deadbeef`). |
| **NEW** `ENV2_COMPOSE/substitutes/xas-sim/CONTRACT.md` | Replaces the xas-sink description; every rule cited to XAS source; 7 deviations; build/run commands. |
| **NEW** `ENV2_COMPOSE/substitutes/xas-sim/test_contract.py` | 23 offline unittest cases (match order, amount/balance/account guards, external, dedup, >2, dual-type, relink refusal, outbound body, dual_write, dedupe tuple, fetch_multiple validation/filter/serialisation, per-merchant reset). `python3 -m unittest substitutes/xas-sim/test_contract.py` → OK. |
| `ENV2_COMPOSE/substitutes/monolith-stub/server.py` | (a) `payout_update` relay now also on the real path `/v1/banking_account_statement/payout_update` (`Route.php:4837`) and forwards the 9-field body verbatim; (b) DA ledger emitter `send_to_ledger_post_source_entity_processing` reproducing `Core.php:3081-3091/5175-5278` + `Ledger/Payout.php:235-425` with all gates, `da_ledger_skipped_ps_recon`, `da_payout_processed`+`_recon`, `da_payout_reversed`+`_recon`, `da_ext_payout_*` on `converted_from_external`, fee variants; SNS topic `api-ledger-journal-create-live` → SQS `journal_create` (self-wired idempotently, `RawMessageDelivery=false`), SQS direct fallback labelled; (c) `GET /_arena/ledger_emits[?merchant_id=]`; (d) `GET|POST /_arena/merchant_features` runtime overrides over merchants.json `merchant.feature`. Existing 18 stub tests still pass. |
| `ENV2_COMPOSE/substitutes/monolith-stub/CONTRACT.md` | New section for the relay + emitter (gates, payload, transport, deviations). |
| `ENV2_COMPOSE/scripts/up.sh` | After `LedgerConfigAPI/CreateInBulk`: `AccountAPI/CreateInBulk{direct_account_x}` (non-2xx tolerated on re-run), `AccountAPI/Activate` for any DA parent still `IN_REVIEW`, and a count check (expects 7 ACTIVATED). `bash -n` clean. |
| `ENV2_COMPOSE/scripts/provision-monolith-db.sh` (**outside my listed ownership, minimal additive change, flagged**) | `monolith_reader` also gets `SELECT` on `payouts.banking_account_statement_details` and `payouts.reversals` (the emitter needs the BASD id and the reversal row; the live grant was applied with the same statements). |
| `ENV2_COMPOSE/config/declared-deviations.yaml` | Appended DEV-160..DEV-166 (my entries only). PyYAML parse OK (83 entries). |
| **NEW** `RED_LOOP/red_loop/ledger_da.py` | `ensure_da_parent_accounts()` (CreateInBulk + Activate, idempotent), `onboard_da_merchant(mid, basd_id, opening)` (CreateOnEvent `direct_merchant_onboarding`, `idempotency-key`), `da_accounts`, `da_sub_accounts`, `da_journals` (journal + entries + balance check), `journal_count`, `idempotency_rows`, `journal_uniqueness`; CLI `ensure-parents|onboard|accounts|journals`. |
| **NEW** `RED_LOOP/surface/m4_xas_ledger.py` | Proof (a)–(h); evidence under `RED_LOOP/runs/m4-xas-ledger-<ts>/` and `reports/implementation/m4-xas-ledger.json`. |
| **NEW** `reports/implementation/m4-xas-ledger.json` | Summary of the final full run (no secrets). |

Not touched: T09 (`provisioner*.py`, `m4_direct_provision.py`) and T10 files (`config/templates/**`, `generate.py`, `init-queues.sh`, `docker-compose.yml`, `cron-driver/**`, `mozart-sim/**`, `bankingaccounts-stub/**`, `bas_fixtures.py`). Fixture merchants ARENAM00000001/2/3 untouched. No compose down, no volume deletion, no secret regeneration.

## 3. xas-sim: exact build / run (for compose transcription)

```
# build (generic substitutes Dockerfile; ${STUB_DIR}/*.py copies awslite.py too)
docker build -f ENV2_COMPOSE/substitutes/Dockerfile --build-arg STUB_DIR=xas-sim --build-arg STUB_NAME=xas-sim \
  --build-arg STUB_PORT=8080 -t rzp-arena/xas-sim:v1-candidate ENV2_COMPOSE/substitutes

# dev run on the live arena (this is what is running now as env2_compose-xas-sim-1)
docker run -d --name env2_compose-xas-sim-1 --network rzp-arena --network-alias xas-sim \
  --read-only --tmpfs /tmp --security-opt no-new-privileges:true \
  -v rzp-arena-secrets-monolith:/run/secrets:ro \
  -e STUB_PORT=8080 -e STUB_NAME=xas-sim \
  -e XAS_API_AUTH_FILE=/run/secrets/monolith_basic_auth -e XAS_API_BASE_URL=http://monolith-stub:8080 \
  rzp-arena/xas-sim:v1-candidate
```

Compose block to add (replace `xas-sink`; PS/FTS `[xas].host` already point at `xas-sim` via T10):

```yaml
  xas-sim:
    build:
      context: ./substitutes
      dockerfile: Dockerfile
      args: {STUB_DIR: xas-sim, STUB_NAME: xas-sim, STUB_PORT: "8080"}
    image: rzp-arena/xas-sim:${ARENA_TAG:-local}
    profiles: ["substitutes"]
    networks: [rzp-arena]
    volumes:
      - secrets-monolith:/run/secrets:ro        # monolith_basic_auth: outbound payout_update credential
    environment:
      STUB_PORT: "8080"
      XAS_SQS_ENDPOINT: http://localstack:4566
      XAS_SOURCE_EVENT_QUEUE: x_account_statement_source_event
      XAS_API_BASE_URL: http://monolith-stub:8080
      XAS_API_AUTH_FILE: /run/secrets/monolith_basic_auth
    <<: *runtime-security
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/health"]
      <<: *healthcheck-defaults
    depends_on:
      localstack: {condition: service_healthy}
      monolith-stub: {condition: service_healthy}
```

`scripts/up.sh` health list: replace `xas-sink` with `xas-sim` in the substitutes healthcheck line (not done by me because the compose service does not exist yet; one-word change). The queue `x_account_statement_source_event` was already present on LocalStack when xas-sim first booted (T10's init-queues); xas-sim also creates it idempotently if absent.

## 4. Restarts performed (owned containers only, plus the sanctioned provisioner)

- `monolith-stub`: rebuilt (`docker compose ... build monolith-stub`) and recreated 4 times (route + emitter + ext-branch iterations) via `docker compose ... up -d --no-deps monolith-stub`.
- `xas-sim`: started with the `docker run` above (new container).
- T09's `provision_direct_merchant("m4-t11-xas")` restarted `monolith-stub, bankingaccounts-stub, dcs-stub, splitz-stub, kong-lite` (its documented behaviour) when provisioning the proof merchant.
- No PS/FTS/ledger/localstack restarts.

## 5. Commands and results

| Command | Result |
|---|---|
| `python3 -m unittest substitutes/xas-sim/test_contract.py` | 23/23 OK |
| `python3 -m unittest substitutes/monolith-stub/test_contract.py` | 18/18 OK (unchanged tests) |
| `python3 RED_LOOP/red_loop/ledger_da.py ensure-parents` (twice) | first: 7 parents created (`IN_REVIEW`) then 7× `Activate` → 200; second: `existed_before: true, created: 0`, all `ACTIVATED` |
| T09 module `provision_direct_merchant("m4-t11-xas")` + `self_check_direct_merchant` | merchant **ARENAD85474858**, balance ARENADB5474858, account 2323000085474858, BASD ARENADS5474858, FTS 974858; 0 failed steps; self-check 29/29 |
| `ledger_da.py onboard ARENAD85474858 ARENADS5474858 0` | 200; 6 sub-accounts `ACTIVATED`, `min_balance NULL`, entities `{account_type, fund_account_type, banking_account_stmt_detail_id:["basd_ARENADS5474858"]}` |
| `m4_xas_ledger.py run --steps abcd` (3 iterations: route 404 fix, `monolith_reader` grant fix) | final abcd: 20/20 |
| `m4_xas_ledger.py run --steps efgh` | e 5/5, g 4/4, h 3/3, f 2/4 (401: wrong harness credential) |
| `m4_xas_ledger.py run --steps f` (API credential; assertion reads PS log) | 4/4 |
| Full run 193850Z `--steps abcdefgh` | a 4/4, b 2/2, c 9/9, d 5/5, f 4/4, g 4/4, h 3/3; e 4/5 (emitter skipped: monolith-stub restarted by another agent at 19:39:32Z wiped the runtime feature override — §11) |
| Full run 194211Z (after the feature-persistence fix) | every payout create 502 `upstream_unreachable`: payouts-api had panicked at 19:41:42Z (§11) |
| `m4_xas_ledger.py merge --descriptor …` | `reports/implementation/m4-xas-ledger.json` = per-step latest passing run (source_run_dir per step: a←194211Z, b/c/d/f/g/h←193850Z, e←193513Z), **all 36 checks pass, all_green=true, merged=true** |
| Re-run queued behind a payouts-api health wait | if it completes it overwrites the summary with a single-run result (see final message) |

## 6. Evidence (final full run)

Run dirs: `RED_LOOP/runs/m4-xas-ledger-{193413Z,193513Z,193813Z,193850Z,194211Z}/proof.json` (+ `descriptor-ARENAD85474858.json` with secret, git-ignored); merged view `RED_LOOP/runs/m4-xas-ledger-merged/proof.json`. Summary: `reports/implementation/m4-xas-ledger.json` (`steps.<x>.source_run_dir` names the run each step's evidence comes from). Per step:

- **(a)** real ledger: 7 DA parents (`AccountAPI/CreateInBulk{direct_account_x}` + `Activate`), 6 merchant sub-accounts via `CreateOnEvent{direct_merchant_onboarding}`; discovery simulation (jsonb `@>` per role) resolves every role; CommissionIncome vs MerchantCash share `{cash, merchant_da}` and are split by `account_category` (revenue vs asset), as the ledger's `AccountDiscoveryParams` does. **The 4 pre-existing Shared-style sub-accounts created by T09's provisioner do not interfere**: their entities carry `banking_account_id`/`merchant_va*`, never `banking_account_stmt_detail_id`, so DA containment queries cannot select them (checked live).
- **(b)** real: kong → PS → FTS → mozart-sim success → monolith relay → `processed`; **the REAL PS producer emitted the source event** (T10's `[configs.account_statement_source_event] enabled_env=true`, `[job]` key + queue) and xas-sim consumed it from SQS; `_schema.event_created_timestamp_present=true`, `event_create_timestamp_present=false`, stored `event_create_timestamp=0` — the production mismatch reproduced.
- **(c)** statement: `substitute_injected` (no real `rbl_banking_account_statement` row existed for the UTR at run time; the harness inserted the worker-shaped row into the REAL PS table and mirrored it to xas-sim); matching: substitute; xas-sim → 9-field `payout_update` (200) → monolith-stub → REAL PS `UpdatePayoutAfterBASRecon` set `payouts.transaction_id = <bas id>`; emitter published `da_payout_processed` + `da_payout_processed_recon` over `sns:api-ledger-journal-create-live->sqs:journal_create` (SNS message ids recorded; LocalStack subscription confirmed); REAL `ledger-worker-journal-create` wrote both journals (worker log `JOURNAL_CREATE_JOB_DATA_SUCCESS`); entries balanced — e.g. `da_payout_processed`: Dr Merchant Balance 13600, Cr Vendor Payable 13600, Dr Commission Receivable 200, Cr Commission Income 164, Cr Output GST 36; `_recon`: Dr Vendor Payable 13600, Cr Merchant Cash 13600 — all on the merchant's 6 DA sub-accounts; `(transactor_id, transactor_event)` unique.
- **(d)** replay of the same source event (same `event_id`) → one stored row, `_replays=1`, no second outbound; re-sync of the same statement → dedupe tuple hit, nothing; direct replay of the 9-field `payout_update` → PS 200 (`PayoutTransactionIDAlreadyUpdated`), `transaction_id` unchanged, emitter re-published two messages with **fresh uuid1 idempotency keys (as the monolith does)** → ledger worker log `JOURNAL_CREATE_BY_JOB_VALIDATION_FAILED … record_already_exist: BAD_REQUEST_RECORD_ALREADY_EXIST` and `JOURNAL_CREATE_JOB_DATA_ERROR` (dropped, no retry) → still exactly 2 journals. **Layer that stopped it: layer 2, application `ValidateJournalExist(transactor_id, transactor_event)`; layer 1 (idempotency table) cannot help because every publish carries a new key** (my `idempotency` table read-back for those keys returned no rows; the log is the proof).
- **(e)** real failed payout (mozart failure; `utr` NULL, `gateway_ref_no` NULL) → PS producer emits nothing (`ErrorPayoutUtrNotFoundForAccountStatementEvent` path: no reference) → event `substitute_injected` with `cms_ref_no` on the real queue; credit statement `substitute_injected` with matching `bank_transaction_id` → xas-sim matched on `cms_ref_number` → `payout_reversal` → PS created the reversal, `failed → reversed`, `reversals.transaction_id = <credit bas id>`; emitter posted `da_payout_reversed` + `da_payout_reversed_recon` (transactor `rvrsl_<id>`), ledger recorded both balanced. Note the PS reversal `amount` = payout amount + fees (13211 vs 13011) while the journal amount uses the payout amount (monolith `getDefaultPayloadForDirectPayout` reads the payout, not the reversal).
- **(f)** real PS route `POST /v1/payouts/transfer_status_webhook` (authenticated with the API service credential — the route allows `cred.API` and `cred.FTS`; the arena's FTS→PS pair is not materialised for the harness) on an `initiated` payout held at mozart, with an external debit statement present: PS queried xas-sim three times (`type=debit` utr → 1 external row; `type=credit` → 0; `type=debit` again in `VerifyPayoutFailedTransaction`), logged `FAILED_PAYOUT_HAS_DEBIT_STATEMENT_NEEDS_TO_BE_REVERSED` and `STATUS_UPDATE_FROM_TRANSFER_STATUS_WEBHOOK_FAILED`, answered HTTP 500 (generic body — the errorclass is `internal_server_error`), payout stayed `initiated`. xas-sim `fetch_log` holds request/response (Basic user `payouts`). The hold was then released with success (payout `processed`, real UTR overwrote the webhook's UTR; the external statement stays unlinked because its UTR differs).
- **(g)** `payout_service_txn_recon` on: PS still linked (`transaction_id` set — the link is PS's, not the ledger's), emitter recorded `skipped: da_ledger_skipped_ps_recon` and published nothing, **no DA journal rows** for the payout. The preserved production gap.
- **(h)** wrong-amount statement → xas-sim `external`, no entity, no outbound, `transaction_id` NULL, no journals.

## 7. Deviations (declared in `config/declared-deviations.yaml`)

DEV-160 xas-sim ingestion is a PS-table mirror · DEV-161 CDC dual-write is synchronous · DEV-162 timestamp mismatch kept, pull fallback 501, inbound auth unverified, in-memory · DEV-163 emitter trigger relocated to `payout_update` arrival · DEV-164 `notes.transaction_id = bas_<id>` / `api_transaction_id = bas id` / `transaction_date = payouts.updated_at` · DEV-165 SNS topic self-wired on LocalStack, labelled SQS fallback · DEV-166 `payout_service_txn_recon`/`da_ledger_journal_writes` are stub features; `monolith_reader` grants.

Additional observations worth a contradiction/L-entry:
- XAS marks a statement `external` locally only; nothing is sent to the monolith/PS for external rows (`dual_write.Update` filters to payout types), so the `da_ext_debit/da_ext_credit` journals of T03 Finding 6 are never produced by the XAS path — only by the monolith's own fetch path or PS's ART route. The twin's ext→payout re-class (`da_ext_payout_processed`) is therefore emitted without a preceding `da_ext_debit` when an XAS-external row is later matched (`converted_from_external=true`).
- The ledger's `da_payout_processed` journal `amount` is payout amount + commission (13800 for 13600+200): that is the real `ledger_config` arithmetic, not the substitute.

## 8. Unresolved

1. `scripts/up.sh` still lists `xas-sink` in the substitutes healthcheck; swap to `xas-sim` when the coordinator wires the compose service (and drop the `xas-sink` service).
2. `seeds/localstack/init-queues.sh` (T10) could create the SNS topic `api-ledger-journal-create-live` and its `journal_create` subscription at boot; today monolith-stub self-wires it on first emit (works, DEV-165).
3. The apidb `features` seed carries no `da_ledger_journal_writes` row for Direct merchants: the REAL PS DA-external path (ART `Unreconciled` → `da_ext_debit`) stays gated off in PS unless T09's provisioner adds that row; the substitute emitter uses its own feature view (DEV-166).
4. Real-worker statement rows: the proof injected worker-shaped rows because no real `rbl_banking_account_statement` row existed for the payout UTRs at run time; when T10's ingestion is exercised for a merchant first, `m4_xas_ledger.py` automatically prefers the real row (`statement_fidelity: real`).
5. The `idempotency` table read-back (`ledger_da.idempotency_rows`) returned no rows for the replayed keys; the worker log proves the layer-2 refusal, but the table/partition the idempotency provider writes to was not chased.
6. FTS retry-preprocessor gate (`CodeDebitEntryExistInStatement`) was not exercised (no FTS retry flow in scope); `[xas].host` is wired and the GET route is served, so it can be proven from the FTS side later.
7. Whether the parent activation step (`AccountAPI/Activate`) matches production ops is inferred from the seeded Shared parents being `ACTIVATED`; journal creation itself has no status gate in `journal/validation.go` / `account_discovery`.

## 9. Confidence

- xas-sim contract (match order, guards, dedup, outbound body, fetch route shape): **HIGH** (each rule read in source; 23 offline cases; live PS/FTS consumers parsed the responses).
- DA emitter payload vs `Ledger/Payout.php`: **HIGH** for fields/gates/events; **MEDIUM** on the three declared substitutions (DEV-163/164) which cannot be made faithful without a monolith transactions table.
- Ledger leg (real accounts, real worker, balanced entries, layer-2 dedup): **HIGH** (live rows and worker logs).
- "PS-recon merchants get no DA journal in production": unchanged from T03 — **MEDIUM**, reproduced as a stub feature, not proven for production cohorts.

## 10. Fresh-boot instructions

1. Coordinator: add the `xas-sim` compose block (§3), remove `xas-sink`, swap the health list in `up.sh`.
2. `scripts/up.sh` now creates + activates the 7 DA parents after the ledger configs (idempotent).
3. Per Direct merchant: `python3 RED_LOOP/red_loop/ledger_da.py onboard <mid> <basd_id> [opening_paise]` after T09's provisioner (or call `ledger_da.onboard_da_merchant` from it); set `da_ledger_journal_writes` in the merchant's `merchants.json` `merchant.feature` list (or `POST monolith-stub /_arena/merchant_features` at runtime).
4. Register the merchant's XAS account: `POST xas-sim /_arena/accounts {merchant_id, account_number, channel, balance_id}` (the proof does it in step (a)).
5. Re-prove: `python3 RED_LOOP/surface/m4_xas_ledger.py run --descriptor <descriptor.json>` (or `--provision <campaign>`), ~6 min, 5 payouts on the merchant.

## 11. Concurrent-arena events observed during the proofs (for the coordinator)

- 19:39:32Z `monolith-stub` was restarted by another agent (T09/T10 provisioner path) between my step (c) and step (e); the in-memory feature override vanished and (e) was skipped with `da_ledger_journal_writes_off`. Fix applied: the proof re-applies the override before every emitter-dependent step and persists `da_ledger_journal_writes` in the merchant's `merchants.json` `merchant.feature` list (boot-time source). End state of `merchants.json` for ARENAD85474858: `[..., da_ledger_journal_writes]` (`payout_service_txn_recon` removed again by step (g)).
- 19:41:42Z the REAL `payouts-api` **panicked and exited (code 2)** while serving another agent's merchant ARENAD80422548: `panic: type mismatch: cannot convert int64 to bool` in `pkg/dcs/features/features.go:252 EnabledFeaturesForKeyFromResponse` right after `MERCHANT_CONFIG_FETCH_FEATURES_FROM_DCS_REQUEST` — a dcs-stub seed value of the wrong type (int64 instead of bool) crashes the accepted PS binary, and the container has no restart policy. `kong-lite` was also recreated twice in that window. My full run 194211Z therefore failed at every payout create (`upstream_unreachable`); I did NOT restart payouts-api (core container, not mine). The final proof run was re-queued behind a payouts-api health wait.

# Per-arena generated artifacts -- never commit credentials or rendered
# config that embeds them. Regenerated by secrets/gen-secrets.sh and
# config/generate.py on every scripts/up.sh run.
secrets/*.txt
secrets/jwks.json
secrets/.env.secrets
generated/
SAFETY_PREFLIGHT.md
EGRESS_AUDIT.md
snapshots/
__pycache__/
*.pyc

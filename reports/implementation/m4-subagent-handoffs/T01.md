# T01 — Baseline and Git Integrity Auditor — handoff

Task ID: **T01** (Milestone 4, Phase 0). Date: 2026-09-06 (18:20–18:35Z).
Verdict: **BASELINE_HOLDS** (full report: `reports/implementation/m4-baseline-report.md`).

## Inspected files / commits

- `reports/implementation/m3-1-acceptance.json`, `RED_LOOP/surface/m3_1_acceptance.py`
- `RED_LOOP/archive/evidence-manifest.json` (+ `.sha256`), `RED_LOOP/archive/build_manifest.py`
- `ENV2_COMPOSE/scripts/{clean-boot.sh,instance-plan.py,instance-cleanup.py,fingerprint.py,healthcheck.sh,up.sh,golden-run.sh}`
- `reports/implementation/RED_LOOP_MILESTONE3_1_COMPLETION.md` (sections "Proof of no writable overlap; live-arena disposition", "Clean boot results")
- `RED_LOOP/runs/live-arena-snapshot-20260906T120713Z.json`, `RED_LOOP/runs/cleanboot-m31-clean-a/**`
- `ENV2_COMPOSE/.env.arena` (subnets/port/enforcement keys only), `ENV2_COMPOSE/.runtime/arena-fingerprint.json`
- Commits/tags: `red-loop-m3.1` → `3a044f8`, `twin-v1.0` → `78def24`, `4d78f86`, `3371d82`, `aca25d2`, `219ca48`, `a107612`, `3873e96`, `39fe41e`

## Source-backed conclusions

1. `red-loop-m3.1` is an annotated tag (`git cat-file -t` = tag) resolving to `3a044f83ed0eda6ead71585f07c450af2ed7978f`, an ancestor of HEAD. Tree clean before and after.
2. HEAD moved during the task: `4d78f86` (task start) → `3371d82` (coordinator commit while the clean boot ran). Diff is `RED_LOOP/m4/gate-catalog.json` + `reports/implementation/m4-decisions.md` only; no runtime input changed (ENV2_COMPOSE `config_digest` unchanged). Tested commit for the clean boot = `3371d82` (from the boot fingerprint).
3. Hash recompute: 9/9 `evidence_sha256` match; manifest file hash `266756e4…` = companion = acceptance gate; `self_sha256` `504b777d…` recomputed with the canonical-JSON scheme and matches; 1917/1917 manifest entries match (0 missing, 0 mismatch).
4. Evaluator re-run (scratch `--out`): 20/21 gates identical. `historical_integrity` fails only on the hardcoded branch-name allowlist in `m3_1_acceptance.py` (`branch in ("milestone-3-1-clean-parity", "milestone-3-1-calibration", "milestone-3-1-claude-final")`); its commit checks still pass. Not drift.
5. Clean boot `m4-base-a`: 26/0/0, egress passed, 0 outside packets, 38,123 control packets, coverage proven, boot_id `45e3f0a6-1cfb-481e-8552-1beaf309199c`. Five core image IDs identical to the live arena. Only `docker-compose.yml` differs from the live tree in the fingerprint (clean-boot.sh patches subnet literals in the disposable copy, by design).
6. Live arena restored: 66 running / 65 healthy (cron-driver has no healthcheck), same network IDs (`bedd720758fa`, `72e15908c82e`), 16 volumes, identical 67 container IDs, kong healthy on :18080, `KONG_ENFORCE_ROUTE_POLICY=1`.
7. Pre-existing observations, not caused by this task: the live arena's network IDs differ from the M3.1 12:07Z snapshot because the arena was re-created at 13:40Z from commit `39fe41e5` (pre-tag); the M3.1 completion report says enforcement was "restored to 0" but the accepted `hardened_gateway` gate requires (and the live arena has) enforcement=1. The acceptance artifact records head `aca25d2` (the commit before the tag commit that added it).

## Changes made

Only: `reports/implementation/m4-baseline-report.md` (new), `reports/implementation/m4-subagent-handoffs/T01.md` (this file), and git-ignored runtime evidence under `RED_LOOP/runs/m4-baseline-20260906T182802Z/`, `RED_LOOP/runs/cleanboot-m4-base-a/`, `.local/m31-clean/m4-base-a/` (disposable working copy; safe to delete). Nothing committed, nothing pushed, no historical file touched.

## Commands run (summarized)

| Step | Command | Result |
|---|---|---|
| Git | `git cat-file -t red-loop-m3.1; git rev-parse 'red-loop-m3.1^{commit}'; git merge-base --is-ancestor …; git status --porcelain` | tag / `3a044f8` / ancestor / clean |
| Hashes | Python SHA-256 over 9 evidence files, manifest, companion, 1917 entries; canonical-JSON self-hash | all match |
| Gates | `python3 RED_LOOP/surface/m3_1_acceptance.py --campaign camp-20260906T154844Z-23100f --out <scratch>/m3-1-acceptance-reeval.json` | exit 1, unmet = [historical_integrity] only |
| Baseline | `docker ps -a`, `docker image inspect --format '{{.Id}} {{.RepoDigests}}'`, `python3 scripts/fingerprint.py --output …`, `shasum -a 256 …` | `RED_LOOP/runs/m4-baseline-20260906T182802Z/` |
| Preflight | `python3 ENV2_COMPOSE/scripts/instance-plan.py m4-base-a` | SAFE TO LAUNCH, no overlap, port 18846 |
| Stop live | `cd ENV2_COMPOSE && docker compose --env-file .env.arena --profile datastores --profile substitutes --profile core stop` | 0 running, 16 volumes kept |
| Clean boot | `bash ENV2_COMPOSE/scripts/clean-boot.sh m4-base-a` (2 min) | exit 0, 26/0/0, egress 0 outside |
| Teardown | `python3 ENV2_COMPOSE/scripts/instance-cleanup.py m4-base-a` | CLEAN=True (0/0/0 left) |
| Restart live | `cd ENV2_COMPOSE && docker compose --env-file .env.arena --profile datastores --profile substitutes --profile core start` | 66/65 healthy after 10 s |
| Verify | network inspect, volume ls, kong inspect/curl, container-id set compare | all unchanged |

## Evidence paths (absolute)

- `/Users/rana.singh/rzp-payouts-architecture/reports/implementation/m4-baseline-report.md`
- `/Users/rana.singh/rzp-payouts-architecture/RED_LOOP/runs/m4-baseline-20260906T182802Z/` — `git.txt`, `docker-ps.txt`, `image-digests.txt`, `arena-fingerprint-live.json`, `config-fixture-spec-sha256.txt`, `seeds-generated-sha256.txt`, `live-arena-snapshot-20260906T182802Z.json`, `m3-1-acceptance-reeval.json`, `orchestrator.log`, `clean-boot-m4-base-a.log`, `live-restoration-proof.txt`
- `/Users/rana.singh/rzp-payouts-architecture/RED_LOOP/runs/cleanboot-m4-base-a/` — `instance-plan.json`, `up.log`, `verifier.log`, `verifier-run/{junit.xml,egress.json,dns.json,arena-fingerprint.json,test_*.jsonl}`

## Unresolved questions

- Whether M4 should keep `m3_1_acceptance.py`'s branch allowlist as-is (historical evaluator, frozen) or whether the M4 evaluator should carry its own `historical_integrity` gate accepting `milestone-4-direct-reconciliation`. T01 did not modify the evaluator.
- `RED_LOOP/runs/m4-baseline-*/` and `cleanboot-m4-base-a/` are not yet in the evidence manifest; if M4 wants them hash-bound, `build_manifest.py` must be re-run (it would rewrite the M3.1 manifest — decide whether M4 uses a separate manifest instead).
- The evidence manifest's 47 `missing_expected_campaign_files` (calibfx-* and cleanboot-* dirs lack campaign-style files) are pre-existing and not gate inputs.

## Confidence

High for git integrity, hash recompute, gate re-evaluation and clean-boot totals (all machine-derived, artifacts retained). High for live-arena restoration (container-ID set, network IDs, volume count and kong reachability all verified after restart).

## Handoff instructions for the coordinator

- Reproduce the clean boot (sequential; only one 66-container arena fits in the 11.65 GiB Docker VM):
  ```
  cd /Users/rana.singh/rzp-payouts-architecture/ENV2_COMPOSE
  docker compose --env-file .env.arena --profile datastores --profile substitutes --profile core stop
  bash scripts/clean-boot.sh m4-base-a          # ~2 min; writes RED_LOOP/runs/cleanboot-m4-base-a/
  python3 scripts/instance-cleanup.py m4-base-a
  docker compose --env-file .env.arena --profile datastores --profile substitutes --profile core start
  ```
  Use a fresh instance id (e.g. `m4-base-b`) to avoid overwriting `cleanboot-m4-base-a/` evidence; `clean-boot.sh` rewrites `RED_LOOP/runs/cleanboot-<id>/` in place.
- Live arena state at handoff (18:33Z): project `env2_compose` running, 66 running / 65 healthy / 0 unhealthy (cron-driver has no healthcheck; ledger-scheduler is a one-shot, exited 0), `KONG_ENFORCE_ROUTE_POLICY=1` (hardened), kong on host port 18080, networks `rzp-arena` `bedd720758fa…` / `rzp-ingress` `72e15908c82e…`, 16 volumes, no disposable-instance resources left.
- Working tree: clean apart from the two new report files; commit them together with nothing else from this task.
